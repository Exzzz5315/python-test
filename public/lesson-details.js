'use strict';

const chapter = (title, question, explanation, code, steps, bridge, checkpoint) => ({
  title,
  question,
  explanation,
  code,
  steps,
  bridge,
  checkpoint,
});

const lessonDetail = (why, prerequisites, chapters, mistakes, drills, glossary) => ({
  why,
  prerequisites,
  chapters,
  mistakes,
  drills,
  glossary,
});

window.LESSON_DETAILS = {
  1: lessonDetail(
    [
      '学习一门编程语言，第一步不是背语法，而是弄清楚“代码写在哪里、由谁执行、结果在哪里出现”。如果这三个问题没有建立起来，后面的每一个报错都会像黑盒。',
      '今天只完成一条最基本的闭环：在编辑器中写入 Python 文本，保存为 .py 文件，通过解释器运行，在终端读取输入并看到输出。你还会学会阅读最基础的错误信息。',
    ],
    [
      ['代码', '写给计算机的指令文本。Python 代码通常保存在扩展名为 .py 的普通文本文件中。'],
      ['程序', '为了完成一个目标而组织起来的一组代码。例如读取姓名并打印欢迎语，就是一个很小的程序。'],
      ['编辑器', '帮助你书写和管理文本文件的工具。Windsurf、VS Code、PyCharm 都属于编辑器或集成开发环境。'],
      ['终端', '通过文本命令操作电脑的窗口。你在这里告诉 Python 运行哪个文件，也会在这里看到输入提示和报错。'],
    ],
    [
      chapter(
        '代码是怎样运行起来的',
        '从点击运行到看到结果，中间发生了什么？',
        [
          '你写下的 test.py 只是一个文本文件，它本身不会执行。Python 解释器是一个已经安装在电脑上的程序，它读取 .py 文件，理解其中的语句，再按照顺序执行。命令 python test.py 的意思就是“请 Python 解释器读取并执行 test.py”。',
          'Python 通常从文件第一行开始，自上而下执行。遇到函数定义时，它先记住函数的名字和内容，并不会立刻运行函数体；直到后面真正调用 main()，函数体才会执行。',
          'Python 会自动把部分代码转换成字节码，但初学阶段不需要手动编译。你真正需要掌握的是保存文件、选择正确解释器、执行文件和阅读终端输出。',
        ],
        'print("第一行")\nprint("第二行")\nprint(1 + 2)',
        [
          '解释器先读取第一条 print，终端输出“第一行”。',
          '继续执行第二条 print，终端输出“第二行”。',
          '执行第三行时先计算 1 + 2 得到 3，再把 3 交给 print。',
          '文件末尾没有更多语句，程序正常结束，终端重新出现命令提示符。',
        ],
        '可以把解释器类比成浏览器的 JavaScript 引擎。浏览器读取 JavaScript，Python 解释器读取 .py 文件；区别是 Python 程序常从终端启动。',
        ['如果文件只有 def main(): ...，但没有调用 main()，会输出什么？', '不会输出。def 只定义函数，只有 main() 调用发生后函数体才运行。'],
      ),
      chapter(
        'print、input 与程序的数据流',
        '程序如何接收信息，又如何把结果告诉我们？',
        [
          'print() 是一个已经由 Python 提供的函数，它把括号中的值输出到终端。字符串需要放在引号中，因为引号告诉 Python 这是文本，而不是变量名。',
          'input() 会先显示提示文字，然后暂停程序等待用户输入。用户按回车后，input() 把输入内容作为字符串返回。等号把这个结果绑定到左边的变量名，后续代码就可以通过变量名使用它。',
          '一个最小程序可以看成三段：输入、处理、输出。即使需求以后很复杂，也仍然是在这条数据流上增加校验、计算、存储和网络交互。',
        ],
        'name = input("请输入你的名字：")\nmessage = "你好，" + name\nprint(message)',
        [
          'input 显示提示并等待键盘输入。',
          '假设输入“小明”，input 返回字符串“小明”，变量 name 指向这个字符串。',
          '加号连接两个字符串，得到“你好，小明”，并交给变量 message。',
          'print 把 message 指向的字符串输出到终端。',
        ],
        '这和前端表单很相似：input() 相当于读取输入框的 value，变量保存数据，print() 相当于把结果渲染到控制台。',
        ['input() 返回的内容默认是什么类型？', '始终是字符串 str。即使输入 18，得到的也是文本“18”，需要 int() 才能转换为整数。'],
      ),
      chapter(
        '函数、缩进和 main 入口',
        '为什么代码要放进 main()，冒号和缩进又代表什么？',
        [
          '函数是一组暂时命名和收纳起来的步骤。def main() 定义一个名为 main 的函数，冒号表示接下来开始函数体。函数体必须向右缩进，Python 推荐四个空格。缩进不是装饰，它决定代码属于哪个代码块。',
          'if __name__ == "__main__": 是入口判断。当你直接运行 test.py 时，Python 会把特殊变量 __name__ 设为 "__main__"，条件成立，于是调用 main()。如果以后其他文件导入 test.py，这个条件不成立，程序不会在导入时突然询问用户输入。',
          '零基础阶段先记住固定结构即可：业务步骤写进 main，文件末尾保留入口判断。随着模块学习深入，你会真正理解导入时机和模块名称。',
        ],
        'def main() -> None:\n    name = input("请输入姓名：")\n    print(f"你好，{name}！")\n\n\nif __name__ == "__main__":\n    main()',
        [
          'def 让 Python 创建 main 函数，-> None 表示这个函数不返回业务结果。',
          '定义阶段不会执行 input 和 print。',
          '执行到 if 时，直接运行文件会让条件成立。',
          'main() 调用函数，程序跳到函数体，按缩进内的顺序执行。',
        ],
        'JavaScript 用花括号划分函数和 if 的代码块，Python 用冒号加缩进。两者表达同一个结构，但 Python 会把错误缩进直接视为语法问题。',
        ['main() 前面为什么需要缩进？', '因为它属于 if 条件成立后才执行的代码块。没有缩进就不属于 if。'],
      ),
      chapter(
        '第一次读懂 traceback',
        '程序报错时应该从哪里看起？',
        [
          '报错不是学习失败，而是解释器给出的诊断报告。完整错误通常包含文件路径、出错行号、那一行代码、异常类型和说明。初学时先看最后一行，再回到它指出的文件与行号。',
          'SyntaxError 表示代码结构不符合语法，例如 if 行末写成分号而不是冒号。NameError 常表示使用了尚未定义的变量。IndentationError 表示缩进层级不正确。',
          '每次只修错误报告指向的第一个问题，保存后重新运行。不要同时随意改很多行，否则很难知道是哪项修改解决了问题。',
        ],
        'if True;\n    print("这段代码会报错")',
        [
          '最后一行会显示 SyntaxError。',
          '上方箭头通常指向分号附近。',
          'if 条件后需要冒号，把分号改成冒号。',
          '保存文件并重新运行，确认错误消失。',
        ],
        '这和浏览器控制台的报错定位方式相同：先读错误类型和消息，再跳到具体文件与行号。',
        ['终端出现错误后，最先应该做什么？', '阅读最后一行的异常类型和说明，再查看上方对应的文件与行号。'],
      ),
    ],
    [
      ['终端提示 command not found: python', '当前终端没有激活课程虚拟环境。', '在“练习代码”目录执行 source ../.venv/bin/activate，或者直接使用 ../.venv/bin/python。'],
      ['if 行末写成分号', 'Python 代码块前必须使用冒号。', '写成 if condition:，下一行向右缩进四个空格。'],
      ['点击运行却没有任何输出', '可能只定义了函数，没有调用，或文件尚未保存。', '检查入口守卫和 main()，按 Command + S 后重新运行。'],
      ['输入数字后无法直接加 1', 'input 返回字符串，字符串不能和整数直接相加。', '使用 int(raw) 转换；类型转换会在第二课详细学习。'],
    ],
    [
      ['跟写', '亲手输入三行 print，让它分别输出文字、数字和 2 + 3 的计算结果。', '不要复制粘贴，注意文本必须有引号。', '终端依次看到三行结果，最后一行是 5。'],
      ['改写', '修改个人名片程序，再接收城市并输出完整欢迎语。', '增加 city = input(...)，在 f-string 中使用 {city}。', '输入姓名和城市后，输出同时包含两项信息。'],
      ['排错', '故意删除 if 行末冒号并运行，读完错误后恢复。', '重点观察异常类型、行号和箭头位置。', '你能不用搜索，根据 traceback 自己恢复程序。'],
    ],
    [['解释器', '读取并执行 Python 代码的程序。'], ['语句', '让 Python 执行一个动作的完整代码单位。'], ['函数', '有名称、可被调用的一组步骤。'], ['字符串', '用于表示文本的数据类型。'], ['traceback', 'Python 报错时展示的调用路径和错误信息。']],
  ),
  2: lessonDetail(
    [
      '第一课已经建立输入到输出的闭环，第二课开始回答一个关键问题：程序中的“数据”究竟是什么。订单金额、用户名、是否登录和缺失值看起来不同，Python 会使用不同类型表达它们。',
      '类型决定一个值允许做什么操作。数字可以计算，字符串可以拼接和切片，布尔值用于判断，None 表示没有值。理解类型后，你才能读懂大多数 TypeError，也能避免把表单文本直接当数字计算。',
    ],
    [
      ['值', '程序正在处理的具体数据，例如 18、3.14、"小明"、True。'],
      ['变量', '指向某个值的名称。变量不是值本身，也不是固定类型的盒子。'],
      ['类型', '值所属的类别，它规定可以进行哪些操作。例如 int 支持加减，str 支持拼接。'],
      ['表达式', '能够计算出一个值的代码片段，例如 2 + 3、name.strip()、price * count。'],
    ],
    [
      chapter(
        '变量是名称与对象的绑定',
        '执行 amount = 100 时，Python 到底保存了什么？',
        [
          'Python 先创建或取得整数对象 100，再让名称 amount 指向它。以后执行 amount = 200，并不是把原来的 100 改造成 200，而是让 amount 改为指向新的整数对象 200。',
          '这种模型解释了动态类型：变量名称本身没有永久类型，它当前指向的对象才有类型。虽然 Python 允许同一名称先指向数字再指向字符串，业务代码仍应让含义保持稳定，否则阅读和排错会变难。',
          'type(value) 可以查看对象类型，id(value) 可以帮助观察对象身份。初学时不需要记住 id 的具体数字，只要理解两个名称可能指向同一对象。',
        ],
        'amount = 100\nprint(type(amount))\n\namount = 200\nprint(amount)\n\nlabel = amount\nprint(label)',
        [
          'amount 首先指向整数 100，type 显示 int。',
          '第二次赋值让 amount 改为指向整数 200。',
          'label = amount 让 label 也指向当前的 200。',
          '打印 label 得到 200，但名称 label 和 amount 仍是两个不同名称。',
        ],
        '这和 JavaScript 的变量引用模型很接近。区别是 Python 常用 type() 探查运行时类型，并通过类型标注帮助编辑器理解预期类型。',
        ['执行 x = 1，然后 x = "一" 是否允许？', '语法上允许，因为名称可以重新绑定；实际业务代码不建议让同一名称表达两种完全不同的含义。'],
      ),
      chapter(
        '数字、布尔值与 None',
        '如何为数量、价格、真假和缺失选择类型？',
        [
          'int 表示没有小数部分的整数，适合数量、序号和以“分”为单位的金额。float 表示浮点数，适合测量或统计近似值，但二进制浮点无法精确表示所有十进制小数，因此重要金额通常使用整数分或 Decimal。',
          'bool 只有 True 和 False 两个值，用于表达条件结果。比较表达式 18 >= 16 会计算出布尔值。Python 区分大小写，必须写 True 和 False，不能写 true 和 false。',
          'None 表示“没有值”或“尚未找到”。它不同于 0、空字符串和 False。判断 None 使用 is None，这表达对象身份；普通数值和文本是否相等使用 ==。',
        ],
        'quantity = 3\nunit_cents = 1299\ntotal_cents = quantity * unit_cents\n\nis_member = True\ndiscount = None\n\nprint(total_cents)\nprint(discount is None)',
        [
          'quantity 与 unit_cents 都是 int，相乘仍得到 int 3897。',
          'is_member 保存布尔值 True，可以直接用于 if 判断。',
          'discount 暂时没有值，因此使用 None。',
          'discount is None 计算出 True，再由 print 输出。',
        ],
        'JavaScript 有 null 和 undefined，Python 主要用 None 表达缺失。JavaScript 的 === 对应场景在 Python 中通常拆为 == 的值比较和 is 的身份比较。',
        ['为什么订单金额建议保存为 1299 分，而不是 12.99 元的 float？', '整数分可以精确计算，避免浮点表示带来的小数误差。展示时再格式化为两位小数。'],
      ),
      chapter(
        '字符串不是一团文字，而是字符序列',
        'Python 如何读取、截取和清理文本？',
        [
          'str 用于文本。单引号和双引号都能创建字符串，项目中保持一致即可。字符串内部的每个字符都有位置，索引从 0 开始；负索引从末尾反向计算。',
          '切片 text[start:end] 取得从 start 开始、到 end 之前结束的部分，不包含 end。省略 start 表示从开头开始，省略 end 表示直到末尾。这个“左闭右开”规则也会出现在 range 和列表切片中。',
          '字符串不可变。strip()、lower()、replace() 等方法不会修改原字符串，而是返回新字符串。因此 cleaned = raw.strip() 需要接住返回值。',
        ],
        'raw = "  Python  "\ncleaned = raw.strip()\n\nprint(cleaned[0])\nprint(cleaned[-1])\nprint(cleaned[0:3])\nprint(raw)\nprint(cleaned)',
        [
          'raw 包含首尾各两个空格。',
          'strip 返回新字符串“Python”，cleaned 指向它。',
          'cleaned[0] 是 P，cleaned[-1] 是 n。',
          'cleaned[0:3] 取得索引 0、1、2，结果是 Pyt。',
          'raw 仍保留空格，证明字符串方法没有原地修改它。',
        ],
        '字符串和 JavaScript string 一样不可变，slice 思路也类似。Python 额外支持负索引和更简洁的 [start:end] 语法。',
        ['"Python"[1:4] 的结果是什么？', '结果是 "yth"。从索引 1 开始，取到索引 4 之前。'],
      ),
      chapter(
        '类型转换与 f-string',
        '为什么 input 得到的数字不能直接计算？',
        [
          'input() 始终返回 str，因为键盘输入本质上是文本。要做算术，必须显式调用 int() 或 float()。如果文本无法转换，Python 会抛出 ValueError，这正是程序应当处理的输入边界。',
          'str() 可以把数字转换成文本，但拼接大量不同类型很繁琐。f-string 在引号前加 f，并在花括号中放表达式，Python 会计算表达式并格式化到字符串中。',
          '格式规则 :.2f 表示按浮点形式显示两位小数。金额内部仍可保存整数分，展示时用 total_cents / 100 得到元并格式化，这样计算精确、展示友好。',
        ],
        'raw_age = input("请输入年龄：")\nage = int(raw_age)\nnext_age = age + 1\nprint(f"明年你将 {next_age} 岁")\n\ntotal_cents = 3897\nprint(f"订单金额：¥{total_cents / 100:.2f}")',
        [
          '用户输入 18，raw_age 得到字符串 "18"。',
          'int("18") 返回整数 18，age 才能参与加法。',
          'age + 1 得到整数 19。',
          'f-string 把 19 放进文字，第二个 f-string 把 38.97 固定显示为两位小数。',
        ],
        '这和 JavaScript 的 Number(value) 及模板字符串 `${value}` 对应。Python 的转换更严格，例如 int("18px") 会直接失败，不会悄悄截取。',
        ['int("12.5") 会得到什么？', '会抛出 ValueError，因为 "12.5" 不是整数文本。需要先明确业务是否允许小数，再选择 Decimal 或 float。'],
      ),
    ],
    [
      ['把 input 结果直接与整数相加', 'input 返回 str，str 和 int 没有定义加法。', '先使用 int(raw) 转换，并考虑捕获 ValueError。'],
      ['使用 if value == None', '虽然部分情况下能工作，但 None 的标准身份判断是 is None。', '写成 if value is None:。'],
      ['调用 name.strip() 后仍使用 name', '字符串不可变，strip 返回的新值没有被保存。', '写成 name = name.strip() 或 cleaned_name = name.strip()。'],
      ['用 float 反复计算金额', '二进制浮点可能产生 0.1 + 0.2 这类精度结果。', '基础业务用整数分；需要十进制规则时学习 Decimal。'],
    ],
    [
      ['观察类型', '创建姓名、年龄、身高、是否学生和昵称缺失五个变量，用 type() 打印类型。', '分别考虑 str、int、float、bool、None。', '你能解释每个变量为什么选择该类型。'],
      ['字符串实验', '对字符串 "  Python Beginner  " 完成清理、转小写、取得 Python 和取得最后一个字符。', '依次使用 strip、lower、切片和负索引。', '输出 python beginner、Python 和 r。'],
      ['安全年龄输入', '读取年龄，转换为 int，输出十年后的年龄；输入无法转换时打印友好提示。', '先学习最小 try/except ValueError，用它包围 int 转换。', '输入 20 得到 30，输入 abc 不显示长 traceback。'],
    ],
    [['int', '整数类型，没有小数部分。'], ['float', '浮点数类型，适合近似小数。'], ['bool', '只有 True 与 False 的逻辑类型。'], ['None', '表示没有值的特殊对象。'], ['不可变', '对象创建后内容不能原地改变。'], ['切片', '按起止位置取得序列的一部分。']],
  ),
  3: lessonDetail(
    ['程序不只是从上到下执行固定语句，它还需要根据数据选择不同路径，并重复处理多条数据。分支和循环就是控制程序执行路线的工具。', '今天先用流程图思维理解条件，再学习如何避免层层嵌套。每个条件都应该对应一句清晰的业务判断。'],
    [['条件', '最终计算为 True 或 False 的表达式。'], ['代码块', '由相同缩进组成、共同属于 if 或循环的一组语句。'], ['迭代', '依次处理集合中每个元素的过程。']],
    [
      chapter('if 分支如何选择路径', '多个条件按什么顺序判断？', ['if 从第一个条件开始判断；成立就执行对应代码块，并跳过同一组后续 elif 和 else。条件顺序因此代表业务优先级。', '比较运算符 ==、!=、>、>=、<、<= 产生布尔值。and 要求两边都成立，or 只需一边成立，not 取反。复杂条件先命名再组合更容易读。'], 'age = 20\nhas_ticket = True\n\nif age < 18:\n    print("未成年")\nelif has_ticket:\n    print("允许入场")\nelse:\n    print("请先购票")', ['age < 18 为 False，跳过第一个块。', 'has_ticket 为 True，执行第二个块。', '同组分支已经命中，else 不再执行。'], '对应 JavaScript 的 if / else if / else；Python 用 elif，并用缩进代替花括号。', ['如果两个 elif 条件都为 True，会执行几个？', '只执行最先命中的一个，后续分支会被跳过。']),
      chapter('真值与短路求值', '为什么空字符串可以直接放进 if？', ['Python 会把空字符串、0、None 和空容器视为假值，其余大多数对象视为真值。这样可简化“是否为空”的判断，但当 0 和 None 含义不同，就要显式比较。', 'and 和 or 会从左到右计算并短路。or 常用于提供默认值，但它会把所有假值都当成需要替换，因此不能用于 0 是合法值的场景。'], 'nickname = input("昵称：").strip()\ndisplay_name = nickname or "匿名用户"\nprint(display_name)\n\nquantity = 0\nif quantity == 0:\n    print("数量明确为零")', ['nickname 非空时，or 直接返回 nickname。', 'nickname 为空时，or 继续返回“匿名用户”。', 'quantity 使用显式 == 0，因为这里要表达准确业务含义。'], 'JavaScript 也有 truthy 和 falsy，但具体集合不完全相同。迁移时不要凭记忆，遇到边界要显式判断。', ['value 为 0 时，value or 10 得到什么？', '得到 10，因为 0 是假值。这也是不能随意用 or 提供数值默认值的原因。']),
      chapter('for、while 与遍历工具', '重复执行应该选择哪种循环？', ['for 适合依次处理一个可迭代对象，while 适合“不知道次数，只知道继续条件”的过程。能用 for 清楚表达时，不要手动维护索引。', 'enumerate 同时给出序号和值；zip 把多个序列按位置配对；range 生成整数序列。循环中的 break 立即结束，continue 跳过当前轮。'], 'names = ["小林", "小周", "小陈"]\nfor index, name in enumerate(names, start=1):\n    print(f"{index}. {name}")\n\nattempts = 0\nwhile attempts < 3:\n    attempts += 1', ['enumerate 为第一项生成序号 1。', 'for 每次把一组序号和姓名解包给两个变量。', 'while 每轮先检查 attempts < 3，必须保证循环内状态最终改变。'], 'JavaScript 的 for...of 对应 Python for。Python enumerate 替代常见的手动 index 变量。', ['while 循环最常见的风险是什么？', '条件永远不变导致无限循环，因此必须确认循环体会推进终止条件。']),
    ],
    [['条件写成 =', '= 是赋值，不是比较。', '相等比较使用 ==。'], ['用 if not value 判断所有缺失', '0、空字符串与 None 都会命中，含义可能混淆。', '根据业务分别使用 is None、== 0 或 == ""。'], ['while 永不结束', '循环体没有改变条件依赖的变量。', '先写出每轮如何接近结束。']],
    [['分支跟写', '实现成绩 A/B/C/D 分级，明确 90、80、70、60 的边界。', '条件从高分到低分排列。', '90 得 A，89 得 B，59 得 D。'], ['遍历改写', '用 enumerate 输出购物清单序号和名称。', 'start=1 可让序号从 1 开始。', '每行形如 1. 牛奶。'], ['规则挑战', '实现会员等级和优惠券的组合折扣。', '先写非法情况，再写特殊情况，最后写默认情况。', '每个输入只命中一条清晰路径。']],
    [['分支', '根据条件选择执行路径。'], ['真值', '对象在布尔上下文中被视为真或假的规则。'], ['短路', '结果已经确定后停止计算剩余表达式。'], ['迭代', '逐个取得并处理元素。']],
  ),
  4: lessonDetail(
    ['随着程序变长，连续写在 main 中会难以理解、复用和测试。函数通过名称把一段逻辑变成可调用单元，是业务代码最重要的组织方式。', '今天不追求把代码拆得越碎越好，而是学习如何让函数有明确输入、单一职责和可预测输出。'],
    [['参数', '函数定义中接收输入的名称。'], ['实参', '调用函数时实际传入的值。'], ['返回值', '函数完成后交还给调用方的结果。'], ['作用域', '一个名称可以被访问的代码范围。']],
    [
      chapter('定义、调用与返回值', '函数如何接收数据并交还结果？', ['def 创建函数，括号中声明参数，return 立即结束函数并把值交回调用处。没有显式 return 的函数会返回 None。', 'print 只是显示信息，return 才能让结果继续参与其他计算。业务计算函数优先 return，最外层界面再负责 print。'], 'def subtotal(price_cents: int, quantity: int) -> int:\n    return price_cents * quantity\n\ntotal = subtotal(1299, 3)\nprint(total)', ['定义 subtotal 时函数体不执行。', '调用时 1299 绑定到 price_cents，3 绑定到 quantity。', '乘法得到 3897，return 交回调用处。', 'total 接住结果，再由 print 输出。'], '对应 JavaScript function 的参数和 return。Python 类型标注帮助说明预期，但默认不会自动阻止错误类型。', ['print(total) 与 return total 有什么不同？', 'print 只向终端显示；return 把值交给调用方继续使用。']),
      chapter('参数种类与清晰调用', '什么时候应该写出参数名？', ['位置参数按顺序匹配，关键字参数按名称匹配。多个相同类型或布尔参数很容易放反，使用仅关键字参数可以强制调用方写名称。', '默认参数适合真正稳定的默认行为。默认值在函数定义时创建一次，因此列表和字典等可变对象不能直接作为默认值。'], 'def create_order(user_id: int, *, express: bool = False) -> dict:\n    return {"user_id": user_id, "express": express}\n\norder = create_order(42, express=True)', ['user_id 是普通位置参数。', '星号后的 express 只能用关键字传入。', '未传 express 时使用 False。', '调用处 express=True 一眼能看出布尔值含义。'], 'TypeScript 常依赖对象参数解决布尔参数含义，Python 的关键字参数可直接实现相同可读性。', ['为什么 def add(items=[]): 有风险？', '默认列表只创建一次，多次调用会共享同一个列表。应使用 None 后在函数体创建。']),
      chapter('作用域、副作用与纯函数', '函数内部变量为什么不会自动出现在外部？', ['Python 按局部、外层函数、全局和内置范围查找名称，简称 LEGB。函数内部赋值默认创建局部名称，不会修改外部同名变量。', '只根据参数计算并返回结果的纯函数最容易测试。读取 input、修改全局列表、写文件等属于副作用，应尽量留在清晰边界。'], 'tax_rate = 0.06\n\ndef add_tax(amount: int) -> int:\n    tax = round(amount * tax_rate)\n    return amount + tax\n\nresult = add_tax(1000)', ['amount 和 tax 是局部名称。', 'tax_rate 在局部没有定义，Python 向全局查找。', '函数返回新金额，没有修改传入整数。'], '前端的纯 reducer 和 selector 与这里的纯函数思路一致：相同输入得到相同输出，测试不需要浏览器或网络。', ['计算折扣的函数应该直接 input() 吗？', '不应该。让函数接收参数，入口层负责 input，这样规则可自动测试。']),
    ],
    [['函数只有 print 没有 return', '调用方得到 None，无法继续组合计算。', '计算函数 return，展示层 print。'], ['可变默认参数', '默认对象会跨调用共享。', '默认写 None，在函数内创建新列表。'], ['函数修改全局变量', '结果依赖隐藏状态，测试和复用困难。', '把需要的数据作为参数传入并返回新结果。']],
    [['跟写', '写 rectangle_area(width, height) 并返回面积。', '不要在函数内 input。', '调用 rectangle_area(3, 4) 得到 12。'], ['重构', '把个人名片的文本构造拆成 build_card 函数。', 'input 留在 main，build_card 只接收参数和返回字符串。', '无需终端输入也能直接测试 build_card。'], ['边界挑战', '实现 checkout，校验价格和数量，再计算小计。', '校验失败 raise ValueError。', '正常返回整数分，非法数量明确失败。']],
    [['参数', '定义函数时声明的输入名称。'], ['实参', '调用时传入的具体值。'], ['return', '结束函数并交回结果。'], ['副作用', '函数对外部世界或共享状态造成的变化。'], ['LEGB', 'Python 名称查找的四层顺序。']],
  ),
  5: lessonDetail(
    ['真实业务总是在处理一组数据：商品列表、用户信息、权限集合和配置映射。Python 提供多种容器，每种容器表达不同约束。', '今天的重点不是背所有方法，而是先问三个问题：是否需要顺序、是否允许重复、通过位置还是键查找。'],
    [['容器', '能够保存多个值的对象。'], ['可变', '对象创建后内容可以原地改变。'], ['哈希', '用于快速定位且在对象生命周期内稳定的值。'], ['解包', '把一个结构中的多个元素分别赋给多个名称。']],
    [
      chapter('list 与 tuple', '有顺序的数据为什么有两种容器？', ['list 有顺序、允许重复并且可修改，适合购物车行、搜索结果等动态集合。tuple 也有顺序，但不可变，适合坐标或固定结构。', '索引和切片规则与字符串一致。append 增加单个元素，extend 加入一组元素，pop 删除并返回元素。'], 'items = ["苹果", "牛奶"]\nitems.append("面包")\nfirst = items[0]\nlast_two = items[-2:]\n\npoint = (120, 80)\nx, y = point', ['列表创建后可 append。', '索引 0 取得第一项。', '[-2:] 取得最后两项。', 'tuple 通过解包分别得到 x 和 y。'], '对应 JavaScript Array，但 Python tuple 提供明确的不可变固定序列。', ['append([1, 2]) 与 extend([1, 2]) 有何不同？', 'append 把整个列表作为一个元素加入，extend 把其中元素逐个加入。']),
      chapter('dict 用键表达含义', '什么时候应该通过名称而不是位置取值？', ['dict 保存键值映射。键必须可哈希且唯一，值可以是任意对象。现代 Python 字典保持插入顺序，但它的核心价值仍是通过键快速查找。', '直接 data[key] 在键不存在时抛 KeyError；data.get(key) 在不存在时默认返回 None。是否允许缺失决定选择哪种方式。'], 'user = {"id": 42, "name": "小林", "active": True}\nprint(user["name"])\nrole = user.get("role", "member")\n\nfor key, value in user.items():\n    print(key, value)', ['字典字面量用冒号关联键和值。', '方括号按键取得姓名。', 'get 为缺失 role 提供默认值。', 'items 同时遍历键和值。'], '对应 JavaScript 普通对象和 Map 的一部分用途；Python 字典键不局限于字符串。', ['什么时候不应使用 get？', '字段必须存在时，直接索引能尽早暴露缺失；get 可能把数据错误隐藏成 None。']),
      chapter('set、可变性与复制', '去重为什么不总能直接使用集合？', ['set 只保存唯一、可哈希元素，适合成员判断、交集和差集。集合不表达业务展示顺序，去重后需要稳定顺序时可用字典键或显式循环。', '赋值不会复制容器。浅拷贝只复制最外层，嵌套对象仍共享；深拷贝成本更高，通常更好的做法是明确构造需要的新数据。'], 'permissions = {"read", "write"}\nrequired = {"read", "delete"}\nmissing = required - permissions\n\noriginal = [[1], [2]]\nshallow = original.copy()\nshallow[0].append(9)\nprint(original)', ['集合差集得到缺失的 delete。', 'copy 创建新的外层列表。', '内部的 [1] 仍是同一对象。', '通过 shallow 修改内部列表时 original 也能看到 9。'], 'JavaScript 的 Set 类似 Python set；对象与数组的浅拷贝问题也与展开语法相似。', ['为什么 list 不能作为 dict 的键？', 'list 可变，哈希值无法在作为键期间保持稳定。']),
    ],
    [['用 set 去重后直接展示', '集合遍历顺序不表达首次出现顺序。', '需要稳定顺序时使用 dict.fromkeys 或显式记录。'], ['写 b = a 以为完成复制', '两个名称仍指向同一容器。', '根据嵌套结构选择 copy 或重新构造。'], ['对可能缺失的键直接索引', '会抛 KeyError。', '明确缺失是否正常，再选择 get 或异常处理。']],
    [['容器选择', '分别为坐标、购物清单、用户详情和权限集合选择容器并说明理由。', '考虑顺序、修改、唯一和查找方式。', 'tuple、list、dict、set 各有一个合理场景。'], ['订单合并', '把相同 SKU 的数量累加到字典。', '用 totals.get(sku, 0) 读取当前数量。', '每个 SKU 只保留一条总数量。'], ['复制实验', '创建嵌套列表，比较赋值、浅拷贝和 deepcopy。', '打印外层和内层 id。', '你能解释哪些层共享。']],
    [['list', '有序、可变、允许重复的序列。'], ['tuple', '有序、不可变的序列。'], ['dict', '按键保存和查找值的映射。'], ['set', '保存唯一可哈希元素的集合。'], ['浅拷贝', '只复制最外层容器。']],
  ),
  6: lessonDetail(
    ['当数据量从三条变成三百万条时，一次性创建完整列表会浪费内存。Python 的迭代协议让代码可以逐个取得数据，生成器则让你自己定义这种按需生产过程。', '今天先理解 for 循环背后的机制，再学习何时使用推导式、何时保持普通循环。'],
    [['可迭代对象', '能够依次产生元素的对象，如列表、字符串和文件。'], ['迭代器', '保存当前读取位置、通过 next 取得下一个值的对象。'], ['惰性', '需要某个值时才计算，而不是提前计算全部结果。']],
    [
      chapter('for 循环背后的迭代协议', 'for 为什么能遍历不同类型？', ['for 会先调用 iter(obj) 取得迭代器，再不断调用 next(iterator)。当迭代器抛出 StopIteration，循环正常结束。', '列表是可迭代对象，每次 iter(list) 可创建新的迭代器；迭代器本身保存位置，被耗尽后不会自动回到开头。'], 'values = [10, 20]\nit = iter(values)\nprint(next(it))\nprint(next(it))\n\nfor value in values:\n    print(value)', ['iter 创建列表迭代器。', '第一次 next 得到 10，第二次得到 20。', '再次 next 会触发 StopIteration。', 'for 自动处理这个结束信号。'], 'JavaScript iterator protocol 也使用 next，但返回 {value, done}；Python 用 StopIteration 表示结束。', ['列表和列表迭代器哪个可以自然重复遍历？', '列表可以每次创建新迭代器；同一个已耗尽迭代器不能自动重来。']),
      chapter('推导式是简洁的数据转换', '什么时候一行推导式比循环更清楚？', ['列表推导式把“遍历、可选筛选、转换”放在一个表达式中。适合简单单层逻辑，复杂分支或副作用应回到普通循环。', '字典和集合也有推导式。推导式会立即构造完整结果；只需要逐个消费时使用生成器表达式。'], 'numbers = [1, 2, 3, 4, 5]\nsquares_of_even = [n * n for n in numbers if n % 2 == 0]\nname_lengths = {name: len(name) for name in ["Ada", "Lin"]}', ['for n in numbers 依次取得数字。', 'if 只保留偶数。', 'n * n 转换保留的数字。', '最终列表是 [4, 16]。'], '对应 JavaScript 的 filter(...).map(...)，Python 常用一条清晰推导式表达相同数据流。', ['包含三层条件和函数调用的推导式应该怎么办？', '改写成普通循环并给中间判断命名，优先可读性。']),
      chapter('yield 与流式处理', '生成器函数为什么能暂停和继续？', ['函数体含 yield 时，调用函数不会立刻执行，而是返回生成器。每次 next 或 for 请求值时，函数运行到 yield 暂停，并保存局部状态；下一次从暂停处继续。', '文件对象天然按行迭代，与生成器组合可以保持固定内存。需要统计时可逐条更新字典，不必先创建所有过滤结果。'], 'def even_numbers(limit: int):\n    number = 0\n    while number < limit:\n        if number % 2 == 0:\n            yield number\n        number += 1\n\nfor value in even_numbers(6):\n    print(value)', ['调用 even_numbers(6) 只创建生成器。', 'for 请求第一个值，函数运行到 yield 0 并暂停。', '下一轮从 yield 后继续，最终产生 2 和 4。', 'number 达到 6 后函数结束，循环结束。'], 'JavaScript generator 使用 function* 与 yield，概念几乎一致；Python 生成器更常用于文件和数据流水线。', ['生成器适合随机读取第 1000 个元素吗？', '不适合。它面向顺序消费，通常必须先经过前 999 个值。']),
    ],
    [['把生成器转成 list 再处理', '失去了惰性和节省内存的价值。', '能顺序消费就直接 for。'], ['推导式中执行 print 或写文件', '副作用隐藏在表达式中，可读性差。', '使用普通循环明确执行。'], ['同一生成器遍历两次', '第一次已经耗尽。', '重新调用生成器函数创建新实例。']],
    [['协议实验', '手动用 iter 和 next 消费三元素列表，并捕获 StopIteration。', '使用 try/except StopIteration。', '能解释 for 自动替你做了什么。'], ['推导改写', '把筛选活跃用户名的普通循环改成推导式。', '只保留一层筛选和一层转换。', '结果与原循环一致且更短。'], ['流式日志', '实现只 yield 状态码 500 以上行的生成器。', '逐行 split，损坏行可以跳过或记录。', '不读取完整文件到列表。']],
    [['iterable', '可以产生迭代器的对象。'], ['iterator', '保存消费状态并产生下一个值的对象。'], ['generator', '由 yield 驱动的特殊迭代器。'], ['惰性求值', '值被请求时才计算。'], ['推导式', '从可迭代数据构造新容器的紧凑语法。']],
  ),
  7: lessonDetail(
    ['单文件练习可以快速开始，但功能增多后需要分文件。模块和包让代码可以复用，并规定哪些部分是入口、业务逻辑和公共接口。', '今天会把前六天代码整理成小包，并理解“运行文件”和“导入模块”为什么行为不同。'],
    [['模块', '一个可被导入的 .py 文件。'], ['包', '组织相关模块的目录，通常包含 __init__.py。'], ['导入', '让当前模块可以访问另一模块定义的名称。'], ['公共 API', '其他模块被鼓励依赖的稳定名称集合。']],
    [
      chapter('一个 .py 文件就是模块', 'import 时 Python 做了什么？', ['import 会查找模块，执行模块顶层代码一次，并把创建的名称放入模块对象。顶层 input、数据库写入等副作用会在导入时发生，因此应放进入口函数。', '使用 from module import name 取得具体名称，使用 import module 保留来源前缀。公共业务代码应避免 import *。'], 'import math\nprint(math.ceil(3.2))\n\nfrom pathlib import Path\nprint(Path.cwd())', ['import math 加载标准库模块。', 'math.ceil 明确表明函数来源。', 'from 导入 Path 后可直接使用名称。'], '对应 ES Module 的 import，但 Python 模块首次导入时会执行顶层代码。', ['为什么模块顶层不应直接调用 input？', '任何导入它的代码都会被迫等待输入，产生意外副作用。']),
      chapter('包与导入路径', '多级目录如何成为可导入包？', ['包目录使用 __init__.py 明确边界。绝对导入从顶层包名开始，阅读时最清楚。python -m package.module 从包上下文执行入口。', '模块搜索依赖当前执行方式和 sys.path。不要通过到处修改 sys.path 解决导入问题，应从项目根目录按模块运行。'], 'order_tools/\n    __init__.py\n    pricing.py\n    cli.py\n\n# cli.py\nfrom order_tools.pricing import subtotal', ['order_tools 是顶层包。', 'pricing 和 cli 是包内模块。', '从项目根目录执行 python -m order_tools.cli。'], '类似前端包和相对导入，但 Python 的执行目录会影响顶层包能否被找到。', ['直接 python order_tools/cli.py 导入失败时，优先尝试什么？', '回到项目根目录，使用 python -m order_tools.cli。']),
      chapter('依赖与第一周整理', '项目用到的第三方库放在哪里？', ['标准库随 Python 提供，第三方库通过 pip 安装到当前虚拟环境。requirements.txt 记录项目直接依赖，确保其他机器能安装。', '第一周项目只需要标准库。不要为了结构好看引入框架。先把计算函数、CLI 入口和包导出整理清楚。'], 'source .venv/bin/activate\npython -m pip list\npython -m order_tools.cli', ['激活虚拟环境后 python 和 pip 指向项目环境。', 'pip list 查看当前安装包。', '按模块运行 CLI，业务模块可以被测试直接导入。'], '对应 node_modules 与 package.json，但 Python 虚拟环境同时隔离解释器入口和包目录。', ['标准库模块需要写进 requirements.txt 吗？', '不需要。只记录项目直接使用的第三方依赖。']),
    ],
    [['在包内直接运行子文件', '顶层包上下文可能缺失，绝对导入失败。', '从项目根目录使用 python -m。'], ['把所有名称塞进 __init__.py', '公共边界膨胀并容易产生循环导入。', '只导出稳定且常用的公共名称。'], ['用全局 pip 安装项目依赖', '不同项目版本互相影响。', '先激活项目 .venv。']],
    [['拆模块', '把金额计算放 pricing.py，把 input 和 print 放 cli.py。', '业务模块不能在导入时询问输入。', 'pricing 可被单独 import。'], ['按模块运行', '从项目根目录执行 python -m order_tools.cli。', '确认目录含 __init__.py。', '入口正常运行且无导入错误。'], ['阶段复盘', '不看示例，从空目录搭建两模块小程序。', '先画出模块依赖箭头。', '运行、导入和语法检查都通过。']],
    [['module', '可独立导入的 Python 文件。'], ['package', '组织模块的目录边界。'], ['标准库', '随 Python 一起提供的模块集合。'], ['第三方依赖', '需要额外安装的外部包。'], ['入口模块', '负责启动程序和连接外部输入输出的模块。']],
  ),
  8: lessonDetail(
    ['函数适合表达一次计算，但有些业务对象需要在多次操作之间持续保存状态，例如购物车中的商品。类把数据和维护这些数据的行为放到同一边界。', '今天先理解实例是什么，再学习类真正解决的问题。类不是“更高级的函数集合”，没有持续状态时普通函数通常更简单。'],
    [['类', '创建同类对象的定义和规则。'], ['实例', '根据类创建出来的具体对象。'], ['属性', '保存在对象上的数据。'], ['方法', '定义在类中、通常操作实例状态的函数。']],
    [
      chapter('从类创建实例', '类、实例和 self 分别是什么？', ['class 语句定义类。调用 Cart() 会创建一个 Cart 实例，然后调用 __init__ 初始化它。self 是当前正在操作的具体实例，由 Python 在方法调用时自动传入。', '不同实例拥有各自实例属性。类属性则放在类上并被实例共同查找，只适合真正共享的数据。'], 'class Cart:\n    currency = "CNY"\n\n    def __init__(self) -> None:\n        self.items = []\n\ncart_a = Cart()\ncart_b = Cart()\ncart_a.items.append("A-01")\nprint(cart_b.items)', ['定义类时创建 currency 和两个方法。', 'Cart() 两次创建两个不同实例。', '每次 __init__ 都创建新的 items 列表。', '修改 cart_a 不影响 cart_b。'], '对应 JavaScript class 与 this。Python 明确把 self 写在方法参数中，调用时不需要手动传。', ['为什么 items 应在 __init__ 中创建，而不是写成类属性 []？', '可变类属性会被所有实例共享，导致不同购物车互相看到商品。']),
      chapter('方法保护对象不变量', '为什么不让调用方直接修改 items？', ['不变量是对象在任何时刻都必须满足的规则，例如商品数量必须为正。若外部直接修改内部字典，就能绕过规则。方法提供受控入口，先校验再改变状态。', 'Python 的下划线前缀表示内部属性约定，并非绝对私有。真正保护来自清晰 API 和所有调用路径都经过同一方法。'], 'class Cart:\n    def __init__(self) -> None:\n        self._items: dict[str, int] = {}\n\n    def add(self, sku: str, quantity: int = 1) -> None:\n        if quantity <= 0:\n            raise ValueError("quantity must be positive")\n        self._items[sku] = self._items.get(sku, 0) + quantity', ['_items 在实例创建时初始化。', 'add 是修改状态的唯一入口。', '非法数量在状态改变前被拒绝。', '合法数量累加到对应 SKU。'], '类似前端状态管理中的 reducer：状态变化集中在有限入口，而不是让任意组件随意改内部结构。', ['属性前面的单下划线表示什么？', '表示“内部使用”的约定，调用者应通过公共方法操作；它不是安全隔离机制。']),
      chapter('组合优于不必要的继承', '订单与订单项应该是什么关系？', ['继承表达稳定的“是一种”关系，组合表达“拥有”。订单拥有订单项，购物车拥有价格策略，这些通常是组合。', '深继承让行为分散在多个父类，调用流难追踪。先用普通属性组合对象，只有多个子类确实共享稳定语义时再考虑继承。'], 'class Order:\n    def __init__(self, items: list[OrderItem]) -> None:\n        self.items = list(items)\n\n    def total_cents(self) -> int:\n        return sum(item.subtotal_cents() for item in self.items)', ['Order 接收订单项列表。', 'list(items) 创建自己的外层列表，减少外部直接修改影响。', '总额委托每个 OrderItem 计算小计。'], 'React 组件同样通常推荐组合而非继承。Python 业务模型也遵循这个原则。', ['没有实例状态的工具类应该怎么处理？', '优先改为模块中的普通函数，不需要 class 和 staticmethod 外壳。']),
    ],
    [['把可变列表写成类属性', '所有实例共享同一列表。', '在 __init__ 中创建实例属性。'], ['对象允许负数量', '状态入口没有维护不变量。', '所有修改方法在写入前校验。'], ['为了复用两行代码建立继承层级', '增加理解和变更成本。', '优先普通函数或组合。']],
    [['实例实验', '创建两个购物车，只向第一个添加商品，验证第二个为空。', '打印 id(cart_a.items) 和 id(cart_b.items)。', '两个列表身份不同。'], ['封装', '增加 remove 方法，数量归零时删除 SKU。', '先定义不存在 SKU 的行为。', '内部字典始终没有零或负数量。'], ['模型挑战', '实现 Order 与 OrderItem 组合并计算总额。', 'OrderItem 负责自己的小计。', 'Order 不需要知道小计公式细节。']],
    [['class', '描述对象数据与行为的定义。'], ['instance', '由类创建的具体对象。'], ['self', '方法正在操作的当前实例。'], ['invariant', '对象始终必须满足的规则。'], ['composition', '一个对象通过属性拥有并使用另一个对象。']],
  ),
  9: lessonDetail(
    ['许多类只是保存字段，手写 __init__、__repr__ 和 __eq__ 会产生大量重复。数据类让这种意图更直接，枚举则为有限状态提供统一名称。', '今天还会接触 Python 数据模型：对象通过特定双下划线方法接入 len、比较和迭代等语言能力。'],
    [['数据类', '通过装饰器自动生成常用数据方法的类。'], ['枚举', '一组有限、命名明确的常量成员。'], ['装饰器', '在定义后包装或配置函数、类的语法。'], ['协议', '对象为了参与某种操作需要提供的方法约定。']],
    [
      chapter('dataclass 消除数据样板', '什么样的类适合数据类？', ['@dataclass 读取类型标注字段，自动生成 __init__、__repr__ 和 __eq__。它适合订单项、地址等字段清晰的值对象。', '可变默认值仍要使用 field(default_factory=list)。frozen=True 阻止普通字段重新赋值，适合创建后不应改变的值对象。'], 'from dataclasses import dataclass, field\n\n@dataclass(frozen=True)\nclass OrderItem:\n    sku: str\n    unit_cents: int\n    quantity: int\n\n@dataclass\nclass Order:\n    id: int\n    items: list[OrderItem] = field(default_factory=list)', ['装饰器处理类定义。', 'OrderItem 自动获得三个参数的构造函数。', 'frozen 阻止创建后改写字段。', 'default_factory 为每个 Order 创建新列表。'], 'TypeScript interface 只描述类型；dataclass 还会在运行时创建真正对象和常用方法。', ['frozen 数据类中的 list 字段是否完全不可变？', '不是。frozen 阻止重新绑定字段，但列表对象自身仍可修改，应选 tuple 或其他不可变结构。']),
      chapter('Enum 约束离散状态', '为什么不用散落的 "open" 字符串？', ['状态字符串散落在代码中容易拼错，重命名也难以查全。Enum 把允许值集中在一个定义，方法签名可直接声明 Status。', '继承 str 的枚举成员更容易与 JSON 框架协作。数据库通常保存 member.value，业务判断比较枚举成员。'], 'from enum import Enum\n\nclass Status(str, Enum):\n    OPEN = "open"\n    PAID = "paid"\n    CANCELLED = "cancelled"\n\nstatus = Status.OPEN\nprint(status.value)', ['类中每一行定义一个成员。', 'Status.OPEN 是枚举成员，不是普通散落字符串。', '.value 得到需要序列化的 "open"。'], '对应 TypeScript string enum 或 union，但 Python Enum 在运行时也是对象。', ['从外部收到 "paid" 如何变成枚举？', '调用 Status("paid")，非法值会抛出 ValueError。']),
      chapter('Python 数据模型', 'len(order) 为什么能调用你写的方法？', ['Python 不要求调用 order.get_length()。当内置 len(order) 运行时，它查找 order.__len__()。这种由特殊方法连接的机制就是数据模型。', '只实现具有自然语义的方法。订单包含几项可以用 __len__，但不要为了展示技巧随意覆盖加法、比较等操作。'], 'class Order:\n    def __init__(self, items):\n        self.items = list(items)\n\n    def __len__(self) -> int:\n        return len(self.items)\n\norder = Order(["A", "B"])\nprint(len(order))', ['len(order) 触发 __len__。', '__len__ 返回内部列表长度 2。', '调用方使用统一内置函数，无需知道内部结构。'], '类似 JavaScript 的 iterator 和 toString 协议，但 Python 的特殊方法覆盖更多语言运算。', ['应该直接调用 order.__len__() 吗？', '通常不应，使用 len(order) 让意图更清楚。']),
    ],
    [['数据类列表默认写 []', '实例共享或直接被 dataclass 拒绝。', '使用 field(default_factory=list)。'], ['比较 status == "open"', '绕过枚举类型并重新引入魔法字符串。', '比较 Status.OPEN。'], ['无意义魔法方法', '让普通操作产生意外语义。', '只实现领域中自然成立的协议。']],
    [['数据类改写', '把普通 OrderItem 类改成 frozen 数据类。', '保留业务校验，可在 __post_init__ 处理。', '初始化、显示和比较无需手写。'], ['枚举解析', '读取字符串并转换 Status，非法值给友好错误。', '捕获 ValueError。', '合法值成为枚举成员。'], ['协议实验', '为 Cart 实现 __len__ 和 __contains__。', '成员判断可用 sku in cart。', 'len 与 in 具有自然业务含义。']],
    [['dataclass', '自动生成常见数据方法的类装饰器。'], ['field', '配置数据类字段默认值和行为。'], ['Enum', '有限命名值集合。'], ['magic method', '连接 Python 语言协议的双下划线方法。']],
  ),
  10: lessonDetail(
    ['真实输入会损坏，文件会不存在，数据库会失败。可靠程序不是没有错误，而是能区分错误类型、保留原因、清理资源并给出可行动信息。', '异常用于中断无法继续的正常流程。日志用于留下事件证据。两者职责不同，不能用 print 代替系统日志，也不能捕获所有异常后假装成功。'],
    [['异常', '程序执行期间出现的异常情况对象。'], ['抛出', '使用 raise 主动报告无法继续的情况。'], ['捕获', '使用 except 处理明确知道如何应对的异常。'], ['上下文管理器', '保证资源进入与退出操作成对执行的协议。']],
    [
      chapter('异常如何传播', '错误没有在当前函数处理时会去哪里？', ['raise 创建或重新抛出异常，当前函数立即停止，Python 沿调用栈向上寻找匹配的 except。如果一直没有处理，最终显示 traceback 并结束程序。', '只捕获能恢复、转换或补充信息的异常。捕获 Exception 后 pass 会隐藏程序错误和数据问题。'], 'def parse_quantity(raw: str) -> int:\n    try:\n        value = int(raw)\n    except ValueError as error:\n        raise ValueError(f"数量不是整数：{raw!r}") from error\n    if value <= 0:\n        raise ValueError("数量必须大于零")\n    return value', ['int 可能抛 ValueError。', 'except 捕获明确转换错误。', 'raise 新错误增加业务上下文。', 'from error 保留原始异常链。'], '对应 JavaScript try/catch/throw；Python 鼓励捕获具体异常类型。', ['raise NewError from error 有什么价值？', '既提供新的业务说明，又保留原始技术原因供 traceback 调试。']),
      chapter('else、finally 与 with', '资源如何保证在失败时也被关闭？', ['try 的 else 只在没有异常时执行，finally 无论成功失败都执行。文件和数据库连接的关闭更适合 with，因为上下文管理器把获取和清理绑在一起。', 'with 块离开时会调用资源的退出逻辑，即使块内抛出异常也会执行。'], 'from pathlib import Path\n\npath = Path("orders.txt")\nwith path.open(encoding="utf-8") as file:\n    content = file.read()\n\nprint(len(content))', ['open 成功后进入 with。', 'file 只在块内使用。', '正常结束或异常离开都会关闭文件。', '关闭后仍可使用已经读出的 content。'], '类似前端中的 try/finally 清理订阅，但 Python 为文件、锁和连接提供统一 with 语法。', ['什么时候仍需要 finally？', '没有现成上下文管理器但某个清理动作无论如何必须执行时。']),
      chapter('日志不是随手 print', '业务系统应该记录哪些信息？', ['logging 支持级别、时间、模块和统一输出目标。DEBUG 用于细节，INFO 记录正常业务事件，WARNING 表示可恢复异常，ERROR 表示操作失败。', '日志应包含订单 ID、请求 ID 等诊断上下文，但不能包含密码、完整密钥或敏感正文。同一个异常通常只在最终处理边界记录一次。'], 'import logging\n\nlogger = logging.getLogger(__name__)\n\ndef create_order(order_id: int) -> None:\n    logger.info("order_created order_id=%s", order_id)', ['按模块名取得 logger。', '消息使用参数占位而非提前拼接。', 'INFO 表示一次正常业务事件。', '日志系统决定最终格式和输出位置。'], '对应前端日志平台，但服务端日志长期集中保存，敏感数据风险更高。', ['为什么不在每一层都 logger.exception？', '同一异常会被重复记录多次，制造噪音；通常在知道请求或任务最终失败的边界记录。']),
    ],
    [['except: pass', '隐藏所有失败。', '捕获具体异常并恢复、转换或重新抛出。'], ['返回 None 表示所有错误', '调用方无法区分未找到、输入坏或系统失败。', '正常缺失可返回 None，异常情况用明确异常。'], ['日志记录原始请求全部内容', '可能泄露密码和个人数据。', '只记录必要标识并脱敏。']],
    [['异常阅读', '分别触发 ValueError、KeyError 和 FileNotFoundError，记录 traceback 最后一行。', '一次只触发一种。', '能说出每种错误含义。'], ['自定义异常', '创建 InvalidTransition 并在非法状态迁移时抛出。', '继承 ValueError 或自定义业务基类。', '调用方可单独捕获。'], ['资源测试', '在 with 文件块中故意抛错，验证退出后文件已关闭。', '检查 file.closed。', '异常不影响资源清理。']],
    [['exception', '执行期间的错误对象。'], ['call stack', '函数之间的调用层级。'], ['exception chaining', '新异常保留原始异常原因。'], ['context manager', '管理资源进入和退出的协议。'], ['log level', '日志严重程度分类。']],
  ),
  11: lessonDetail(
    ['文件是程序与外部世界交换数据的常见边界。路径可能不存在，编码可能不同，内容可能损坏，写入可能中途失败。', '今天不仅学习打开文件，还要建立“外部数据不可信、重要写入不能半完成”的工程习惯。'],
    [['路径', '文件或目录在文件系统中的位置。'], ['编码', '字符与字节之间的转换规则。'], ['序列化', '把内存对象转换成可存储或传输格式。'], ['原子操作', '对外看起来要么完全发生，要么完全没有发生。']],
    [
      chapter('Path 代替字符串拼路径', '相对路径究竟相对哪里？', ['Path 对象提供 / 运算符拼接、exists 检查和读写方法。相对路径默认相对于程序当前工作目录，不一定是 .py 文件所在目录。', '需要固定资源时可从 Path(__file__).resolve().parent 出发；用户输入路径时还要限制允许访问范围。'], 'from pathlib import Path\n\nbase = Path(__file__).resolve().parent\ndata_path = base / "data" / "orders.json"\nprint(data_path.exists())', ['__file__ 是当前模块文件路径。', 'resolve 转为规范绝对路径。', 'parent 取得模块目录。', '/ 运算符安全拼接子路径。'], '类似前端构建工具中的 path.resolve，但 pathlib 使用对象方法而非字符串工具函数。', ['Path("data.json") 相对哪个目录？', '相对启动 Python 命令时的当前工作目录。']),
      chapter('文本、JSON 与 CSV', '序列化格式能保存哪些 Python 值？', ['文本读写显式指定 UTF-8，避免操作系统默认编码差异。JSON 支持对象、数组、字符串、数字、布尔和 null，对应 Python dict、list、str、数字、bool 和 None。', '日期、Decimal、枚举和数据类不能直接全部 JSON 化，需要先转换。CSV 是表格行格式，字段中逗号和换行应交给 csv 模块处理，不能手工 split。'], 'import json\nfrom pathlib import Path\n\npath = Path("order.json")\ndata = {"id": 42, "paid": False}\npath.write_text(json.dumps(data, ensure_ascii=False), encoding="utf-8")\nloaded = json.loads(path.read_text(encoding="utf-8"))', ['dict 先由 json.dumps 转成 JSON 文本。', 'ensure_ascii=False 保留可读中文。', 'write_text 用 UTF-8 写入。', 'read_text 后 json.loads 恢复基本 Python 容器。'], '对应 JSON.stringify 与 JSON.parse；Python 额外需要明确文件编码。', ['为什么不应用 eval 读取数据文件？', 'eval 会执行任意 Python 代码，外部内容可能造成严重安全问题。']),
      chapter('安全写入与错误边界', '程序中途退出时如何保护原文件？', ['直接覆盖正式文件时，磁盘写到一半失败可能留下损坏内容。更安全的方式是在同目录写临时文件，完整成功后用 replace 替换目标。', '读取时区分文件不存在、权限错误和内容格式错误。不要把所有错误都显示为“文件不存在”。'], 'from pathlib import Path\n\ntarget = Path("config.json")\ntemp = target.with_suffix(".tmp")\n\ntemp.write_text("{\"enabled\": true}", encoding="utf-8")\ntemp.replace(target)', ['临时路径与目标在同目录。', '先完整写入 temp。', '只有写入成功后才 replace。', '失败时旧 target 仍保持原内容。'], '类似前端保存本地缓存时先构造完整新状态再一次提交，避免对外暴露半更新。', ['为什么临时文件最好与目标同目录？', '同文件系统上的 replace 更可能提供原子语义，也继承相同目录权限环境。']),
    ],
    [['依赖默认编码', '不同系统读取中文可能失败。', '显式 encoding="utf-8"。'], ['手工 split CSV', '引号、逗号和换行字段会解析错误。', '使用 csv.reader 或 DictReader。'], ['直接覆盖重要文件', '失败时可能留下半文件。', '临时写入成功后 replace。']],
    [['路径实验', '打印 cwd、__file__ 和模块目录，解释差异。', '使用 Path.cwd()。', '能说明相对路径来源。'], ['JSON 往返', '保存一个含中文的订单字典，再读回并断言相等。', 'ensure_ascii=False。', '读回结构与原值相等。'], ['原子写入', '封装 save_json_atomic(path, data)。', '异常时清理临时文件。', '旧文件不会因失败损坏。']],
    [['Path', '面向对象的路径表示。'], ['UTF-8', '常用的 Unicode 文本编码。'], ['JSON', '跨语言结构化文本格式。'], ['serialization', '对象与存储格式之间的转换。'], ['atomic replace', '完整新文件一次替换旧文件。']],
  ),
  12: lessonDetail(
    ['Python 在运行时允许动态类型，但大型业务代码仍需要清楚表达函数期望什么、返回什么。类型标注让编辑器、检查工具和同事更早发现接口误用。', '类型提示不是运行时防火墙。外部请求仍需 Pydantic 等运行时校验，内部标注主要服务于静态理解。'],
    [['类型标注', '写在名称旁边、描述预期类型的元数据。'], ['静态检查', '不实际执行程序就分析可能的类型问题。'], ['结构化类型', '根据对象拥有的属性和方法判断兼容。'], ['泛型', '用类型参数描述适用于多种具体类型的结构。']],
    [
      chapter('函数与容器类型', '标注改变运行方式吗？', ['参数和返回标注不会默认强制检查。它们进入函数的 __annotations__，主要由编辑器和工具使用。', 'list[str] 表示字符串列表，dict[str, int] 表示字符串到整数的映射。联合类型 A | B 表示值可能属于两者之一。'], 'def find_name(user_id: int) -> str | None:\n    if user_id == 42:\n        return "小林"\n    return None\n\nnames: list[str] = ["小林", "小周"]', ['user_id 预期是 int。', '函数可能返回 str 或 None。', '调用方应处理 None 分支。', '运行时仍可能有人错误传入字符串。'], '对应 TypeScript 类型，但 Python 类型默认在运行时不擦除到完全不可见，也不自动阻止调用。', ['Optional[str] 与 str | None 是否等价？', '在这里等价，都表示字符串或 None，不表示参数可省略。']),
      chapter('TypedDict 与数据边界', '普通字典如何拥有字段提示？', ['TypedDict 描述特定键和对应类型，运行时值仍是普通 dict。适合尚未转为领域对象的 JSON 结构。', '当数据需要方法、不变量或运行时校验时，应使用数据类或 Pydantic 模型，不要让巨大嵌套 TypedDict 代替领域设计。'], 'from typing import TypedDict\n\nclass OrderPayload(TypedDict):\n    user_id: int\n    sku: str\n    quantity: int\n\ndef total(payload: OrderPayload) -> int:\n    return payload["quantity"]', ['定义只描述字典形状。', '实际 payload 仍使用方括号取键。', '编辑器可以提示缺失或错误字段。'], '对应 TypeScript interface 描述对象字面量，但 Python 运行时不会因为 TypedDict 自动验证输入。', ['外部 JSON 满足 TypedDict 标注就一定安全吗？', '不一定，标注不执行运行时校验，仍需在信任边界验证。']),
      chapter('Protocol 与最小行为接口', '不继承同一基类也能被视为同一类型吗？', ['Protocol 描述对象需要提供的方法和属性。具体类只要结构匹配即可，不必显式继承，这叫结构化子类型。', '只有存在多种实现、测试替身或清晰边界时才引入协议。一个实现配一个接口会增加无用跳转。'], 'from typing import Protocol\n\nclass OrderStore(Protocol):\n    def get(self, order_id: int) -> Order | None: ...\n\ndef load_order(store: OrderStore, order_id: int) -> Order:\n    order = store.get(order_id)\n    if order is None:\n        raise LookupError(order_id)\n    return order', ['协议只声明 get 行为。', '数据库仓储和内存假对象都可满足。', 'load_order 依赖能力而非具体类。'], '类似 TypeScript 结构化 interface。Python 类无需 implements 声明。', ['只有一个简单仓储实现时需要 Protocol 吗？', '通常不需要。等真正出现替换或测试边界再增加。']),
    ],
    [['把所有类型写成 Any', '检查工具失去价值，错误继续传播。', '从公共边界和核心模型补准确类型。'], ['把 | None 当作参数可省略', '可省略由默认值决定。', '需要可省略时同时写 = None。'], ['用标注代替输入校验', '运行时仍能收到错误值。', '信任边界进行真实校验。']],
    [['补标注', '为前一周订单函数补参数、返回和容器类型。', '先处理公共函数。', '编辑器能提示错误调用。'], ['None 分支', '实现 find_order，并让调用方完整处理不存在。', '使用显式 if order is None。', '不存在不会引发 AttributeError。'], ['最小协议', '写内存仓储和 SQLite 仓储共同满足 get。', '不要要求无关方法。', '同一业务函数可接受二者。']],
    [['annotation', '名称旁描述预期类型的元数据。'], ['union', '一个值可能属于多个类型之一。'], ['TypedDict', '描述固定键字典结构的类型工具。'], ['Protocol', '描述所需行为的结构化接口。'], ['Any', '关闭该值上的大部分静态类型检查。']],
  ),
  13: lessonDetail(
    ['测试让你在修改代码后快速确认旧承诺没有被破坏。它不是为了追求数字，而是把输入、行为和预期写成可重复执行的示例。', 'pytest 会发现以 test_ 开头的函数，执行它们并报告断言差异。今天从一个纯函数测试开始，再扩展到参数化和 fixture。'],
    [['测试用例', '一个明确场景的输入、操作和预期结果。'], ['断言', '验证实际结果必须满足某个条件。'], ['fixture', '为测试准备并清理依赖资源的机制。'], ['参数化', '用多组数据重复运行同一测试逻辑。']],
    [
      chapter('测试公共行为', '一个最小测试包含什么？', ['Arrange 准备输入，Act 执行被测行为，Assert 验证结果。简单测试不必写注释分区，但思路应清楚。', '测试名称描述场景和结果，不要叫 test_1。断言对外结果，不依赖私有方法调用次数。'], 'def subtotal(price: int, quantity: int) -> int:\n    return price * quantity\n\ndef test_subtotal_multiplies_price_and_quantity():\n    result = subtotal(1299, 3)\n    assert result == 3897', ['定义被测函数。', 'pytest 发现 test_ 开头的测试。', '调用函数取得实际值。', 'assert 不成立时测试失败并显示差异。'], '对应 Jest/Vitest 的 expect，但 pytest 直接使用 Python assert 并重写失败信息。', ['测试中 print 实际值算验证吗？', '不算。人工阅读不能自动判断通过，应该使用 assert。']),
      chapter('异常和参数化', '如何证明非法输入真的失败？', ['pytest.raises 验证代码抛出预期异常。可以进一步断言消息关键内容，确保失败原因正确。', 'parametrize 把边界表格转换为多次独立测试，适合折扣、分级和校验规则。'], 'import pytest\n\n@pytest.mark.parametrize(("quantity", "expected"), [(1, 100), (3, 300)])\ndef test_total_cases(quantity, expected):\n    assert subtotal(100, quantity) == expected\n\ndef test_rejects_zero_quantity():\n    with pytest.raises(ValueError):\n        validate_quantity(0)', ['装饰器定义两组参数。', '同一测试函数独立运行两次。', '异常测试只有抛出 ValueError 才通过。'], '类似 test.each，但 pytest 参数名直接对应测试函数参数。', ['只写 with pytest.raises(Exception) 有什么问题？', '范围太宽，错误类型不对也可能通过；应断言具体异常。']),
      chapter('fixture 与隔离', '多个测试如何共享准备步骤而不共享脏状态？', ['fixture 是由 pytest 按名称注入的准备函数。默认 function 作用域让每个测试获得新对象，减少相互影响。', '含 yield 的 fixture 可在测试后清理文件或连接。测试不能依赖执行顺序，每个测试都应独立建立所需状态。'], '@pytest.fixture\ndef cart():\n    return Cart()\n\ndef test_new_cart_is_empty(cart):\n    assert len(cart) == 0\n\ndef test_add_item(cart):\n    cart.add("A-01")\n    assert len(cart) == 1', ['pytest 看到 cart 参数并寻找同名 fixture。', '每个测试调用 fixture 得到新 Cart。', '第二个测试的修改不污染第一个。'], '对应 beforeEach，但 fixture 可以按参数组合，更适合局部声明依赖。', ['fixture 改成 session 作用域有什么风险？', '可变状态会在整个测试会话共享，导致顺序依赖和污染。']),
    ],
    [['只测成功路径', '边界和异常最容易出错却没有保护。', '至少加入最小值、最大值和非法输入。'], ['断言实现细节', '重构内部结构时无业务变化也失败。', '断言公共返回、状态或异常。'], ['测试共享数据库状态', '顺序变化导致不稳定。', '每个测试使用干净事务或临时数据库。']],
    [['首个测试', '为个人名片 build_card 写返回值测试。', '把 input 移出被测函数。', '无需人工输入即可运行。'], ['边界矩阵', '为数量校验写 -1、0、1 和最大值测试。', '参数化合法和非法情况。', '每个边界意图清楚。'], ['Fixture', '为 Cart 建 fixture 并写两个互不影响的测试。', '默认 function 作用域。', '任意顺序运行都通过。']],
    [['pytest', '常用 Python 测试框架。'], ['assertion', '验证实际结果符合预期。'], ['fixture', '测试依赖的准备与清理机制。'], ['parametrize', '让一段测试逻辑运行多组输入。'], ['isolation', '测试之间不共享会互相影响的状态。']],
  ),
  14: lessonDetail(
    ['程序退出后，内存中的订单会消失。数据库把数据持久保存，并提供查询、约束和事务。SQLite 是一个嵌入式关系数据库，无需启动独立服务器，适合学习和本地业务。', '今天先直接写 SQL，理解表、行、列和事务，再考虑更高层工具。掌握底层后，使用 ORM 时才知道它替你做了什么。'],
    [['数据库', '持久存储和查询结构化数据的系统。'], ['表', '由固定列定义、保存多行记录的结构。'], ['主键', '唯一标识每一行的列。'], ['事务', '把多步操作作为不可分割整体提交或回滚。']],
    [
      chapter('表结构与约束', '哪些业务规则应该交给数据库兜底？', ['CREATE TABLE 定义列名、类型和约束。PRIMARY KEY 唯一标识，NOT NULL 禁止缺失，UNIQUE 防重复，CHECK 约束范围，FOREIGN KEY 保证引用存在。', '应用校验提供友好错误，数据库约束是最终防线。两者不是重复浪费，而是处在不同信任边界。'], 'CREATE TABLE orders (\n    id INTEGER PRIMARY KEY,\n    order_no TEXT NOT NULL UNIQUE,\n    total_cents INTEGER NOT NULL CHECK(total_cents >= 0),\n    status TEXT NOT NULL\n);', ['每一列声明名称和存储类型。', 'id 作为主键自动唯一。', 'order_no 不可为空且不能重复。', 'total_cents 不能是负数。'], '类似前端 TypeScript 类型加服务端校验，但数据库约束即使绕过应用也会执行。', ['为什么状态业务规则不能全部只靠 CHECK？', '复杂状态迁移依赖旧值、角色和操作，通常由业务层处理；数据库保存基础合法范围。']),
      chapter('参数化查询与结果读取', '为什么不能用 f-string 拼 SQL？', ['字符串拼接会让用户值进入 SQL 结构，攻击者可改变查询含义。sqlite3 使用 ? 占位符，值作为第二个参数单独绑定。', 'execute 返回 cursor，可使用 fetchone、fetchall 或直接迭代。row_factory 可让结果按列名访问。'], 'import sqlite3\n\nconn = sqlite3.connect("orders.db")\nconn.row_factory = sqlite3.Row\nrow = conn.execute(\n    "SELECT * FROM orders WHERE id = ?",\n    (42,),\n).fetchone()\nif row is not None:\n    print(row["status"])', ['连接数据库文件。', 'SQL 结构固定，id 使用占位符。', '(42,) 是单元素 tuple，逗号不可省。', '无结果时 fetchone 返回 None。'], '和前端调用 API 不同，服务端 SQL 参数必须通过数据库驱动绑定。', ['表名能否也用 ? 占位符？', '通常不能。结构标识符需来自固定代码或严格白名单，参数绑定用于值。']),
      chapter('事务与回滚', '创建订单和订单项为什么必须一起成功？', ['一个业务动作跨多条 SQL 时，如果中途失败却保留前半部分，数据库进入不一致状态。事务的原子性保证全部提交或全部回滚。', 'sqlite3 连接可用 with 管理事务：正常离开提交，异常离开回滚。连接关闭仍要明确处理或使用 closing。'], 'import sqlite3\n\nwith sqlite3.connect("orders.db") as conn:\n    cursor = conn.execute(\n        "INSERT INTO orders(order_no, total_cents, status) VALUES (?, ?, ?)",\n        ("O-1001", 3897, "open"),\n    )\n    order_id = cursor.lastrowid\n    conn.execute(\n        "INSERT INTO order_items(order_id, sku, quantity) VALUES (?, ?, ?)",\n        (order_id, "A-01", 3),\n    )', ['进入 with 后开始一组数据库操作。', '先插入订单并取得新主键。', '订单项引用该主键。', '第二条失败会让 with 回滚第一条。'], '类似前端状态的原子提交，但数据库事务还能应对进程故障和并发写入。', ['每插入一行就 commit 有什么问题？', '业务动作可能只提交一半，而且频繁提交降低性能。']),
    ],
    [['f-string 拼 SQL', '输入可改变 SQL 结构。', '使用 ? 和参数 tuple。'], ['忘记启用外键', 'SQLite 连接默认可能不强制外键。', '每个连接执行 PRAGMA foreign_keys = ON。'], ['每行单独提交', '失败时留下半完成数据。', '以业务动作划分事务。']],
    [['建表', '创建 users 和 orders，orders.user_id 外键引用用户。', '为订单号加 UNIQUE。', '无用户的订单被拒绝。'], ['CRUD', '实现新增和按 ID 查询，查询不存在返回 None。', '所有值使用参数。', '引号输入也不会改变 SQL。'], ['回滚演练', '第二条订单项故意写非法数量，验证订单没有保存。', '把两条 INSERT 放在同一 with。', '查询订单数量仍为零。']],
    [['SQLite', '嵌入式关系数据库。'], ['SQL', '定义、查询和修改关系数据的语言。'], ['constraint', '数据库强制执行的数据规则。'], ['parameter binding', '把 SQL 结构和值分开传递。'], ['transaction', '共同提交或回滚的一组操作。']],
  ),
  15: lessonDetail(
    ['Web API 是前端与后端之间的契约。浏览器发出 HTTP 请求，服务器解析请求、执行规则和数据库操作，再返回 HTTP 响应。', '你已经在前端使用过 fetch，今天从服务器视角重新拆开方法、路径、状态码、头和 JSON。'],
    [['请求', '客户端发送给服务器的方法、地址、头和可选正文。'], ['响应', '服务器返回的状态码、头和可选正文。'], ['资源', 'API 中被识别和操作的业务对象。'], ['幂等', '同一请求执行多次与一次的最终效果相同。']],
    [
      chapter('HTTP 请求和响应的组成', '一次 fetch 到达服务器时带了什么？', ['请求行包含方法和路径，请求头描述内容类型、认证等元数据，请求体携带 JSON。服务器返回状态码、响应头和正文。', 'HTTP 本身无状态，每个请求应携带处理所需身份或会话信息。服务端不能假设“上一个请求刚刚做过什么”。'], 'POST /orders HTTP/1.1\nContent-Type: application/json\nAuthorization: Bearer token\n\n{"user_id": 42, "items": [{"sku": "A-01", "quantity": 2}]}', ['POST 表示创建意图。', '/orders 标识订单集合。', 'Content-Type 声明正文为 JSON。', '服务端解析正文后仍必须校验字段和权限。'], '对应 fetch(url, {method, headers, body}) 的每个参数。现在需要设计服务器如何解释它们。', ['Content-Type 与 Accept 有什么区别？', 'Content-Type 描述当前正文格式，Accept 表示客户端希望收到的格式。']),
      chapter('资源、方法与幂等性', '路径和方法如何共同表达业务？', ['路径优先使用名词资源：/orders 是集合，/orders/42 是单个订单。GET 读取，POST 创建，PUT 替换，PATCH 局部更新，DELETE 删除。', 'GET、PUT 和 DELETE 设计上通常应幂等。POST 创建通常不幂等，重复提交可通过幂等键或业务唯一约束避免重复。'], 'GET /orders?status=paid&limit=20\nPOST /orders\nGET /orders/42\nPATCH /orders/42\nPOST /orders/42/cancellation', ['查询参数筛选订单集合。', 'POST 集合创建新资源。', 'GET 具体 ID 读取一条。', '取消是有业务规则的动作，可建明确子资源。'], '前端路由关注页面，API 路由关注业务资源。两者都需要稳定命名。', ['重复执行 DELETE /orders/42 是否必须每次都返回相同状态码？', '幂等要求最终状态相同，不要求每次响应完全一致。']),
      chapter('状态码与错误契约', '客户端如何知道失败原因？', ['2xx 表示成功，4xx 表示请求或权限问题，5xx 表示服务器未能完成。201 创建成功，204 成功无正文，400 通用坏请求，401 未认证，403 无权限，404 不存在，409 当前状态冲突，422 字段校验失败。', '状态码只提供大类，JSON 错误 code 提供稳定机器判断，message 给人阅读，details 放安全的上下文。'], '{\n  "code": "INVALID_TRANSITION",\n  "message": "已发货订单不能取消",\n  "details": {"order_id": 42, "status": "shipped"}\n}', ['HTTP 409 表达资源状态冲突。', 'code 供前端稳定分支。', 'message 可展示或辅助调试。', 'details 不应泄露内部堆栈。'], '前端不应靠匹配中文 message 判断逻辑，应使用状态码和稳定 code。', ['401 和 403 的区别是什么？', '401 表示未通过认证，403 表示身份已知但没有所需权限。']),
    ],
    [['所有失败返回 200', '客户端无法用协议区分成功失败。', '选择语义状态码并提供错误 code。'], ['路径使用 createOrder 动词', '方法已经表达动作，路径不一致。', '普通 CRUD 使用名词资源。'], ['把 traceback 返回前端', '泄露代码路径和内部实现。', '记录服务端日志，客户端返回安全错误。']],
    [['请求拆解', '从浏览器 Network 面板找一个请求，标出方法、路径、状态码和 JSON。', '先选自己熟悉的接口。', '能解释每部分由谁产生。'], ['契约设计', '为订单创建、列表、详情、取消设计请求响应。', '先写失败情况。', '状态码和错误 code 一致。'], ['幂等思考', '说明重复创建订单如何避免双单。', '考虑业务订单号或 Idempotency-Key。', '能写出服务端判断步骤。']],
    [['HTTP method', '请求想执行的操作类别。'], ['status code', '响应结果的协议分类。'], ['header', '请求或响应的元数据。'], ['REST', '围绕资源和统一 HTTP 语义组织接口的风格。'], ['idempotency', '重复执行不继续改变最终状态的性质。']],
  ),
  16: lessonDetail(
    ['FastAPI 把 Python 类型标注连接到 HTTP：路由函数参数描述请求，Pydantic 模型验证 JSON，返回类型形成文档。', '框架负责通用协议工作，业务规则仍由你编写。今天只实现最小应用、请求模型和响应边界。'],
    [['ASGI', 'Python 异步 Web 应用与服务器之间的标准接口。'], ['路由', '方法和路径到处理函数的映射。'], ['请求模型', '描述并验证客户端输入。'], ['OpenAPI', '机器可读的 HTTP API 契约格式。']],
    [
      chapter('应用、服务器与路由', 'Uvicorn 和 FastAPI 分别负责什么？', ['FastAPI 创建应用对象并定义路由，Uvicorn 是 ASGI 服务器，负责监听端口、接收 HTTP 并调用应用。', '装饰器 @app.get 把方法和路径注册到函数。函数返回 dict 时框架将其编码为 JSON。'], 'from fastapi import FastAPI\n\napp = FastAPI(title="Order API")\n\n@app.get("/health")\ndef health() -> dict[str, bool]:\n    return {"ok": True}\n\n# uvicorn app:app --reload', ['第一个 app 是模块名。', '第二个 app 是模块中的应用对象。', '请求 GET /health 时执行 health。', '返回 dict 被编码为 JSON。'], '类似 Express app.get，但 FastAPI 从函数签名获取更多类型与文档信息。', ['--reload 适合生产环境吗？', '不适合。它用于开发时监控文件变化并重启。']),
      chapter('路径、查询和请求体', 'FastAPI 如何知道数据从哪里来？', ['路径模板中的同名参数来自 URL 路径；普通标量且不在路径中通常来自查询参数；Pydantic 模型来自 JSON 请求体。', '类型转换失败或 Field 约束不满足时，框架在进入路由前返回 422。'], 'from pydantic import BaseModel, Field\n\nclass OrderCreate(BaseModel):\n    user_id: int = Field(gt=0)\n    note: str = Field(default="", max_length=200)\n\n@app.post("/orders", status_code=201)\ndef create_order(payload: OrderCreate):\n    return {"id": 1, **payload.model_dump()}', ['JSON 被解析为 OrderCreate。', 'user_id 必须大于零。', 'note 可省略但最长 200。', '合法数据才进入函数。'], '对应前端运行时 schema 校验库，但 FastAPI 将 schema 同时用于文档和请求处理。', ['Pydantic 字段校验适合检查“订单是否已支付”吗？', '不适合，该规则依赖数据库状态，应在业务层检查。']),
      chapter('响应模型和自动文档', '为什么不能直接返回数据库整行？', ['response_model 定义对外字段，过滤内部值并验证输出。输入模型、数据库模型和响应模型不一定相同。', 'FastAPI 根据路由与模型生成 /docs 和 /openapi.json。文档是代码契约的结果，但仍需要示例和业务说明。'], 'class OrderRead(BaseModel):\n    id: int\n    user_id: int\n    status: str\n\n@app.post("/orders", response_model=OrderRead, status_code=201)\ndef create_order(payload: OrderCreate):\n    return {"id": 1, "user_id": payload.user_id, "status": "open", "secret": "hidden"}', ['内部结果含 secret。', '响应模型只声明三个公开字段。', '最终 JSON 不包含 secret。', '/docs 显示请求和响应 schema。'], '类似 TypeScript DTO，但响应模型在运行时参与过滤和校验。', ['响应模型校验失败通常说明什么？', '服务端实现没有满足自己声明的契约，是应修复的服务器错误。']),
    ],
    [['把所有逻辑写进路由', '业务无法脱离 HTTP 测试。', '路由只处理协议，规则逐步移到函数或服务。'], ['请求和响应共用一个模型', '输入字段可能被意外回显。', '按边界分别定义 Create 与 Read。'], ['开发时看得到 /docs 就认为完成', '文档不代表业务规则正确。', '仍需测试成功和失败路径。']],
    [['启动应用', '创建 /health 并从浏览器访问。', '使用 uvicorn module:app --reload。', '状态 200 且 JSON 为 ok true。'], ['请求校验', '创建 OrderCreate，尝试 user_id=0 和超长 note。', '在 /docs 直接发送。', '非法输入在路由前返回 422。'], ['响应边界', '内部字典加入 internal_note，响应模型不声明。', '观察最终 JSON。', '内部字段不泄露。']],
    [['FastAPI', '基于类型提示的 Python Web 框架。'], ['Uvicorn', '运行 ASGI 应用的服务器。'], ['Pydantic', '运行时解析和校验数据的库。'], ['schema', '数据允许字段与类型的结构说明。'], ['OpenAPI', '描述 HTTP API 的标准。']],
  ),
  17: lessonDetail(
    ['数据库连接、配置和当前用户会被许多路由使用。如果每个路由手动建立和清理，会出现重复与遗漏。FastAPI 依赖系统把这些请求级资源统一管理。', '今天还会明确三种校验位置：字段形状在模型，业务状态在服务，数据库完整性由约束兜底。'],
    [['依赖注入', '由框架提供函数所需对象，而不是函数内部自行创建。'], ['请求作用域', '只在一次 HTTP 请求生命周期内存在的资源范围。'], ['异常处理器', '把特定异常统一转换成 HTTP 响应。'], ['业务异常', '用业务语言描述规则失败的异常。']],
    [
      chapter('Depends 与资源生命周期', '数据库连接何时打开和关闭？', ['依赖函数在路由执行前运行，返回或 yield 的值注入参数。同一请求中相同依赖默认复用结果。', 'yield 前创建资源，yield 后清理。无论路由成功还是异常，清理部分都会执行。'], 'from fastapi import Depends\n\ndef get_db():\n    conn = sqlite3.connect(DB_PATH)\n    conn.row_factory = sqlite3.Row\n    try:\n        yield conn\n    finally:\n        conn.close()\n\n@app.get("/orders/{order_id}")\ndef get_order(order_id: int, db = Depends(get_db)):\n    ...', ['请求到来先执行 get_db。', '连接通过 yield 注入 db。', '路由使用连接。', '请求结束执行 finally 关闭。'], '类似 React Context 的提供与消费，但 FastAPI 依赖还有请求生命周期和清理能力。', ['为什么不在全局创建一个 SQLite 连接供所有请求用？', '连接并发和生命周期难控制，请求级连接更安全清楚。']),
      chapter('三层校验各司其职', '一条规则应该放在哪一层？', ['字段格式和单字段范围由 Pydantic 处理；需要查询当前订单或用户的数据规则由服务处理；非空、唯一和外键等最终完整性由数据库约束兜底。', '把“订单已发货不能取消”只写在路由会让脚本或任务调用绕过。它必须位于所有入口共同经过的业务函数。'], 'def cancel_order(db, order_id: int) -> Order:\n    order = find_order(db, order_id)\n    if order is None:\n        raise OrderNotFound(order_id)\n    if order.status == Status.SHIPPED:\n        raise InvalidTransition("已发货订单不能取消")\n    ...', ['先读取当前状态。', '不存在抛业务异常。', '不允许的状态抛另一业务异常。', '路由不重复规则。'], '前端表单校验改善体验，服务端校验保护数据。两边都需要，但服务端是最终可信边界。', ['数据库 UNIQUE 失败后还需要应用提前检查吗？', '可提前检查提供友好体验，但仍必须保留 UNIQUE 处理并发竞态。']),
      chapter('统一异常到 HTTP', '如何避免每个路由重复 try/except？', ['业务层抛不知道 HTTP 的异常，应用边界注册 exception_handler 将类型映射为状态码与错误 JSON。', '意外异常由统一 500 处理并记录服务端日志，响应不包含堆栈。'], '@app.exception_handler(OrderNotFound)\nasync def handle_not_found(request, error):\n    return JSONResponse(\n        status_code=404,\n        content={"code": "ORDER_NOT_FOUND", "message": str(error)},\n    )', ['任何请求抛 OrderNotFound 都到同一处理器。', '处理器选择 404。', 'code 保持稳定。', '业务异常本身不导入 FastAPI。'], '类似前端全局错误边界，但这里还要准确映射协议状态码。', ['为什么业务层不直接 raise HTTPException？', '会把核心规则绑定到 Web 框架，脚本和测试难以复用。']),
    ],
    [['每个路由打开连接却不关闭', '异常路径泄露资源。', '使用 yield 依赖统一管理。'], ['所有校验写进 Pydantic', '模型无法可靠访问当前数据库状态。', '跨实体业务规则放服务层。'], ['业务代码抛 HTTPException', '核心层耦合传输协议。', '抛业务异常，由边界翻译。']],
    [['连接依赖', '实现 get_db 并打印打开关闭顺序。', '请求成功和失败各试一次。', '两条路径都关闭。'], ['规则迁移', '把取消校验从路由移到 cancel_order。', '直接调用函数写单测。', '绕过 HTTP 规则仍生效。'], ['错误映射', '为 NotFound、Duplicate、InvalidTransition 注册处理器。', '分别使用 404、409、409。', '所有响应结构一致。']],
    [['dependency injection', '外部提供函数依赖的机制。'], ['request scope', '一次请求内的资源生命周期。'], ['domain exception', '用业务含义描述失败。'], ['exception handler', '统一捕获并翻译异常的边界。']],
  ),
  18: lessonDetail(
    ['随着路由增加，SQL、业务规则和 HTTP 细节混在一起会让任何修改都牵动整段代码。分层的目的，是让不同变化原因有清楚位置。', '今天只使用最小三层词汇：router 处理 HTTP，service 完成用例，repository 或数据库函数处理持久化。简单项目不需要更多层。'],
    [['用例', '用户或系统想完成的一次业务动作。'], ['服务层', '编排规则、实体与事务的用例入口。'], ['仓储', '封装领域对象的持久化和查询细节。'], ['DTO', '跨边界传递数据的结构对象。']],
    [
      chapter('Router 只做协议翻译', '路由函数应该多薄？', ['路由读取路径、查询、请求模型和依赖，调用业务用例，再返回响应。它可以做少量协议转换，但不应包含 SQL 或多层状态判断。', '“薄”不是硬性行数，而是变化原因单一：HTTP 契约变化改路由，业务规则变化不应改路由。'], '@router.post("/orders", response_model=OrderRead, status_code=201)\ndef create(payload: OrderCreate, db = Depends(get_db)):\n    order = create_order(db, payload.user_id, payload.items)\n    return OrderRead.model_validate(order)', ['框架先验证 payload。', '路由取得 db 依赖。', '调用业务函数完成用例。', '结果转换为公开响应。'], '对应前端组件只协调事件和展示，不把所有领域计算堆在 JSX 中。', ['路由里写一条简单 get 查询一定错误吗？', '不一定。分层解决真实复杂度；当查询和规则增长或需要测试时再抽离。']),
      chapter('Service 维护业务和事务', '一个完整业务动作在哪里开始和结束？', ['服务层读取现有状态、校验规则、调用持久化并控制事务。核心规则可以是模块函数，不必为“架构”强制创建 Service 类。', '一个服务函数应以业务动词命名，例如 cancel_order。它返回业务结果或抛业务异常。'], 'def cancel_order(conn, order_id: int) -> Order:\n    with conn:\n        order = require_order(conn, order_id)\n        order.cancel()\n        save_order(conn, order)\n        append_event(conn, order.id, "cancelled")\n    return order', ['事务覆盖整个取消用例。', 'require_order 处理不存在。', '实体方法维护状态规则。', '订单和事件共同提交。'], '类似前端 application service 或 thunk，但服务端还承担持久事务。', ['为什么事务不应在每个 repository 函数内自动 commit？', '服务无法把多个写入组成一个原子业务动作。']),
      chapter('Repository 隔离 SQL', '什么时候值得增加持久化层？', ['当 SQL 数量增加、行到对象转换重复或测试需要替换存储时，数据库函数或 repository 可隐藏这些细节。', '不要一开始创建通用 BaseRepository。订单、用户查询语义不同，过度通用会泄露 SQL 细节并增加抽象。'], 'def find_order(conn, order_id: int) -> Order | None:\n    row = conn.execute(\n        "SELECT id, status FROM orders WHERE id = ?",\n        (order_id,),\n    ).fetchone()\n    return Order(id=row["id"], status=Status(row["status"])) if row else None', ['函数只处理查询与映射。', '参数绑定保护输入。', '无行返回 None。', '有行转换为领域对象。'], '类似前端 API client 隔离 fetch 细节，但这里隔离 SQL 和行映射。', ['一个实现是否必须先定义 Repository 接口？', '不必须。直接函数或具体类更简单，出现第二实现或替身需求再抽象。']),
    ],
    [['按目录模板创建十层', '没有真实职责差异，调用跳转过多。', '从 router、业务函数和数据库函数开始。'], ['repository 自动 commit', '破坏跨多写入事务。', '事务由用例边界控制。'], ['业务规则在三层各写一次', '修复时版本漂移。', '收敛到所有调用都经过的核心入口。']],
    [['流程标注', '在现有创建接口每行标记 HTTP、业务或 SQL。', '先理解真实流。', '能看到哪些变化原因混杂。'], ['提取服务', '把取消逻辑提到 cancel_order 并直接单测。', '路由只传参数。', '无 HTTP 也能验证状态。'], ['事务测试', '事件写入失败时断言订单状态未改变。', '让两项写入在一个事务。', '数据库保持一致。']],
    [['router', 'HTTP 协议入口。'], ['service', '业务用例编排边界。'], ['repository', '持久化查询与映射边界。'], ['transaction boundary', '一组业务写入共同提交的范围。'], ['DTO', '边界之间的数据载体。']],
  ),
  19: lessonDetail(
    ['Web 服务暴露在不可信网络中。安全不是最后加一个登录接口，而是从每个信任边界限制输入、身份、权限、数据和日志。', '今天建立最小安全模型：认证确认身份，授权检查权限，秘密不进源码，密码只保存专用哈希。'],
    [['认证', '确认请求者身份。'], ['授权', '确认已知身份是否允许执行操作。'], ['哈希', '把输入单向映射为摘要以便验证。'], ['机密', 'API Key、密码等不能公开的数据。']],
    [
      chapter('认证和授权是两个步骤', '登录成功为什么还不能执行管理操作？', ['认证验证凭证并得到用户身份。授权根据角色、资源归属和动作决定是否允许。每个受保护用例都应在服务端检查。', '401 用于没有有效身份，403 用于身份已知但权限不足。为了减少资源枚举，个别场景可按安全策略返回 404，但应保持一致。'], 'def require_admin(user = Depends(current_user)):\n    if "admin" not in user.roles:\n        raise Forbidden("需要管理员权限")\n    return user', ['current_user 先完成认证。', 'require_admin 检查角色。', '不满足抛业务或 HTTP 边界异常。', '路由依赖返回已授权用户。'], '前端隐藏按钮只改善体验，不能构成授权。攻击者可以直接构造请求。', ['“只能修改自己的订单”属于认证还是授权？', '授权。身份已知后还要比较资源归属。']),
      chapter('密码、API Key 与环境变量', '为什么不能保存明文密码？', ['密码应使用 Argon2、bcrypt 或 scrypt 等专用慢哈希，并为每个密码使用随机盐。验证时重新计算并安全比较，不需要解密原密码。', 'API Key 等服务秘密通过环境变量或秘密管理系统注入。开发用 .env 也不能提交版本库。比较敏感 token 可使用 hmac.compare_digest。'], 'import hmac\nimport os\n\ndef verify_api_key(provided: str) -> bool:\n    expected = os.environ["ADMIN_API_KEY"]\n    return hmac.compare_digest(provided, expected)', ['启动环境提供真实密钥。', '源码只读取变量名。', 'compare_digest 降低普通字符串早停比较的时间泄露。', '日志不能输出 provided 或 expected。'], '对应前端环境变量，但服务端秘密可真正保密；打包进浏览器的变量对用户可见。', ['哈希和加密有什么区别？', '哈希设计为单向验证，加密用密钥可逆恢复原文。']),
      chapter('常见输入和数据风险', 'Pydantic 校验后还会有注入吗？', ['类型校验不自动阻止所有注入。SQL 必须参数化，文件路径必须限制范围，外部 URL 可能引发服务端请求伪造，HTML 输出需要前端正确转义。', '遵循最小权限：数据库账号只拥有所需权限，响应只返回所需字段，错误和日志不包含敏感内容。依赖要定期更新，但不要盲目追新。'], 'safe_row = conn.execute(\n    "SELECT * FROM users WHERE email = ?",\n    (email,),\n).fetchone()', ['email 即使包含引号也只是参数值。', 'SQL 结构不会被改变。', '响应模型再限制公开字段。'], '前端防 XSS 与后端防 SQL 注入都遵循同一原则：不要把不可信数据当作代码或结构执行。', ['客户端已经验证过输入，服务端可以跳过吗？', '不可以。客户端完全由请求者控制，服务端必须重新验证。']),
    ],
    [['把密钥写进源码', '版本历史和分发包会永久泄露。', '环境变量注入并加入启动校验。'], ['前端隐藏按钮当授权', '请求可绕过界面。', '每个服务端用例检查权限。'], ['自己设计密码加密算法', '很难抵抗已知攻击。', '使用成熟密码哈希库和默认安全参数。']],
    [['身份矩阵', '列出匿名、普通用户、管理员对三个接口的权限。', '用表格覆盖资源归属。', '每个格子有允许或拒绝。'], ['API Key', '实现环境变量读取和安全比较。', '缺失时启动失败。', '日志无密钥。'], ['越权测试', '用户 A 尝试修改用户 B 的订单。', '直接构造 HTTP 请求。', '服务端返回 403 且数据不变。']],
    [['authentication', '确认身份。'], ['authorization', '判断权限。'], ['credential', '证明身份的凭据。'], ['password hash', '用于验证密码的单向慢哈希。'], ['least privilege', '只给予完成任务所需的最小权限。']],
  ),
  20: lessonDetail(
    ['Web 服务大量时间花在等待数据库、网络和文件 I/O。异步编程让单个线程在等待时处理其他任务，但它不会让 CPU 计算自动更快。', '今天理解协程何时真正执行，并为并发调用加入超时、取消和降级。'],
    [['协程', '可以暂停并稍后继续的异步计算对象。'], ['事件循环', '调度可运行协程和 I/O 完成事件的循环。'], ['Task', '已被安排到事件循环执行的协程。'], ['取消', '请求正在运行的异步任务尽快停止。']],
    [
      chapter('async def、coroutine 与 await', '调用异步函数为什么没有立刻运行？', ['async def 定义协程函数，调用后返回 coroutine 对象。await 才把控制交给协程，并在其等待 I/O 时让事件循环处理其他任务。', 'asyncio.run 在同步入口创建事件循环并运行顶层协程。Web 服务器已经管理事件循环，路由中不应再调用 asyncio.run。'], 'import asyncio\n\nasync def fetch_price() -> int:\n    await asyncio.sleep(0.1)\n    return 1299\n\nasync def main():\n    price = await fetch_price()\n    print(price)\n\nasyncio.run(main())', ['调用 main 创建顶层协程。', 'asyncio.run 驱动它。', 'await sleep 时当前协程暂停。', '计时到达后继续并返回 1299。'], '对应 Promise 与 await，但 Python coroutine 如果没有 await 或调度，通常不会自动开始。', ['忘记 await 会得到什么？', '通常得到 coroutine 对象和“never awaited”警告，而不是最终结果。']),
      chapter('并发执行独立 I/O', '连续 await 和并发启动有何不同？', ['先 await A 再 await B 会串行等待。如果 A、B 独立，可创建 Task 或使用 TaskGroup 同时推进。', 'TaskGroup 提供结构化生命周期：组内任务完成后才离开，失败时相关任务被一致取消。'], 'async def snapshot():\n    async with asyncio.TaskGroup() as group:\n        stock_task = group.create_task(fetch_stock())\n        price_task = group.create_task(fetch_price())\n    return {"stock": stock_task.result(), "price": price_task.result()}', ['进入 TaskGroup。', '两个协程都被安排执行。', '离开 with 前等待两者完成。', '之后安全读取结果。'], '对应 Promise.all，但 TaskGroup 对异常和取消的生命周期更结构化。', ['两个请求有依赖关系时应该并发吗？', '不应。后一个需要前一个结果时必须顺序 await。']),
      chapter('超时、取消和阻塞边界', '外部服务一直不返回怎么办？', ['每个外部 I/O 都应有超时。超时后决定整个用例失败还是仅降级可选数据。取消异常需要传播，finally 中仍要清理资源。', 'time.sleep、同步 HTTP 和 CPU 密集循环会阻塞事件循环。使用对应异步库、线程池或独立进程，不要仅把函数改成 async。'], 'async def load_inventory():\n    try:\n        async with asyncio.timeout(0.8):\n            return await fetch_inventory()\n    except TimeoutError as error:\n        raise InventoryUnavailable() from error', ['最多等待 0.8 秒。', '超时会取消内部等待并抛 TimeoutError。', '转换为业务可理解异常。', '上层映射为稳定响应。'], 'Promise 也需要 AbortController 和超时；await 本身从不自动提供期限。', ['在 async def 中调用 time.sleep 会怎样？', '阻塞整个事件循环线程，其他协程也无法推进。']),
    ],
    [['把所有路由改 async', '内部仍是同步阻塞代码，没有收益甚至阻塞事件循环。', '先识别真实 I/O 库是否异步。'], ['创建 Task 后不等待', '任务生命周期脱离请求，错误和清理丢失。', '使用 TaskGroup 或明确 await。'], ['外部调用无超时', '请求可无限占用资源。', '定义连接、读取或总超时与失败策略。']],
    [['顺序对比', '两个各等待 1 秒的协程分别串行和 TaskGroup 运行。', '用 perf_counter 记录时间。', '串行约 2 秒，并发约 1 秒。'], ['超时', '给模拟慢库存添加 200ms 超时。', '捕获 TimeoutError。', '程序在有限时间返回明确失败。'], ['阻塞实验', '比较 time.sleep 与 asyncio.sleep 对并发计时器的影响。', '不要在生产服务实验。', '能解释事件循环被谁阻塞。']],
    [['coroutine', '可暂停恢复的异步计算。'], ['event loop', '调度协程与 I/O 的执行器。'], ['Task', '已安排执行的协程。'], ['timeout', '操作允许等待的最长时间。'], ['cancellation', '协作请求任务停止。']],
  ),
  21: lessonDetail(
    ['单元测试保护核心规则，API 测试保护从 HTTP 请求到数据库响应的完整链路。二者互补：所有组合都走 HTTP 会慢，只测函数又可能漏掉路由和序列化。', '今天建立临时数据库、依赖覆盖和失败契约测试，并完成第三周综合复盘。'],
    [['集成测试', '验证多个真实组件共同工作的测试。'], ['测试客户端', '无需真实网络端口就能调用应用的工具。'], ['依赖覆盖', '测试时把生产依赖替换为可控实现。'], ['契约测试', '验证接口对外输入输出约定。']],
    [
      chapter('API 测试的完整路径', '一个 client.post 会经过哪些层？', ['TestClient 或 HTTPX 把请求交给 ASGI 应用，经历路由匹配、Pydantic 校验、依赖、服务、数据库、响应模型和异常处理器。', '因此 API 测试适合验证状态码、JSON 和持久化结果；纯规则仍用更快单元测试定位。'], 'def test_create_order(client):\n    response = client.post(\n        "/orders",\n        json={"user_id": 42, "items": [{"sku": "A-01", "quantity": 2}]},\n    )\n    assert response.status_code == 201\n    body = response.json()\n    assert body["user_id"] == 42', ['客户端构造真实 JSON 请求。', '应用执行全部请求链。', '先断言协议状态码。', '再验证关键业务字段。'], '对应前端组件集成测试，但这里不需要启动浏览器。', ['为什么不只断言 status_code == 201？', '响应字段或数据库写入仍可能错误，应断言本场景关键结果。']),
      chapter('临时数据库与依赖覆盖', '测试为什么不能连接开发数据库？', ['测试会创建、修改和删除数据，必须使用每个测试独立的临时数据库。tmp_path 提供临时目录，fixture 建表，依赖覆盖让应用使用测试连接。', '测试结束后恢复 dependency_overrides，避免其他测试受到污染。'], '@pytest.fixture\ndef client(tmp_path):\n    db_path = tmp_path / "test.db"\n    initialize_database(db_path)\n\n    def get_test_db():\n        with sqlite3.connect(db_path) as conn:\n            conn.row_factory = sqlite3.Row\n            yield conn\n\n    app.dependency_overrides[get_db] = get_test_db\n    yield TestClient(app)\n    app.dependency_overrides.clear()', ['每个测试获得新的临时路径。', '按生产 schema 建表。', '覆盖连接依赖。', '测试后清除覆盖。'], '类似前端 mock API base URL，但这里可以使用真实 SQLite 行为而不碰开发数据。', ['能否让所有测试共享一个数据库提高速度？', '小项目通常不值得，隔离更重要；大套件可用事务回滚等受控策略。']),
      chapter('风险驱动的失败路径', '哪些 API 场景最值得先测？', ['优先测试输入非法、资源不存在、重复提交、非法状态、权限不足和事务回滚。这些失败会直接影响数据正确性或客户端分支。', '不要断言完整错误文案的每个标点，优先断言状态码、稳定 code 和关键 details。'], 'def test_cannot_cancel_shipped_order(client, shipped_order):\n    response = client.post(f"/orders/{shipped_order}/cancellation")\n    assert response.status_code == 409\n    assert response.json()["code"] == "INVALID_TRANSITION"', ['fixture 建立已发货订单。', '请求尝试非法取消。', '409 表达状态冲突。', '稳定 code 供客户端判断。'], '对应前端测试错误态组件，但服务端首先要保证错误契约稳定。', ['测试顺序改变后失败说明什么？', '测试共享或泄露状态，需要隔离数据和依赖。']),
    ],
    [['测试连接真实开发库', '可能污染或删除个人数据。', '使用 tmp_path 临时数据库。'], ['mock 掉所有层', '测试不再验证真实集成。', '只替换外部不稳定边界。'], ['只测试 2xx', '最危险的规则没有保护。', '按风险覆盖 4xx 与回滚。']],
    [['首个 API 测试', '测试 /health 和订单创建。', '先断言状态码再断言 JSON。', '不启动真实端口。'], ['依赖覆盖', '让 API 使用 tmp_path 数据库。', '测试后 clear。', '开发数据库完全不变。'], ['阶段项目', '90 分钟实现 Todo API 与 6 个测试。', '先跑通创建，再补更新删除和失败。', '成功与 4 类失败均有证据。']],
    [['integration test', '验证多个组件连接后的行为。'], ['TestClient', '进程内调用 ASGI 应用的客户端。'], ['dependency override', '测试替换应用依赖。'], ['temporary database', '测试专用且可丢弃的数据存储。'], ['contract', '客户端可依赖的接口约定。']],
  ),
  22: lessonDetail(
    ['代码在开发、测试和生产环境中相同，变化的应是数据库路径、日志级别和秘密等配置。配置需要集中读取、启动时验证，而不是散落在业务函数中。', '当生产问题发生时，请求 ID、结构化日志和健康检查让你知道发生了什么、影响了谁、系统是否仍可服务。'],
    [['配置', '在不同运行环境中变化的应用参数。'], ['环境变量', '由操作系统向进程提供的键值配置。'], ['可观察性', '通过日志、指标和追踪理解系统内部状态。'], ['健康检查', '供运行平台判断进程存活或依赖就绪的接口。']],
    [
      chapter('集中加载和验证配置', '为什么不能在每个函数里 os.getenv？', ['分散读取让默认值和缺失行为不一致，也无法在启动时一次验证。设置对象应在应用装配阶段创建，再通过依赖传入。', '秘密没有安全默认值，缺失应快速失败；开发便利配置可以有明确默认。不要把整个环境变量对象传遍业务层。'], 'from dataclasses import dataclass\nimport os\n\n@dataclass(frozen=True)\nclass Settings:\n    database_path: str\n    admin_api_key: str\n\ndef load_settings() -> Settings:\n    return Settings(\n        database_path=os.getenv("DATABASE_PATH", "data/app.db"),\n        admin_api_key=os.environ["ADMIN_API_KEY"],\n    )', ['数据库路径有开发默认值。', 'API Key 使用索引读取，缺失立即失败。', '返回不可变设置对象。', '应用启动时只加载一次。'], '类似前端构建配置，但服务端环境变量在运行时提供且可以保密。', ['为什么 API Key 不应有 "change-me" 默认值？', '生产可能忘记覆盖并以公开默认密钥运行。']),
      chapter('结构化日志与请求 ID', '一次请求跨多函数时如何关联日志？', ['请求进入时生成或接受受控 request_id，后续日志都带相同标识。日志使用稳定字段名，便于搜索和聚合。', 'INFO 记录业务完成，WARNING 记录可恢复降级，ERROR 记录最终失败。不要把每个函数入口都写日志，重点记录决策和结果。'], 'request_id = request.headers.get("X-Request-ID") or str(uuid.uuid4())\nlogger.info(\n    "order_created request_id=%s order_id=%s user_id=%s",\n    request_id, order.id, order.user_id,\n)', ['取得或生成请求 ID。', '业务完成时记录标识。', '不记录请求完整正文。', '排障时按 request_id 搜索同一链路。'], '对应前端埋点 correlation id，但服务端还要关联数据库和外部服务调用。', ['用户提供的 request_id 可以完全信任吗？', '不应无限长度或任意格式地写日志，应校验或生成内部 ID。']),
      chapter('存活、就绪和启动检查', '进程在运行为什么还可能不能接请求？', ['存活检查说明进程未卡死，就绪检查确认数据库等关键依赖可用。部署平台可在未就绪时停止分配流量。', '健康检查必须快速、稳定，不执行业务写入。非关键外部服务可通过降级而不是让整个应用不就绪。'], '@app.get("/health")\ndef health():\n    return {"ok": True}\n\n@app.get("/ready")\ndef ready(db = Depends(get_db)):\n    db.execute("SELECT 1")\n    return {"ready": True}', ['health 不依赖数据库。', 'ready 执行最小数据库查询。', '查询失败让就绪检查失败。', '接口不创建业务数据。'], '类似前端应用启动检查，但服务端会被负载均衡器主动探测。', ['健康检查可以调用五个外部 API 吗？', '通常不应，检查会变慢且抖动；只验证接收流量必需的关键依赖。']),
    ],
    [['秘密使用开发默认值', '生产遗漏配置也能启动。', '关键秘密缺失立即失败。'], ['日志打印整个请求', '泄露敏感数据并制造大量噪音。', '记录必要标识与结果。'], ['health 查询所有依赖', '探针不稳定并放大故障。', '区分存活与关键就绪。']],
    [['配置检查', '移除 API Key 启动应用，确认错误清楚。', '只显示变量名。', '应用在接流量前失败。'], ['请求关联', '一次创建订单产生两条带相同 request_id 的日志。', '中间件保存上下文。', '可通过 ID 找全链路。'], ['就绪演练', '数据库路径无权限时 /ready 失败而 /health 仍可响应。', '理解两者语义。', '部署可据此停止流量。']],
    [['settings', '集中管理应用配置的对象。'], ['environment variable', '进程外注入的配置值。'], ['request ID', '关联一次请求所有记录的标识。'], ['liveness', '进程是否活着。'], ['readiness', '应用是否准备好接收流量。']],
  ),
  23: lessonDetail(
    ['项目结构的目标是让修改落在少数、正确的位置。目录对称和设计模式数量不等于可维护。先追踪真实调用流，再按变化原因组织。', '今天会识别循环依赖、重复规则和不稳定公共接口，并用最小改动收敛它们。'],
    [['内聚', '一个模块中的内容共同服务同一职责。'], ['耦合', '一个模块对其他模块细节的依赖程度。'], ['依赖方向', '模块之间谁可以引用谁的规则。'], ['公共接口', '其他模块允许依赖的稳定入口。']],
    [
      chapter('按变化原因组织模块', '哪些代码应该放在一起？', ['总在同一需求中一起修改的代码应靠近。HTTP 字段、订单规则和 SQLite 查询因不同原因变化，可以分别放置。', '不要按名词机械创建 models、utils、helpers 巨型桶。模块名应表达业务或明确技术边界。'], 'app/\n    main.py       # 应用装配与 HTTP\n    orders.py     # 订单用例和规则\n    database.py   # SQLite 连接和查询\n    settings.py   # 启动配置', ['每个文件有一个主要变化原因。', 'main 依赖其他模块完成装配。', 'orders 不导入 FastAPI。', 'database 不决定 HTTP 状态码。'], '类似前端按 feature 和边界组织，而不是把所有函数塞进 utils。', ['文件超过多少行必须拆？', '没有固定数字。出现多个不同变化原因或难以命名的职责时才是信号。']),
      chapter('依赖方向与循环导入', '两个模块互相 import 说明什么？', ['核心业务应被外层 HTTP 和数据库适配依赖，核心不反向依赖框架。循环导入常表示共享模型位置错误或两个模块职责纠缠。', '先画导入箭头，移动真正共同的类型或合并无法独立变化的模块。不要用函数内 import 到处掩盖结构问题。'], 'main.py -> orders.py -> database.py\n             |\n             -> domain.py', ['箭头指向被依赖模块。', 'domain 不导入 FastAPI。', '低层模块不知道 HTTP。', '依赖整体向核心规则收敛。'], '对应前端状态层不应反向依赖具体页面组件。', ['把所有共享内容放 common.py 能解决循环吗？', '可能暂时消除导入，但容易形成无边界杂物模块，应移动到有明确含义的位置。']),
      chapter('根因修复和删除重复', '同一校验有三个版本时改哪里？', ['先用搜索找到所有调用者和规则副本，确认它们是否应表达同一业务。然后把规则放到所有入口共同经过的位置，删除其他副本。', '最小正确改动可能是共享函数中的一个 guard，而不是每个路由各加一个 if。修改后用测试覆盖所有入口。'], 'def transition(order: Order, target: Status) -> None:\n    allowed = ALLOWED_TRANSITIONS[order.status]\n    if target not in allowed:\n        raise InvalidTransition(order.status, target)\n    order.status = target', ['状态表是唯一规则来源。', '所有 API、脚本和任务调用 transition。', '非法转换统一失败。', '调用者不再复制判断。'], '与前端共享 reducer 的根因修复相同：修共同状态入口，不在每个组件打补丁。', ['删除重复前为什么要搜索所有调用者？', '确保没有旁路，理解不同副本是否真是同一规则，并防止漏修。']),
    ],
    [['先套架构模板', '为不存在的变化增加层。', '从真实调用和痛点出发。'], ['循环导入用局部 import 掩盖', '依赖问题继续存在。', '画依赖图并重新定位职责。'], ['只修工单指出的一个路由', '同一根因的其他入口仍坏。', '修改共同业务入口并覆盖调用者。']],
    [['依赖图', '画出当前 main、service、repository、models 的导入箭头。', '标记任何双向箭头。', '能说明每条依赖原因。'], ['重复搜索', '用 rg 搜索状态字符串和 UPDATE 语句。', '列出所有写状态入口。', '确认唯一业务入口。'], ['删除练习', '找一个只有一处调用的包装函数，判断能否内联。', '不牺牲清晰边界。', '减少无意义跳转。']],
    [['cohesion', '模块内部内容围绕同一目的。'], ['coupling', '模块间相互依赖程度。'], ['dependency direction', '代码引用关系的整体方向。'], ['circular import', '模块互相导入形成环。'], ['root cause', '造成多个症状的共同原因。']],
  ),
  24: lessonDetail(
    ['性能问题需要测量证据。猜测后添加缓存和异步，常把代码变复杂却没有改善真正瓶颈。', '今天建立可重复基线，区分 CPU、数据库和网络等待，再选择最小优化。'],
    [['基准', '在固定条件下测量性能的可重复实验。'], ['分析器', '统计程序时间花在哪些函数的工具。'], ['索引', '数据库为快速查找维护的辅助结构。'], ['缓存', '保存计算或读取结果以便复用。']],
    [
      chapter('先建立可重复基线', '什么样的耗时数字才有意义？', ['固定数据规模、环境和操作，多次运行并关注分布而不是单次数字。time.perf_counter 测端到端时间，timeit 测小段代码，cProfile 定位累计函数耗时。', '先定义正确性断言，防止“优化”通过少做工作得到更快结果。'], 'from time import perf_counter\n\nstarted = perf_counter()\nfor _ in range(30):\n    response = client.get("/orders?limit=100")\n    assert response.status_code == 200\nelapsed = perf_counter() - started\nprint(elapsed / 30)', ['固定请求和数据。', '运行多次降低偶然波动。', '每次仍验证成功。', '记录平均值作为基线的一部分。'], '对应前端 Web Vitals 和 Performance 面板，原则同样是先测量再改。', ['只测一次接口耗时为何不可靠？', '冷启动、系统负载和缓存会造成偶然波动。']),
      chapter('算法与数据库查询', '慢是 Python 循环还是 SQL？', ['先用 profile 和查询日志定位。重复线性搜索可改为 dict 或 set，嵌套循环可能从 O(n²) 改为 O(n)。', '数据库使用 EXPLAIN QUERY PLAN 查看是否全表扫描。索引适合高频筛选、连接和排序列，但会增加写入成本。'], 'EXPLAIN QUERY PLAN\nSELECT id, status\nFROM orders\nWHERE user_id = ?\nORDER BY created_at DESC\nLIMIT 20;', ['查看查询计划。', '若高频按 user_id 和 created_at 查询，可评估复合索引。', '添加后用同样数据重测。', '同时观察插入成本。'], '类似前端用 Map 替代重复 find，但服务端还要考虑数据库执行计划。', ['给每一列都加索引好吗？', '不好。索引占空间并拖慢写入，应服务真实高频查询。']),
      chapter('缓存的收益和债务', '什么数据适合缓存？', ['缓存适合读取昂贵、重复率高、结果允许短暂过期的数据。必须定义键、容量、有效期、失效和故障行为。', '单进程 lru_cache 只在当前进程有效，不能当分布式共享状态。数据变化频繁或强一致时，直接查询可能更简单可靠。'], 'from functools import lru_cache\n\n@lru_cache(maxsize=128)\ndef country_name(code: str) -> str:\n    return load_country_name(code)', ['以 code 作为缓存键。', '首次调用执行加载。', '相同 code 后续复用。', '最多保留 128 个结果。'], '对应前端 memoization，但服务端多进程、失效和数据一致性让缓存边界更复杂。', ['没有容量限制的缓存风险是什么？', '键持续增加会导致内存无限增长。']),
    ],
    [['看到慢就加缓存', '真实瓶颈可能在错误查询。', '先 profile 和查询计划。'], ['优化后不验证结果', '可能通过漏数据变快。', '保留正确性断言和相同输入。'], ['索引越多越好', '写入与存储成本上升。', '只为真实查询模式添加。']],
    [['基线', '为列表接口固定 1000 条数据，测 30 次。', '记录环境与查询参数。', '得到可重复基线。'], ['算法改进', '把列表中的重复成员查找改为 set。', '比较不同 n。', '能解释复杂度变化。'], ['索引验证', '用 EXPLAIN 比较索引前后计划。', '不仅记录毫秒。', '确认查询实际使用索引。']],
    [['benchmark', '可重复的性能测量。'], ['profiling', '定位程序各部分耗时。'], ['complexity', '数据规模增长时资源需求的趋势。'], ['index', '加速数据库查询的辅助结构。'], ['cache invalidation', '数据变化后让缓存不再返回旧值。']],
  ),
  25: lessonDetail(
    ['你的电脑能运行不代表项目可交付。其他开发者需要知道 Python 版本、依赖、配置、启动和检查命令，并能从干净环境重复成功。', '今天建立最短质量流水线：声明直接依赖、Ruff 检查格式、pytest 验证行为。'],
    [['依赖', '项目运行或开发需要的外部包。'], ['版本约束', '允许安装的软件版本范围。'], ['lint', '静态发现可疑代码和风格问题。'], ['CI', '每次变更自动执行检查的持续集成流程。']],
    [
      chapter('可重复环境与依赖', 'requirements 应该记录什么？', ['requirements.txt 记录项目直接使用的第三方包和兼容范围。标准库不记录，当前机器所有包也不应整包复制。', '应用交付需要稳定版本。更新依赖时重新运行测试，安全更新不能只看版本号。虚拟环境目录不提交版本库。'], 'python3 -m venv .venv\nsource .venv/bin/activate\npython -m pip install -r requirements.txt\npython -m pip check', ['创建隔离环境。', '激活后 pip 安装到项目。', 'requirements 提供直接依赖。', 'pip check 验证已安装依赖关系。'], '对应 package.json 与 node_modules；.venv 像 node_modules 一样是本机生成物。', ['为什么不提交 .venv？', '体积大、平台相关且可由依赖声明重建。']),
      chapter('Ruff 与测试分工', '格式化、lint 和测试各发现什么？', ['格式化统一排版，lint 发现未使用导入、潜在错误和规则违规，测试验证业务行为。三者互补，任何一个都不能替代其余。', '从默认配置开始。只有团队真实需要时再启用更多规则，避免大量无价值警告。'], 'ruff format --check .\nruff check .\npytest -q', ['格式检查保证文件已经统一排版。', 'lint 扫描静态问题。', 'pytest 运行行为测试。', '失败命令返回非零退出码。'], '对应 Prettier、ESLint 与 Vitest 的分工。', ['Ruff 全通过能证明业务正确吗？', '不能。静态检查不理解完整业务预期，仍需要测试。']),
      chapter('一条本地和 CI 检查链', '为什么检查顺序影响反馈速度？', ['快速检查放前面，失败立即停止，减少等待。脚本使用 set -e 确保任何命令失败都传递退出码。', 'CI 应调用与本地相同的核心命令，避免“本地能过，CI 使用另一套流程”。先建立可靠检查，再添加部署。'], '#!/bin/sh\nset -e\nruff format --check .\nruff check .\npytest -q', ['脚本遇到第一项失败立即退出。', '本地开发者执行同一文件。', 'CI 也调用同一文件。', '退出码决定检查通过或失败。'], '对应 npm run check，目标是一个短、可靠、每天愿意运行的入口。', ['为什么不让脚本自动修复所有问题？', 'CI 应验证而非偷偷改代码；本地可另设 format 命令。']),
    ],
    [['pip freeze 全局环境', '包含无关和平台依赖。', '在项目 venv 只声明直接依赖。'], ['只在 CI 检查', '反馈太晚。', '提供同一条本地命令。'], ['一次启用大量 lint 规则', '产生噪音和机械修改。', '从真实问题逐步增加。']],
    [['干净安装', '新建临时 venv 按 README 安装。', '不要依赖当前激活环境。', '所有命令可复制执行。'], ['故障检查', '加入未使用 import，确认 Ruff 失败。', '修复后再运行。', '退出码恢复为零。'], ['统一入口', '创建 scripts/check.sh 或等价命令。', '顺序从快到慢。', '本地一次执行全链。']],
    [['dependency', '项目依赖的外部软件包。'], ['version constraint', '允许解析的版本范围。'], ['formatter', '自动统一代码排版。'], ['linter', '静态发现潜在问题。'], ['CI', '自动执行构建和检查的系统。']],
  ),
  26: lessonDetail(
    ['综合项目不是把所有知识一次堆上去，而是从一个用户可完成的主流程开始。轻量工单系统将连接模型、数据库、API、规则和测试。', '今天只完成“创建工单并查询”这一条纵向切片。通知、搜索、缓存和微服务都不是当前需求。'],
    [['需求', '用户需要达成的结果与约束。'], ['领域模型', '业务中重要实体、状态和规则的代码表达。'], ['纵向切片', '从入口到存储完整交付一小段用户能力。'], ['操作记录', '记录谁在何时对实体做了什么。']],
    [
      chapter('从用户行为提取模型', '需求句子如何变成字段和规则？', ['“员工创建工单，支持人员处理并关闭”至少包含 Ticket、User、Status 和 TicketEvent。字段应服务明确展示、筛选或规则，不为未来猜测添加。', '先写示例数据和关键动作，再画表。时间统一保存 UTC，外部展示时转换时区。'], 'Ticket\n- id\n- title\n- status\n- creator_id\n- assignee_id\n- created_at\n\nTicketEvent\n- ticket_id\n- actor_id\n- action\n- created_at', ['Ticket 表示当前状态。', 'Event 表示历史动作。', 'assignee 初始可以为空。', '每个字段对应需求。'], '类似前端先定义页面状态，但后端模型还要长期持久和维护历史。', ['是否现在加入优先级、SLA、附件和评论？', '没有明确当前需求就不加，先交付主链路。']),
      chapter('状态机与统一迁移', '哪些状态变化是允许的？', ['状态机明确当前状态、目标状态和允许边。open 可 assigned，assigned 可 processing，processing 可 resolved。非法边必须在共同业务入口拒绝。', '事件记录与状态更新在同一事务，避免当前状态变化却没有审计记录。'], 'ALLOWED = {\n    Status.OPEN: {Status.ASSIGNED},\n    Status.ASSIGNED: {Status.PROCESSING},\n    Status.PROCESSING: {Status.RESOLVED},\n    Status.RESOLVED: {Status.CLOSED, Status.PROCESSING},\n}\n\ndef transition(ticket, target):\n    if target not in ALLOWED[ticket.status]:\n        raise InvalidTransition(ticket.status, target)\n    ticket.status = target', ['映射描述所有合法出口。', 'transition 是唯一状态入口。', '非法目标抛明确异常。', '成功后服务保存状态和事件。'], '对应前端 reducer 状态机，但后端必须防止所有入口绕过。', ['为什么不用一串散落 if？', '状态表集中可审查、测试和扩展，避免不同接口规则漂移。']),
      chapter('先跑通创建纵向切片', '如何避免先写一周基础设施却没有可用功能？', ['创建切片从请求模型开始，经过路由、服务、数据库事务、响应模型和 API 测试。每层只实现这条链路需要的最少内容。', '完成后再以同样方式加入查询和指派。每次切片都可运行、可测试、可演示。'], 'POST /tickets\n  -> TicketCreate 校验\n  -> create_ticket 业务函数\n  -> INSERT tickets + INSERT events 同一事务\n  -> TicketRead 响应\n  -> API 测试', ['标题在请求边界清理和校验。', '服务创建默认 open 工单。', '工单与 created 事件共同提交。', '测试验证 201、字段和数据库。'], '类似前端垂直功能切片：从交互到状态和视图一起完成，而不是先搭所有抽象。', ['第一条切片完成的定义是什么？', '真实请求可获得正确响应，数据持久化，关键失败有测试。']),
    ],
    [['先建通用框架', '几天后仍没有用户能力。', '先完成创建工单端到端。'], ['状态直接 UPDATE', '绕过统一迁移规则。', '所有状态写入经过 transition。'], ['事件与实体分事务', '失败可产生不一致审计。', '同一业务动作共同提交。']],
    [['需求示例', '写三条真实工单示例和两条非法操作。', '用具体用户和状态。', '字段与规则来自示例。'], ['Schema', '建立 tickets 和 ticket_events 外键与约束。', '标题非空可由应用和 CHECK 协作。', '无工单不能插事件。'], ['纵向切片', '完成 POST /tickets 与一个 API 测试。', '先不做标签。', '201 响应和 created 事件都存在。']],
    [['domain model', '业务实体和规则的代码表达。'], ['state machine', '有限状态及允许迁移的模型。'], ['vertical slice', '贯穿所有层的最小用户能力。'], ['audit event', '记录业务动作的历史事件。'], ['YAGNI', '不为尚未需要的功能提前建设。']],
  ),
  27: lessonDetail(
    ['功能能演示只是第一阶段，可靠交付还要证明失败时数据安全、错误稳定、文档可复现。今天以风险清单补齐测试，而不是平均测试每行代码。', 'README 是项目面向下一位开发者的接口。所有命令必须在干净环境亲自验证。'],
    [['风险驱动测试', '优先覆盖影响大且容易发生的失败。'], ['故障注入', '主动制造依赖失败来验证系统反应。'], ['README', '说明安装、运行、测试和限制的项目入口文档。'], ['交付门槛', '项目可以被接受前必须满足的检查集合。']],
    [
      chapter('按业务风险设计测试', '哪些情况失败会造成最大损失？', ['优先级通常是权限越权、事务半提交、非法状态和重复操作，然后是普通校验。简单属性访问不必每个单独测试。', '单元测试覆盖 transition 等规则，API 测试覆盖 HTTP 契约，数据库测试覆盖约束和回滚。测试层次与风险位置对应。'], 'def test_cannot_resolve_open_ticket():\n    ticket = Ticket(status=Status.OPEN)\n    with pytest.raises(InvalidTransition):\n        transition(ticket, Status.RESOLVED)\n    assert ticket.status is Status.OPEN', ['建立明确初始状态。', '执行非法动作。', '断言异常类型。', '还要断言状态未被部分修改。'], '类似前端优先测试付款、权限和状态恢复，而非每个静态标签。', ['只断言抛异常为什么不够？', '实现可能先修改状态再抛错，还需验证数据未改变。']),
      chapter('故障注入验证恢复', '数据库第二次写入失败时会发生什么？', ['让测试故意触发 UNIQUE、CHECK 或模拟外部超时，验证事务回滚、响应错误和日志上下文。', '故障测试要可重复且局部，不通过随机 sleep 制造偶发失败。外部服务使用确定的测试替身。'], 'def test_event_failure_rolls_back_ticket(db):\n    with pytest.raises(sqlite3.IntegrityError):\n        create_ticket_with_invalid_event(db)\n    count = db.execute("SELECT COUNT(*) FROM tickets").fetchone()[0]\n    assert count == 0', ['制造事件约束失败。', '业务函数向上传递异常。', '事务回滚工单插入。', '查询确认没有半数据。'], '对应前端模拟网络失败，但服务端还需验证持久数据恢复。', ['用真实第三方 API 制造超时适合单元测试吗？', '不适合，不稳定且慢；用可控替身模拟超时。']),
      chapter('README 与可复现交付', '一个新人最少需要知道什么？', ['README 依次说明运行环境、创建 venv、安装依赖、配置变量、启动、访问文档、测试和 lint。命令从项目根目录可复制执行。', '列出已知限制，明确哪些未实现。不要用庞大架构介绍掩盖启动步骤。'], '# Ticket Service\n\n## Setup\npython3 -m venv .venv\nsource .venv/bin/activate\npip install -r requirements.txt\n\n## Run\nuvicorn app.main:app --reload\n\n## Check\nruff check .\npytest -q', ['环境步骤完整。', '启动命令明确模块路径。', '检查命令可复制。', '文档再补配置和示例。'], '对应前端项目 README 的 npm install、dev、test，但 Python 还需说明虚拟环境。', ['如何验证 README 没有隐含步骤？', '在新建干净目录或 venv 中逐条复制命令。']),
    ],
    [['追求覆盖率数字', '大量低价值测试掩盖关键风险。', '按权限、事务和状态优先。'], ['故障测试依赖随机时机', '测试偶发失败。', '使用确定替身和明确异常。'], ['README 只写项目介绍', '其他人无法运行。', '把可执行命令放在前面。']],
    [['风险排序', '列出十个可能失败场景，按影响和概率排序。', '先选前五个测试。', '测试资源集中关键风险。'], ['回滚演练', '让事件写入失败，确认实体和事件都无新数据。', '查询两个表。', '事务原子性有证据。'], ['干净复现', '新 venv 完整执行 README。', '记录并消除隐含步骤。', '十分钟内能启动。']],
    [['risk-based testing', '按失败风险分配测试资源。'], ['fault injection', '可控制造故障。'], ['rollback', '撤销事务中尚未提交的变化。'], ['reproducibility', '不同环境能按相同步骤得到相同结果。'], ['known limitation', '明确记录当前不支持或有边界的事项。']],
  ),
  28: lessonDetail(
    ['掌握不是“看完 28 天”，而是能在没有答案时解释、实现、排错和交付。最后一天通过演示、限时实现和代码评审验证闭环。', '下一阶段不需要同时学十个框架。根据工作中最可能出现的任务选择一个方向，继续用真实项目获得反馈。'],
    [['技术演示', '用有限时间解释问题、方案和证据。'], ['代码评审', '系统检查正确性、安全、可维护性和交付质量。'], ['限时实现', '在明确范围内独立完成小型业务能力。'], ['学习迁移', '把已掌握原则应用到新库和新问题。']],
    [
      chapter('十分钟演示讲什么', '如何证明项目可靠，而不是只证明能启动？', ['先讲用户问题和主流程，再展示数据模型、一次成功操作、一次失败保护、测试结果和已知限制。不要逐文件朗读。', '演示前准备固定数据和命令，失败演练选择非法状态或权限拒绝，直接展示数据没有被修改。'], '1. 用户问题和范围\n2. 核心模型\n3. 创建与状态流转\n4. 非法操作如何失败\n5. 测试与质量检查\n6. 已知限制', ['每部分有明确目的。', '成功路径展示业务价值。', '失败路径展示可靠性。', '限制展示边界意识。'], '对应前端项目演示，但后端特别需要展示数据一致性与接口契约。', ['为什么已知限制应该主动说明？', '它让使用者知道安全边界，也证明你能区分已完成与未来需求。']),
      chapter('代码评审的检查顺序', '面对陌生代码先看哪里？', ['先看数据安全和正确性：信任边界、权限、事务、异常。再看测试是否保护关键规则，最后看命名、重复和依赖。', '使用搜索找 except、SQL、密钥名称和状态写入点。工具检查通过只是入口，还要追踪真实调用流。'], 'ruff check .\npytest -q\nrg "except:|API_KEY|password|UPDATE|status" app tests', ['静态检查发现明显问题。', '测试验证已声明行为。', '搜索定位高风险代码。', '沿写入调用链检查规则和事务。'], '类似前端评审先看权限、数据流和副作用，再讨论格式。', ['测试全绿是否代表可以直接上线？', '不代表，还要检查未覆盖风险、配置、安全、数据迁移和部署环境。']),
      chapter('选择下一条学习路线', 'FastAPI 之后应该学什么？', ['常规后台可继续深入 FastAPI 或学习 Django；数据处理学习 SQL 和 pandas；后台任务学习队列；部署学习容器、进程与监控；高并发再深入 asyncio。', '选择依据按顺序是当前工作需求、综合项目真实缺口、个人兴趣。每次只选一个可在四到八周完成的主题，并留下项目产出。'], '如果下一份任务是后台管理系统：Django\n如果需要数据报表：SQL + pandas\n如果需要邮件与异步任务：任务队列\n如果需要上线：部署、日志、监控', ['先写未来三个月任务。', '选择一个最接近真实交付的方向。', '定义一个小项目和验收。', '每周用真实反馈调整。'], '前端经验不会浪费：HTTP、组件边界、测试和异步心智模型都能迁移，只需补齐服务端数据与安全。', ['要不要同时学 Django、Flask、爬虫和 AI？', '不要。先把一个方向用于真实项目，形成完整反馈后再扩展。']),
    ],
    [['只展示成功路径', '无法证明系统面对坏输入可靠。', '至少演示一个受保护失败。'], ['评审只讨论风格', '正确性和安全风险被忽略。', '按数据、安全、测试、设计顺序。'], ['同时开启多个学习方向', '每个都停在入门。', '围绕真实任务选择一个专项。']],
    [['模拟演示', '用计时器在十分钟内完成六部分演示。', '录屏回看停顿。', '陌生人能理解价值和边界。'], ['限时功能', '60 分钟为工单增加评论和三项测试。', '先写输入输出与失败。', '主链路、404、权限和空评论有证据。'], ['学习计划', '写三个月只包含一个专项的路线。', '每两周有可运行产出。', '目标能被真实任务验证。']],
    [['technical demo', '用证据说明系统解决什么和如何可靠。'], ['code review', '系统审查变更质量。'], ['time-box', '限制任务投入时间以控制范围。'], ['transfer learning', '把既有原理迁移到新技术。'], ['delivery', '让成果可安装、运行、验证和使用。']],
  ),
};
