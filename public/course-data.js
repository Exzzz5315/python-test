'use strict';

const makeQuiz = (question, options, answer, topic, explanation, correction) => ({
  question,
  options,
  answer,
  topic,
  explanation,
  correction,
});

const makeDay = ({ day, week, title, intro, goals, concepts, lab, quiz, tip }) => ({
  day,
  week,
  title,
  intro,
  goals,
  concepts,
  lab,
  quiz,
  tip,
});

window.COURSE = {
  weeks: [
    '语言基础与 Python 思维',
    '工程能力、数据与质量',
    'Web API 业务开发',
    '生产化与综合项目',
  ],
  schedule: [
    ['45m', '知识点与示例'],
    ['60m', '跟写和改写'],
    ['90m', '代码任务'],
    ['30m', '随堂测验'],
    ['15m', '复盘记录'],
  ],
  days: [
    makeDay({
      day: 1,
      week: 1,
      title: '环境、运行方式与第一段程序',
      intro: '先建立可靠的运行心智模型。你会知道 Python 在哪里运行、依赖放在哪里，以及脚本如何成为可复用模块。',
      goals: [
        '在项目虚拟环境中运行 Python 脚本和模块。',
        '理解解释器、REPL、模块和入口守卫。',
        '能从 traceback 中找到文件、行号和异常类型。',
      ],
      concepts: [
        {
          title: '解释器与虚拟环境',
          body: '解释器负责执行代码，虚拟环境为单个项目提供独立的解释器入口和依赖目录。它相当于把 Node 项目的依赖边界延伸到 Python 运行时。',
          code: 'source .venv/bin/activate\npython --version\npython -m pip list',
        },
        {
          title: '脚本与模块',
          body: 'python file.py 把文件当作入口脚本，python -m package.module 按模块路径执行。后者更适合包内导入。',
          code: 'python practice.py\npython -m order_tools.cli',
        },
        {
          title: '入口守卫',
          body: '模块被直接执行时 __name__ 等于 __main__，被其他模块导入时则是模块名。入口守卫让导入不会意外启动程序。',
          code: 'def main():\n    print("开始学习 Python")\n\nif __name__ == "__main__":\n    main()',
        },
      ],
      lab: {
        title: '命令行个人名片',
        brief: '接收姓名、前端经验和 Python 学习目标，校验空输入后输出一张格式化名片。',
        starter: 'def build_card(name: str, years: int, goal: str) -> str:\n    # 在这里返回格式化文本\n    ...\n\ndef main() -> None:\n    ...',
        checks: ['空姓名必须提示重新输入', '工作年限转换为整数', '业务逻辑放在 build_card 中', '直接运行和被导入时行为不同'],
      },
      quiz: [
        makeQuiz(
          '虚拟环境主要解决什么问题？',
          ['让 Python 自动变快', '隔离不同项目的依赖和解释器入口', '把 Python 编译成 JavaScript', '自动修复语法错误'],
          1,
          '虚拟环境',
          '虚拟环境把项目依赖与全局环境分开，避免版本互相影响。',
          '回到终端分别运行 which python 和 .venv/bin/python --version，观察解释器入口。',
        ),
        makeQuiz(
          '为什么常把 main() 放在入口守卫中？',
          ['让变量变成常量', '让函数自动异步执行', '避免模块被导入时自动运行入口逻辑', '让 pip 找到依赖'],
          2,
          '模块入口',
          '入口守卫只在文件被直接运行时调用 main()，导入模块时不会产生副作用。',
          '创建两个文件，让其中一个导入另一个，并打印两个文件里的 __name__。',
        ),
      ],
      tip: '今天不要安装更多库。先确认你能解释每条命令使用了哪个 Python。',
    }),
    makeDay({
      day: 2,
      week: 1,
      title: '变量、基础类型与字符串',
      intro: 'Python 的变量名绑定到对象，而不是固定类型的盒子。今天重点理解对象、值、转换和不可变性。',
      goals: ['熟练处理数字、布尔值、None 和字符串。', '使用 f-string 构建可读文本。', '在输入边界显式完成类型转换。'],
      concepts: [
        {
          title: '名称绑定',
          body: '赋值让名称指向对象。名称可以重新绑定到不同类型，但业务代码仍应保持类型稳定，方便阅读和检查。',
          code: 'count = 3\ncount = count + 1\nprint(type(count))',
        },
        {
          title: 'None 与相等性',
          body: 'None 表示缺失值。使用 is None 判断身份；普通值比较使用 ==。不要用 if not value 混淆 0、空字符串和缺失值。',
          code: 'discount = None\nif discount is None:\n    print("没有折扣")',
        },
        {
          title: '字符串处理',
          body: '字符串不可变。strip、replace 等方法返回新字符串。f-string 能直接嵌入表达式，但复杂逻辑应先算好再格式化。',
          code: 'name = "  Ada  ".strip()\namount = 1280\nprint(f"{name}: ¥{amount / 100:.2f}")',
        },
      ],
      lab: {
        title: '订单金额格式化',
        brief: '读取商品单价和数量，以整数分计算小计，并输出带两位小数的人民币金额。',
        starter: 'def parse_cents(value: str) -> int:\n    ...\n\ndef subtotal(unit_cents: int, quantity: int) -> int:\n    ...',
        checks: ['12.30 应转为 1230 分', '数量必须是正整数', '非法金额给出明确错误', '金额计算不依赖 float 累积'],
      },
      quiz: [
        makeQuiz(
          '判断变量 value 是否为 None，最合适的写法是？',
          ['value == False', 'not value', 'value is None', 'type(value) == null'],
          2,
          'None 判断',
          'None 是单例，Python 约定使用 is None 判断身份。',
          '分别让 value 等于 None、0 和空字符串，观察 not value 为什么无法区分它们。',
        ),
        makeQuiz(
          '执行 text.strip() 后，原字符串 text 会怎样？',
          ['原地删除空格', '返回新字符串，原字符串不变', '变成列表', '只删除中间空格'],
          1,
          '字符串不可变性',
          '字符串是不可变对象，方法会返回处理后的新字符串。',
          '把 text.strip() 的返回值分别打印和重新赋值，确认差异。',
        ),
      ],
      tip: '凡是来自 input、文件或网络的数据，都先当作不可信字符串处理。',
    }),
    makeDay({
      day: 3,
      week: 1,
      title: '分支、循环与模式匹配',
      intro: '控制流就是业务规则的骨架。目标不是少写行数，而是让每个条件的含义和优先级一眼可见。',
      goals: ['正确使用真值和短路求值。', '用 for、enumerate 和 zip 处理序列。', '为互斥业务状态选择清晰分支。'],
      concepts: [
        { title: '真值与短路', body: '空字符串、空容器、0 和 None 都是假值。and 与 or 会短路并返回操作数本身，不一定返回布尔值。', code: 'display_name = user_input.strip() or "匿名用户"' },
        { title: '遍历工具', body: 'enumerate 同时提供索引和值，zip 并行遍历多个序列。它们通常比手动维护下标更安全。', code: 'for index, item in enumerate(items, start=1):\n    print(index, item)' },
        { title: '模式匹配', body: 'match 适合根据稳定形状或离散状态分支。普通范围判断仍优先使用 if，因为它更直接。', code: 'match status:\n    case "paid":\n        ship()\n    case "cancelled":\n        refund()\n    case _:\n        wait()' },
      ],
      lab: {
        title: '会员优惠规则',
        brief: '根据会员等级、订单金额和优惠券状态计算折扣，并返回用户可读的折扣原因。',
        starter: 'def discount_rate(level: str, amount_cents: int, coupon: str | None) -> float:\n    ...',
        checks: ['先校验金额非负', '过期券不能生效', '不可叠加规则有明确优先级', '边界金额有测试'],
      },
      quiz: [
        makeQuiz('表达式 user_input.strip() or "匿名" 的结果是什么？', ['总是布尔值', '清理后非空则返回它，否则返回匿名', '只会返回原字符串', '空字符串时报错'], 1, '短路求值', 'or 返回第一个真值操作数，否则返回最后一个操作数。', '在 REPL 中分别用空字符串和非空字符串执行表达式。'),
        makeQuiz('for 循环的 else 块在什么时候执行？', ['每次循环后', '循环正常结束且没有 break 时', '只有循环体从未执行时', '发生异常时'], 1, '循环控制', 'for 的 else 在循环未被 break 提前终止时执行。', '写一个查找质数因子的短循环，分别让它 break 和正常结束。'),
      ],
      tip: '业务条件超过三层缩进时，先考虑提前返回或拆出有名字的判断函数。',
    }),
    makeDay({
      day: 4,
      week: 1,
      title: '函数、参数与作用域',
      intro: '函数是 Python 业务代码最重要的组织单元。今天会把输入、规则和输出分开，让测试变得自然。',
      goals: ['设计清楚的函数签名。', '区分位置参数和关键字参数。', '避免可变默认参数和隐藏副作用。'],
      concepts: [
        { title: '签名表达约束', body: '关键字参数能提升业务调用的可读性。仅关键字参数用星号分隔，适合容易混淆的布尔值和多个同类型参数。', code: 'def create_order(user_id: int, *, express: bool = False) -> dict:\n    ...\n\ncreate_order(42, express=True)' },
        { title: '默认值时机', body: '默认参数在函数定义时求值一次。列表或字典作为默认值会在调用之间共享，应使用 None 后在函数体创建。', code: 'def add_tag(tag: str, tags: list[str] | None = None):\n    tags = [] if tags is None else tags\n    tags.append(tag)\n    return tags' },
        { title: '纯函数', body: '纯函数只依赖参数并返回结果，不修改外部状态。价格、折扣和校验规则优先写成纯函数，测试成本最低。', code: 'def subtotal(price: int, quantity: int) -> int:\n    return price * quantity' },
      ],
      lab: {
        title: '可测试的结算流程',
        brief: '把订单结算拆成输入校验、小计、折扣、税费和展示格式化函数。',
        starter: 'def validate_order(price: int, quantity: int) -> None:\n    ...\n\ndef checkout(price: int, quantity: int, *, member: bool = False) -> int:\n    ...',
        checks: ['核心计算函数不读 input', '函数名表达业务含义', '不使用可变默认参数', '金额始终用整数分'],
      },
      quiz: [
        makeQuiz('为什么不应把 [] 直接作为参数默认值？', ['列表不能作为参数', '默认列表在调用之间共享', '列表会自动变成元组', '函数无法返回列表'], 1, '默认参数', '默认参数只在定义函数时创建一次，后续调用会复用同一个列表。', '连续调用两次带列表默认值的函数并打印 id，观察对象是否相同。'),
        makeQuiz('仅关键字参数最适合解决哪个问题？', ['提升数值计算速度', '让易混淆参数在调用处带名字', '让函数自动缓存', '阻止函数返回 None'], 1, '函数签名', '仅关键字参数强制调用方写出参数名，减少多个布尔值或数字的位置错误。', '设计一个含 dry_run 和 notify 的函数，并比较位置调用和关键字调用。'),
      ],
      tip: '如果函数名里出现“并且”，通常意味着它可能承担了两个职责。',
    }),
    makeDay({
      day: 5,
      week: 1,
      title: '列表、元组、字典与集合',
      intro: '容器选择决定数据处理代码是否自然。今天从业务约束出发选择序列、映射和集合。',
      goals: ['根据顺序、唯一性和查找需求选择容器。', '理解可变性、哈希性和浅拷贝。', '使用解包写出清晰的数据转换。'],
      concepts: [
        { title: '序列与映射', body: 'list 保持顺序且可变，tuple 常表示固定结构，dict 按键查找并保持插入顺序。业务实体更复杂时应考虑数据类。', code: 'sku, quantity = ("A-01", 3)\ncart = {sku: quantity}' },
        { title: '集合运算', body: 'set 适合成员判断、去重、交集和差集。它不表达展示顺序，不能依赖遍历顺序完成 UI 输出。', code: 'requested = {"read", "write"}\nallowed = {"read"}\nmissing = requested - allowed' },
        { title: '复制与别名', body: 'b = a 只增加一个指向同一对象的名称。a.copy() 是浅拷贝，嵌套对象仍然共享。', code: 'original = [[1], [2]]\ncopy = original.copy()\ncopy[0].append(9)\nprint(original)' },
      ],
      lab: {
        title: '合并订单行',
        brief: '合并相同 SKU 的数量，去重标签，同时保持 SKU 第一次出现的顺序。',
        starter: 'def merge_lines(lines: list[dict]) -> list[dict]:\n    totals: dict[str, int] = {}\n    ...',
        checks: ['数量小于等于 0 时拒绝', '不修改输入列表', '输出顺序稳定', '标签去重后仍有可预测顺序'],
      },
      quiz: [
        makeQuiz('哪个对象可以安全地作为字典键？', ['列表 [1, 2]', '字典 {"a": 1}', '元组 (1, 2)', '集合 {1, 2}'], 2, '哈希性', '由可哈希元素组成的元组可作为键，列表、字典和集合是可变对象，不能作为键。', '在 REPL 中分别创建以四种对象为键的字典并观察异常。'),
        makeQuiz('执行 b = a 后修改 b 内的列表，a 会怎样？', ['一定不变', 'a 与 b 指向同一对象，因此也会看到修改', 'a 自动深拷贝', '代码语法错误'], 1, '对象别名', '赋值不会复制对象，只是让两个名称指向同一对象。', '打印 id(a) 和 id(b)，再尝试 copy 与 deepcopy。'),
      ],
      tip: '先问数据是否需要顺序、是否允许重复、如何查找，再决定容器。',
    }),
    makeDay({
      day: 6,
      week: 1,
      title: '推导式、迭代器与生成器',
      intro: 'Python 的数据处理优势来自迭代协议。理解惰性求值后，你能处理远大于内存的数据流。',
      goals: ['读写简单推导式。', '区分可迭代对象、迭代器和生成器。', '用 yield 构建惰性数据流水线。'],
      concepts: [
        { title: '推导式', body: '推导式适合一层转换和一层筛选。包含复杂分支或多个副作用时，普通循环更容易读。', code: 'active_names = [u["name"] for u in users if u["active"]]' },
        { title: '迭代协议', body: '可迭代对象能产生迭代器，迭代器通过 next 逐个返回值并保存当前位置。for 循环替你调用 iter 和 next。', code: 'iterator = iter([10, 20])\nprint(next(iterator))\nprint(next(iterator))' },
        { title: '生成器', body: '含 yield 的函数返回生成器。每次 next 执行到下一个 yield，适合日志、分页和流式解析。', code: 'def error_lines(lines):\n    for line in lines:\n        if " 500 " in line:\n            yield line' },
      ],
      lab: {
        title: '流式日志汇总',
        brief: '逐行筛选 5xx 日志并统计路径，不把整个日志文件载入内存。',
        starter: 'def iter_errors(lines):\n    ...\n\ndef count_paths(lines) -> dict[str, int]:\n    ...',
        checks: ['输入可接受任意可迭代对象', '筛选函数使用 yield', '损坏行记录但不中断', '只遍历输入一次'],
      },
      quiz: [
        makeQuiz('生成器表达式与列表推导式最重要的差异是？', ['生成器惰性产出值', '生成器不能循环', '列表推导式没有条件', '生成器一定更快'], 0, '惰性求值', '生成器不会立即构造完整列表，而是在迭代时逐个计算。', '用 range(1000000) 比较列表推导式和生成器对象的内存表现。'),
        makeQuiz('一个迭代器被完全消费后，再次 for 会怎样？', ['自动从头开始', '通常不会产生任何值', '抛出语法错误', '复制出新迭代器'], 1, '迭代器状态', '迭代器保存消费位置，耗尽后不会自动重置。可迭代容器通常能再次产生新迭代器。', '分别对列表和 iter(list) 运行两轮 for，比较输出。'),
      ],
      tip: '不要为了看起来 Pythonic 写六层推导式。可读的循环往往更专业。',
    }),
    makeDay({
      day: 7,
      week: 1,
      title: '模块、包与第一周复盘',
      intro: '把能运行的脚本整理成能维护的包。今天会明确导入边界，并完成第一周阶段测试。',
      goals: ['创建可导入的 Python 包。', '理解绝对导入和 python -m。', '把副作用限制在入口模块。'],
      concepts: [
        { title: '包结构', body: '目录中的 __init__.py 让包边界清晰。公共 API 可以在其中显式导出，但不要把所有内部实现都暴露出去。', code: 'order_tools/\n    __init__.py\n    pricing.py\n    cli.py' },
        { title: '导入规则', body: '优先使用从顶层包开始的绝对导入。循环导入通常说明模块职责互相依赖，需要移动共享数据或重新划分边界。', code: 'from order_tools.pricing import subtotal' },
        { title: '模块执行', body: 'python -m order_tools.cli 从包上下文执行模块，包内绝对导入更稳定。', code: 'python -m order_tools.cli' },
      ],
      lab: {
        title: '整理 order_tools 包',
        brief: '把金额、折扣、校验和 CLI 拆到最少数量的模块，提供一个稳定公共入口。',
        starter: 'order_tools/\n    __init__.py\n    pricing.py\n    cli.py',
        checks: ['业务模块导入时不读取 input', '绝对导入可用', 'python -m order_tools.cli 可运行', '没有重复的金额计算'],
      },
      quiz: [
        makeQuiz('python -m order_tools.cli 的主要优势是？', ['自动安装依赖', '在包上下文中按模块路径执行', '跳过所有导入', '把代码编译成二进制'], 1, '模块执行', '按模块路径执行能正确建立包上下文，减少直接运行包内文件时的导入问题。', '分别尝试 python order_tools/cli.py 和 python -m order_tools.cli，观察导入路径。'),
        makeQuiz('出现循环导入时，最值得先检查什么？', ['是否需要升级 CPU', '模块职责是否相互缠绕', '是否应把所有内容放进 __init__.py', '是否应改用全局变量'], 1, '模块边界', '循环导入通常是职责边界不清的信号，先检查共同模型和依赖方向。', '画出两个模块的导入箭头，找出可移动的共享数据或纯函数。'),
      ],
      tip: '第一周验收：不看答案，从空文件写出一个带入口守卫的三函数程序。',
    }),
    makeDay({
      day: 8,
      week: 2,
      title: '类、实例与封装',
      intro: '类用于维护实体状态和不变量，不是函数的收纳箱。今天只在状态与行为紧密相关时使用它。',
      goals: ['理解实例属性和类属性。', '用属性和方法保护实体不变量。', '优先组合，避免不必要的继承。'],
      concepts: [
        { title: '实例状态', body: '实例属性属于具体对象，通常在 __init__ 中建立。类属性被所有实例共享，只适合真正的共享常量或行为。', code: 'class Cart:\n    currency = "CNY"\n\n    def __init__(self):\n        self.items = []' },
        { title: '维护不变量', body: '对象应拒绝无效状态。例如购物车添加方法应保证数量为正，而不是让每个调用者重复校验。', code: 'def add(self, sku: str, quantity: int) -> None:\n    if quantity <= 0:\n        raise ValueError("quantity must be positive")' },
        { title: '组合优先', body: '订单拥有地址、订单项和支付信息，通常是组合关系。只有稳定的“是一种”关系才考虑继承。', code: 'class Order:\n    def __init__(self, items: list[OrderItem]):\n        self.items = items' },
      ],
      lab: {
        title: '购物车实体',
        brief: '实现添加、移除、修改数量和计算总额，保证购物车中不存在零数量行。',
        starter: 'class Cart:\n    def __init__(self) -> None:\n        self._items: dict[str, int] = {}\n\n    def add(self, sku: str, quantity: int = 1) -> None:\n        ...',
        checks: ['数量必须为正', '删除不存在 SKU 有明确行为', '调用方无法直接破坏内部字典', '总额计算可单独测试'],
      },
      quiz: [
        makeQuiz('适合把逻辑写成类的最强信号是？', ['函数超过十行', '稳定状态与相关行为需要共同维护不变量', '项目文件太少', '希望代码看起来高级'], 1, '类的使用场景', '类的核心价值是把状态、行为和不变量放在同一边界内。', '把一个纯折扣函数和购物车实体比较，说明谁真正需要持续状态。'),
        makeQuiz('类属性被修改后，通常影响谁？', ['只有当前局部变量', '通过该类查找属性的所有实例', '解释器中的所有类', '只影响 __init__'], 1, '属性查找', '实例没有同名属性时会从类上查找，因此类属性是共享的。', '创建两个实例，修改类属性，再为其中一个实例设置同名属性观察遮蔽。'),
      ],
      tip: '如果一个类没有状态，只有一堆 staticmethod，先考虑普通模块函数。',
    }),
    makeDay({
      day: 9,
      week: 2,
      title: '数据类、枚举与 Python 数据模型',
      intro: '用数据类消除无意义样板，用枚举约束离散状态，再通过少量数据模型协议让对象自然工作。',
      goals: ['使用 dataclass 表达值对象。', '使用 Enum 代替散落的状态字符串。', '理解 repr、eq 和迭代等协议。'],
      concepts: [
        { title: '数据类', body: 'dataclass 自动生成初始化、显示和比较方法，适合字段明确的数据对象。field 可控制默认工厂和比较行为。', code: 'from dataclasses import dataclass, field\n\n@dataclass\nclass Order:\n    id: int\n    tags: list[str] = field(default_factory=list)' },
        { title: '枚举', body: 'Enum 把允许值集中管理，减少拼写错误。序列化到 JSON 时通常使用 member.value。', code: 'from enum import Enum\n\nclass Status(str, Enum):\n    OPEN = "open"\n    CLOSED = "closed"' },
        { title: '数据模型协议', body: 'Python 通过双下划线方法接入语言能力。只实现确实有自然语义的协议，不要为炫技添加魔法方法。', code: 'def __len__(self) -> int:\n    return len(self.items)' },
      ],
      lab: {
        title: '订单状态模型',
        brief: '使用数据类定义订单项和订单，用枚举定义状态，并实现受约束的状态迁移。',
        starter: '@dataclass(frozen=True)\nclass OrderItem:\n    sku: str\n    unit_cents: int\n    quantity: int\n\nclass OrderStatus(str, Enum):\n    ...',
        checks: ['订单项创建后不可变', '列表默认值不共享', '非法状态迁移抛出业务异常', 'repr 不包含敏感字段'],
      },
      quiz: [
        makeQuiz('数据类中列表字段的安全默认值写法是？', ['items: list = []', 'items = list', 'items: list = field(default_factory=list)', 'items: list = None 永远不处理'], 2, '数据类默认值', 'default_factory 会为每个实例创建新的列表。', '创建两个实例并比较 items 的 id，确认没有共享。'),
        makeQuiz('枚举相比散落字符串的主要收益是？', ['运行速度翻倍', '集中约束允许值并减少拼写错误', '自动创建数据库', '自动完成网络请求'], 1, '枚举', '枚举为有限状态提供单一来源，编辑器也能提示成员。', '故意拼错一个状态字符串，再改用枚举观察错误出现的位置。'),
      ],
      tip: '数据类适合数据，不要把复杂业务流程全部堆进一个巨大数据类。',
    }),
    makeDay({
      day: 10,
      week: 2,
      title: '异常、上下文管理器与日志',
      intro: '可靠业务系统需要明确失败。异常传递失败原因，上下文管理器保证清理，日志留下可诊断证据。',
      goals: ['设计有语义的异常层次。', '只捕获能处理的异常。', '使用 with 和 logging 管理资源与诊断信息。'],
      concepts: [
        { title: '异常边界', body: '底层异常保留技术原因，业务层可通过 raise NewError(...) from error 增加业务上下文。不要捕获后沉默。', code: 'try:\n    amount = int(raw)\nexcept ValueError as error:\n    raise InvalidAmount(raw) from error' },
        { title: '完整控制流', body: 'else 在没有异常时运行，finally 无论成功失败都运行。资源释放通常交给 with，比手写 finally 更可靠。', code: 'with path.open(encoding="utf-8") as file:\n    data = file.read()' },
        { title: '日志', body: '使用 logger 和参数化消息。记录事件、标识符与结果，不记录密码、密钥或完整隐私数据。', code: 'logger.info("order_created order_id=%s", order_id)' },
      ],
      lab: {
        title: '可靠订单导入',
        brief: '导入多条订单，单条校验失败写入错误报告，系统性读写失败则终止并保留异常链。',
        starter: 'class InvalidOrder(ValueError):\n    pass\n\ndef import_orders(path: Path) -> tuple[list[dict], list[str]]:\n    ...',
        checks: ['不使用裸 except', '错误包含记录索引', '文件由 with 关闭', '日志不重复打印同一异常'],
      },
      quiz: [
        makeQuiz('finally 块什么时候运行？', ['只在成功时', '只在异常时', '无论是否发生异常通常都会运行', '只在 return 之前跳过'], 2, '异常控制流', 'finally 用于无论结果如何都要执行的清理，但资源管理通常优先用 with。', '写一个带 return 和异常的函数，在 finally 中打印，观察执行顺序。'),
        makeQuiz('为什么不应使用 except: pass？', ['语法不允许', '它会隐藏包括编程错误在内的失败', '它会自动重试', '它只能捕获网络错误'], 1, '异常处理', '宽泛捕获并沉默会丢失诊断信息，让数据损坏或逻辑错误继续扩散。', '把 pass 改成捕获明确异常并记录上下文，比较可诊断性。'),
      ],
      tip: '只在你知道如何恢复、转换或补充上下文时捕获异常。',
    }),
    makeDay({
      day: 11,
      week: 2,
      title: '文件、路径与序列化',
      intro: '文件系统是业务开发常见边界。今天关注编码、损坏输入、原子替换和可移植路径。',
      goals: ['使用 pathlib 处理路径。', '安全读写 JSON 和 CSV。', '避免半写入文件破坏已有数据。'],
      concepts: [
        { title: 'Pathlib', body: 'Path 用对象表达路径拼接、检查和遍历，比手工字符串拼接更清晰且跨平台。', code: 'from pathlib import Path\nconfig_path = Path(__file__).parent / "config.json"' },
        { title: '编码与序列化', body: '文本文件显式指定 UTF-8。JSON 只支持有限类型，日期、Decimal 和枚举需要先转换为可表示值。', code: 'data = json.loads(path.read_text(encoding="utf-8"))' },
        { title: '安全写入', body: '重要文件先写同目录临时文件，刷新成功后再替换目标。这样进程中断时原文件仍可用。', code: 'temp.write_text(content, encoding="utf-8")\ntemp.replace(target)' },
      ],
      lab: {
        title: 'JSON 订单导入器',
        brief: '读取 UTF-8 JSON，逐条验证必填字段，输出有效订单和带行号的错误报告。',
        starter: 'def load_orders(path: Path) -> tuple[list[dict], list[dict]]:\n    ...',
        checks: ['文件不存在错误明确', '损坏 JSON 不覆盖输出', '未知字段处理策略明确', '结果报告使用原子替换'],
      },
      quiz: [
        makeQuiz('为什么文件读写应显式指定 encoding="utf-8"？', ['让文件更大', '避免依赖不同系统的默认编码', '让 JSON 自动加密', '让 Path 变成字符串'], 1, '文本编码', '系统默认编码可能不同，显式 UTF-8 能提升可移植性和可重复性。', '查看 locale 与 Python 默认编码，再解释跨机器风险。'),
        makeQuiz('原子替换写入的核心价值是？', ['提高网络速度', '避免写到一半破坏原文件', '自动压缩文件', '省略异常处理'], 1, '安全写入', '先完整写临时文件，再替换正式文件，可让读者看到旧完整版本或新完整版本。', '在写入中途故意抛错，确认原目标文件仍完整。'),
      ],
      tip: '路径来自用户输入时，先解析并限制允许目录，避免越界访问。',
    }),
    makeDay({
      day: 12,
      week: 2,
      title: '类型标注与结构化接口',
      intro: '类型标注服务于沟通和工具检查。目标是让公共边界更清楚，不是把 Python 写成另一种 Java。',
      goals: ['为函数和容器写准确类型。', '使用 TypedDict 和 Protocol 表达结构。', '识别 Any 和过度泛型的风险。'],
      concepts: [
        { title: '联合与容器', body: '现代 Python 使用 | 表达联合类型。None 只在缺失确实被允许时出现，不要为方便给所有返回值加 None。', code: 'def find_order(order_id: int) -> Order | None:\n    ...' },
        { title: 'TypedDict', body: 'TypedDict 描述仍是普通 dict 的固定字段结构，适合 JSON 边界。需要行为和不变量时改用数据类或模型。', code: 'class OrderPayload(TypedDict):\n    user_id: int\n    items: list[str]' },
        { title: 'Protocol', body: 'Protocol 关注对象能做什么，不要求显式继承。只有确实存在多个实现或需要测试替身时才值得引入。', code: 'class OrderStore(Protocol):\n    def get(self, order_id: int) -> Order | None: ...' },
      ],
      lab: {
        title: '订单包类型化',
        brief: '为公共函数补齐类型，定义 JSON 输入结构和最小仓储协议，清除无意的 Any。',
        starter: 'class OrderInput(TypedDict):\n    ...\n\nclass OrderStore(Protocol):\n    ...',
        checks: ['所有公共函数有返回类型', 'Optional 只用于真实缺失', '不为单一实现建立无用协议', '类型名表达业务含义'],
      },
      quiz: [
        makeQuiz('Order | None 表示什么？', ['Order 的子类', '值可能是 Order，也可能是 None', 'Order 会自动转换为字符串', '函数一定抛异常'], 1, '联合类型', '竖线表示联合类型，调用方必须处理两种可能。', '写一个 find 函数，并让编辑器提示 None 分支。'),
        makeQuiz('Protocol 最适合描述什么？', ['对象必须继承的唯一基类', '对象需要提供的行为结构', '数据库表结构', '运行时环境变量'], 1, '结构化类型', 'Protocol 根据方法和属性结构检查兼容性，不要求显式继承。', '写两个无继承关系但都有 get 方法的类，让它们满足同一协议。'),
      ],
      tip: '先给公共边界和核心领域模型加类型，内部局部变量通常能自动推断。',
    }),
    makeDay({
      day: 13,
      week: 2,
      title: 'pytest 与测试设计',
      intro: '测试的价值是快速指出哪条业务承诺被破坏。今天练习边界、异常、参数化和最小测试替身。',
      goals: ['使用 Arrange-Act-Assert 组织测试。', '用参数化覆盖业务矩阵。', '用 fixture 管理可复用测试上下文。'],
      concepts: [
        { title: '测试行为', body: '断言公共输出、状态和异常，而不是内部调用次数或私有变量。这样重构实现时测试仍然有效。', code: 'def test_member_gets_discount():\n    total = checkout(10_000, member=True)\n    assert total == 9_000' },
        { title: '参数化', body: '相同规则的多组输入用 parametrize 表达，测试名和 case id 应让失败原因易读。', code: '@pytest.mark.parametrize(("amount", "expected"), [(0, 0), (100, 90)])\ndef test_discount(amount, expected):\n    assert discount(amount) == expected' },
        { title: 'Fixture', body: 'fixture 管理测试所需资源和清理。范围越小越独立，默认函数级通常最安全。', code: '@pytest.fixture\ndef cart():\n    return Cart()' },
      ],
      lab: {
        title: '折扣规则测试矩阵',
        brief: '为折扣和订单状态迁移至少写 8 个场景，包含边界、非法输入和冲突规则。',
        starter: 'import pytest\n\n@pytest.mark.parametrize(...)\ndef test_discount(...):\n    ...',
        checks: ['每个测试可独立运行', '异常类型和消息均有关键断言', '参数化 case 名易懂', '不依赖执行顺序'],
      },
      quiz: [
        makeQuiz('好的单元测试最应该断言什么？', ['函数内部调用的每一步', '对外可观察的业务行为', '代码总行数', '私有变量命名'], 1, '测试边界', '测试公共行为能允许实现重构，同时保护业务承诺。', '把一个依赖私有方法的测试改为断言返回值或状态。'),
        makeQuiz('fixture 默认的作用域通常是？', ['session', 'module', 'class', 'function'], 3, 'Fixture 生命周期', 'pytest fixture 默认每个测试函数创建一次，有利于隔离。', '让 fixture 打印创建和清理，运行两个测试观察次数。'),
      ],
      tip: '先写最可能出错的业务分支，不追求漂亮但无意义的覆盖率数字。',
    }),
    makeDay({
      day: 14,
      week: 2,
      title: 'SQLite、SQL 与事务',
      intro: '数据库可靠性来自约束、参数化查询和清晰事务。今天直接使用标准库 sqlite3 理解底层行为。',
      goals: ['设计带约束的基础表结构。', '使用参数化 SQL 防止注入。', '用事务保证多步写入要么全成功，要么全失败。'],
      concepts: [
        { title: '约束', body: '主键、唯一、非空、检查和外键约束把不变量放在数据最终边界。应用校验提供友好错误，数据库约束防止漏网数据。', code: 'CREATE TABLE orders (\n  id INTEGER PRIMARY KEY,\n  number TEXT NOT NULL UNIQUE,\n  total_cents INTEGER NOT NULL CHECK(total_cents >= 0)\n)' },
        { title: '参数化 SQL', body: '参数通过占位符单独传递，数据库驱动负责转义和类型绑定。表名等结构部分不能作为普通参数，应使用固定白名单。', code: 'row = conn.execute(\n    "SELECT * FROM orders WHERE id = ?",\n    (order_id,),\n).fetchone()' },
        { title: '事务', body: '把一个业务动作涉及的所有写入放在同一事务中。异常时回滚，成功后提交。', code: 'with sqlite3.connect(path) as conn:\n    conn.execute("INSERT INTO orders ...")\n    conn.execute("INSERT INTO order_items ...")' },
      ],
      lab: {
        title: '订单数据库',
        brief: '创建订单和订单项表，实现新增、按 ID 查询、更新状态和游标式或偏移分页。',
        starter: 'def create_schema(conn: sqlite3.Connection) -> None:\n    ...\n\ndef create_order(conn, order: Order) -> int:\n    ...',
        checks: ['启用外键约束', '所有值使用参数绑定', '订单与订单项同事务写入', '重复订单号由唯一约束拒绝'],
      },
      quiz: [
        makeQuiz('防止 SQL 注入最直接的做法是？', ['手工替换引号', '使用参数化查询', '把 SQL 转成大写', '捕获所有异常'], 1, 'SQL 安全', '参数化查询把 SQL 结构和值分开，由驱动安全绑定参数。', '尝试用包含引号的订单号查询，比较拼接与参数绑定。'),
        makeQuiz('事务中第二条写入失败时，正确结果应是？', ['第一条保留，第二条忽略', '整个业务动作回滚', '自动重试无限次', '删除数据库文件'], 1, '事务原子性', '同一业务动作的写入应共同提交或共同回滚，避免半完成状态。', '故意违反第二条记录的约束，并查询确认第一条也未提交。'),
      ],
      tip: '第二周验收：关闭网络，只用标准库完成一条可靠的数据库写入和回滚测试。',
    }),
    makeDay({
      day: 15,
      week: 3,
      title: 'HTTP 与 REST 基础',
      intro: '把前端熟悉的 HTTP 换到服务端视角，明确每个请求如何被校验、处理、持久化并转换成响应。',
      goals: ['为业务资源设计稳定 URL 和方法。', '正确选择常见状态码。', '理解幂等性、分页、缓存和 CORS。'],
      concepts: [
        { title: '资源与方法', body: 'URL 表达资源，HTTP 方法表达动作。POST 常用于创建，GET 读取，PATCH 局部更新，DELETE 删除。复杂业务动作可使用明确子资源。', code: 'POST /orders\nGET /orders/42\nPATCH /orders/42\nPOST /orders/42/cancellation' },
        { title: '状态码', body: '200 表示成功读取或更新，201 表示创建成功，204 表示无响应体，400 是通用坏请求，401 未认证，403 无权限，404 不存在，409 状态冲突。', code: 'HTTP/1.1 201 Created\nLocation: /orders/42' },
        { title: '接口契约', body: '稳定错误结构、分页参数和响应字段比追求纯粹 REST 更重要。先让客户端能可靠处理，再讨论风格。', code: '{\n  "code": "ORDER_NOT_FOUND",\n  "message": "订单不存在",\n  "details": {"order_id": 42}\n}' },
      ],
      lab: {
        title: '设计订单 API 契约',
        brief: '为创建、详情、列表、取消和发货写出路径、方法、请求体、响应体和错误状态。',
        starter: '# 用 Markdown 或 JSON 写接口契约\nPOST /orders\nRequest: ...\nResponse 201: ...\nErrors: ...',
        checks: ['资源使用名词复数', '创建返回 201', '非法状态冲突使用 409', '分页有默认值和最大值'],
      },
      quiz: [
        makeQuiz('认证信息缺失或无效时，通常返回哪个状态码？', ['400', '401', '403', '409'], 1, 'HTTP 状态码', '401 表示尚未通过认证，403 表示身份已知但没有权限。', '写出“没带密钥”和“密钥有效但不是管理员”两个响应。'),
        makeQuiz('PATCH 与 PUT 的常见区别是？', ['PATCH 只能读取', 'PUT 通常替换完整资源，PATCH 表达局部修改', 'PUT 不能有请求体', '两者永远完全相同'], 1, 'HTTP 方法', 'PUT 通常要求目标资源的完整表示并具有幂等语义，PATCH 表达部分变化。', '为更新订单备注分别设计 PUT 和 PATCH 请求体。'),
      ],
      tip: '先设计失败响应。客户端通常比成功路径更依赖稳定错误契约。',
    }),
    makeDay({
      day: 16,
      week: 3,
      title: 'FastAPI 路由与 Pydantic 模型',
      intro: '开始搭建真实 API。FastAPI 用类型标注生成校验和文档，但业务规则仍需要你明确实现。',
      goals: ['创建 FastAPI 应用和路由。', '定义请求与响应模型。', '通过 /docs 验证 OpenAPI 契约。'],
      concepts: [
        { title: '路由', body: '装饰器把路径和方法绑定到函数。路径参数来自 URL，查询参数常用于筛选和分页。', code: 'from fastapi import FastAPI\n\napp = FastAPI()\n\n@app.get("/orders/{order_id}")\ndef get_order(order_id: int):\n    ...' },
        { title: 'Pydantic 模型', body: 'BaseModel 在请求边界解析和校验数据。Field 表达长度和数值约束，跨字段业务规则仍应在服务层处理。', code: 'class OrderCreate(BaseModel):\n    user_id: int = Field(gt=0)\n    note: str = Field(default="", max_length=200)' },
        { title: '响应模型', body: '响应模型控制公开字段并校验输出，避免把数据库行、密钥或内部状态直接返回。', code: '@app.post("/orders", response_model=OrderRead, status_code=201)\ndef create_order(payload: OrderCreate):\n    ...' },
      ],
      lab: {
        title: '第一个订单 API',
        brief: '实现健康检查、创建订单、查询订单和分页列表，并在 /docs 中手动调用。',
        starter: 'from fastapi import FastAPI\nfrom pydantic import BaseModel, Field\n\napp = FastAPI(title="Order API")\n\n@app.get("/health")\ndef health():\n    return {"ok": True}',
        checks: ['创建成功返回 201', '路径参数自动转为 int', '列表分页有上限', '响应模型不暴露内部字段'],
      },
      quiz: [
        makeQuiz('FastAPI 请求模型的主要职责是？', ['直接创建数据库表', '解析并校验外部输入', '替代所有业务规则', '自动完成权限检查'], 1, '请求校验', 'Pydantic 模型负责输入形状和字段约束，业务规则应在领域或服务层。', '给数量字段添加 gt=0，并在 /docs 中提交 0 观察响应。'),
        makeQuiz('为什么创建接口应单独定义响应模型？', ['让响应变慢', '控制公开字段并验证输出契约', '省略状态码', '让数据库自动连接'], 1, '响应边界', '响应模型能过滤内部字段并保证客户端看到稳定结构。', '在内部对象加入 secret 字段，确认响应模型不会返回它。'),
      ],
      tip: '路由函数越薄越好，但不需要为了薄而提前创建十层目录。',
    }),
    makeDay({
      day: 17,
      week: 3,
      title: '依赖注入、校验与统一错误',
      intro: '数据库连接、配置和权限属于请求横切能力。FastAPI 依赖系统可以复用它们，并保证清理顺序。',
      goals: ['使用 Depends 提供请求资源。', '区分字段校验和业务校验。', '把业务异常映射为稳定 HTTP 错误。'],
      concepts: [
        { title: '依赖生命周期', body: '带 yield 的依赖可在请求结束后清理资源。FastAPI 默认在同一请求中缓存同一依赖结果。', code: 'def get_db():\n    conn = sqlite3.connect(DB_PATH)\n    try:\n        yield conn\n    finally:\n        conn.close()' },
        { title: '校验层次', body: '字段范围和格式在 Pydantic 中处理，依赖数据库或多个实体的业务规则在服务层处理。', code: 'if repository.exists_number(payload.number):\n    raise DuplicateOrder(payload.number)' },
        { title: '统一异常', body: '业务代码抛领域异常，Web 边界统一转换为状态码和错误结构，避免每个路由复制 try/except。', code: '@app.exception_handler(OrderNotFound)\nasync def order_not_found(_, error):\n    return JSONResponse(status_code=404, content={"code": "ORDER_NOT_FOUND", "message": str(error)})' },
      ],
      lab: {
        title: '统一请求边界',
        brief: '封装数据库连接依赖，建立 OrderNotFound、DuplicateOrder 和 InvalidTransition 的异常映射。',
        starter: 'def get_db():\n    ...\n\n@app.exception_handler(OrderNotFound)\nasync def handle_not_found(request, error):\n    ...',
        checks: ['连接每个请求关闭', '业务代码不依赖 HTTPException', '错误 code 稳定', '意外异常不泄露堆栈给客户端'],
      },
      quiz: [
        makeQuiz('依赖函数使用 yield 的典型目的是什么？', ['生成多个 HTTP 响应', '在请求后执行资源清理', '跳过类型校验', '让函数变成线程'], 1, '依赖生命周期', 'yield 前建立资源，yield 后在请求结束阶段清理。', '让依赖打印 open 和 close，再访问接口观察顺序。'),
        makeQuiz('“订单已发货，不能取消”应在哪一层校验？', ['HTML 层', '服务或领域业务层', '只在数据库字段类型中', '仅靠 Pydantic 字符串长度'], 1, '业务校验', '该规则依赖订单当前状态，是业务不变量，应由服务或领域层统一维护。', '绕过 HTTP 直接调用 service.cancel，确认规则仍生效。'),
      ],
      tip: '让领域异常不知道 HTTP，让 Web 层只负责翻译。',
    }),
    makeDay({
      day: 18,
      week: 3,
      title: 'CRUD 分层与业务规则',
      intro: '分层的目的不是增加文件，而是让变化原因分开。今天用最小 router、service、repository 边界完成业务流程。',
      goals: ['建立最小且真实的职责边界。', '让业务规则脱离 HTTP 可测试。', '在服务层明确事务边界。'],
      concepts: [
        { title: 'Router', body: 'Router 负责 HTTP 输入输出、依赖和状态码。不要在其中拼 SQL 或实现复杂状态判断。', code: '@router.post("/orders", response_model=OrderRead)\ndef create(payload: OrderCreate, service: OrderService = Depends(get_service)):\n    return service.create(payload)' },
        { title: 'Service', body: 'Service 编排业务规则和事务，是用例入口。一个简单 CRUD 不一定需要类，模块函数也可以。', code: 'def cancel_order(conn, order_id: int):\n    order = get_order(conn, order_id)\n    order.cancel()\n    save_order(conn, order)' },
        { title: 'Repository', body: 'Repository 隔离持久化语句。只有当数据库细节确实干扰业务测试或存在多个查询时再引入。', code: 'def find_order(conn, order_id: int) -> Order | None:\n    row = conn.execute("SELECT ... WHERE id = ?", (order_id,)).fetchone()\n    return to_order(row) if row else None' },
      ],
      lab: {
        title: '订单完整流程',
        brief: '完成创建、列表、详情、取消和发货，让状态规则可脱离 HTTP 单测。',
        starter: 'def cancel_order(conn, order_id: int) -> Order:\n    ...\n\ndef ship_order(conn, order_id: int) -> Order:\n    ...',
        checks: ['路由不含 SQL', '状态规则只有一个实现', '一个业务动作一个事务', '简单逻辑没有额外接口层'],
      },
      quiz: [
        makeQuiz('事务边界通常应围绕什么建立？', ['单条 SQL 字符串', '一个完整业务动作', '每个 Python 文件', '每个响应字段'], 1, '事务边界', '事务应保证一个业务用例涉及的所有写入共同成功或失败。', '列出“创建订单”涉及的表，并画出同一事务范围。'),
        makeQuiz('什么时候值得增加 repository？', ['所有项目第一天', '持久化细节已干扰业务逻辑或测试', '只有一个常量时', '为了让目录更对称'], 1, '分层设计', 'repository 应解决真实的持久化复杂度，不是必须套用的仪式。', '先用两个普通查询函数完成任务，再判断是否存在需要封装的共同变化。'),
      ],
      tip: '最小边界先跑通，只有出现第二个变化原因时再拆。',
    }),
    makeDay({
      day: 19,
      week: 3,
      title: '认证、授权与安全基础',
      intro: '安全从信任边界开始。今天关注密钥、身份、权限、输入和日志，而不是自己发明加密算法。',
      goals: ['区分认证与授权。', '安全处理密码和 API Key。', '识别注入、越权和敏感信息泄露。'],
      concepts: [
        { title: '认证与授权', body: '认证回答“你是谁”，授权回答“你能做什么”。身份有效不代表可以执行所有操作。', code: 'def require_admin(user = Depends(current_user)):\n    if "admin" not in user.roles:\n        raise HTTPException(status_code=403)' },
        { title: '机密与哈希', body: '密码使用专用慢哈希算法，API Key 存环境变量并进行恒定时间比较。哈希不可逆，加密可逆，场景不同。', code: 'import hmac\n\nif not hmac.compare_digest(provided, expected):\n    raise InvalidCredential()' },
        { title: '最小暴露', body: '请求校验、参数化 SQL、最小响应字段和脱敏日志共同缩小攻击面。客户端校验不能替代服务端校验。', code: 'logger.info("login_failed user_id=%s", user_id)' },
      ],
      lab: {
        title: '管理 API Key',
        brief: '为发货和批量导出接口增加 API Key 依赖，密钥从环境变量读取。',
        starter: 'def require_api_key(x_api_key: str = Header()):\n    expected = os.environ["ADMIN_API_KEY"]\n    ...',
        checks: ['密钥不写源码', '比较使用 hmac.compare_digest', '日志不记录密钥', '无密钥 401，无权限 403'],
      },
      quiz: [
        makeQuiz('用户已登录但没有管理员权限，应返回？', ['200', '401', '403', '404 永远更安全'], 2, '授权', '身份已经确认但权限不足时通常返回 403。', '分别为未认证和权限不足写两个测试。'),
        makeQuiz('密码哈希与加密的核心区别是？', ['哈希通常是单向验证，加密可以解密', '哈希一定更短', '加密不能使用密钥', '两者完全相同'], 0, '密码安全', '密码验证需要抗暴力破解的单向哈希，不应存可解密明文。', '查阅 scrypt、bcrypt 或 Argon2 的用途，写一句选择理由。'),
      ],
      tip: '任何来自浏览器的权限声明都不可信，服务端必须根据自身数据判断。',
    }),
    makeDay({
      day: 20,
      week: 3,
      title: '异步、并发与超时',
      intro: 'asyncio 适合等待网络和其他 I/O。它不是自动加速器，阻塞调用放进 async def 仍然会卡住事件循环。',
      goals: ['理解 coroutine、Task 和事件循环。', '并发执行独立 I/O。', '正确处理超时、取消和清理。'],
      concepts: [
        { title: 'Coroutine', body: '调用 async def 返回 coroutine 对象，只有 await、创建 Task 或运行事件循环后才真正推进执行。', code: 'async def fetch():\n    return 42\n\nresult = await fetch()' },
        { title: '结构化并发', body: 'TaskGroup 把相关任务生命周期放在同一作用域，任一失败时能一致取消和收集。', code: 'async with asyncio.TaskGroup() as group:\n    stock = group.create_task(fetch_stock())\n    price = group.create_task(fetch_price())' },
        { title: '超时与阻塞', body: '外部调用必须设置超时。同步数据库、time.sleep 或 CPU 重任务不能直接阻塞事件循环，应使用同步路由、线程或独立工作进程。', code: 'async with asyncio.timeout(0.8):\n    return await fetch_inventory()' },
      ],
      lab: {
        title: '聚合库存和价格',
        brief: '并发请求库存与推荐价格，总超时 800ms，价格失败可降级，库存失败直接返回错误。',
        starter: 'async def product_snapshot(sku: str) -> dict:\n    async with asyncio.TaskGroup() as group:\n        ...',
        checks: ['独立 I/O 并发启动', '明确总超时', '降级策略只用于可选数据', '取消时不遗留任务'],
      },
      quiz: [
        makeQuiz('调用 async def 函数后立刻得到什么？', ['最终结果', 'coroutine 对象', '新线程', 'HTTP 响应'], 1, 'Coroutine', '异步函数调用先创建 coroutine，需要 await 或调度后才执行。', '在 REPL 中打印 fetch() 的类型，再 await 它。'),
        makeQuiz('在 async def 中直接调用 time.sleep(2) 会怎样？', ['只暂停当前协程且不影响其他任务', '阻塞事件循环线程', '自动改成 asyncio.sleep', '启动新进程'], 1, '阻塞边界', 'time.sleep 是同步阻塞调用，会让同一事件循环上的其他任务无法推进。', '并发运行一个计时器，比较 time.sleep 与 asyncio.sleep。'),
      ],
      tip: '异步代码没有超时和取消策略，就还没有完成。',
    }),
    makeDay({
      day: 21,
      week: 3,
      title: 'API 测试与第三周复盘',
      intro: 'API 测试验证从请求校验到数据库响应的完整链路。今天让成功和失败路径都可重复运行。',
      goals: ['使用 TestClient 或 HTTPX 调用应用。', '覆盖数据库依赖和测试配置。', '验证状态码、响应结构和持久化结果。'],
      concepts: [
        { title: '请求链路', body: 'API 测试从 HTTP 输入出发，穿过路由、服务和数据库，适合保护契约。核心规则仍保留更快的单元测试。', code: 'def test_create_order(client):\n    response = client.post("/orders", json={"user_id": 1})\n    assert response.status_code == 201' },
        { title: '依赖覆盖', body: '测试使用临时数据库和确定配置，不访问开发数据或真实外部服务。FastAPI 可通过 dependency_overrides 替换依赖。', code: 'app.dependency_overrides[get_db] = get_test_db' },
        { title: '失败路径', body: '至少验证输入非法、资源不存在、状态冲突和权限不足。只测 200 会留下最危险的边界空白。', code: 'assert response.json()["code"] == "INVALID_TRANSITION"' },
      ],
      lab: {
        title: '订单 API 测试套件',
        brief: '编写创建、重复订单、非法状态、404、权限和分页测试，并使用临时 SQLite。',
        starter: '@pytest.fixture\ndef client(tmp_path):\n    ...\n\ndef test_create_order(client):\n    ...',
        checks: ['每个测试获得干净数据库', '不请求真实网络', '断言业务错误 code', '测试可任意顺序运行'],
      },
      quiz: [
        makeQuiz('API 测试为什么应使用临时数据库？', ['让 SQL 语法失效', '隔离测试并避免污染开发数据', '跳过事务', '提高生产数据库速度'], 1, '测试隔离', '临时数据库让每次运行结果确定，并保护真实数据。', '连续运行同一测试两次，确认结果不受上次数据影响。'),
        makeQuiz('下列哪个失败路径最应该有 API 测试？', ['CSS 颜色变化', '非法状态迁移返回稳定 409 错误', '编辑器主题', '函数注释长度'], 1, '失败契约', '状态冲突是客户端必须可靠处理的业务契约。', '为已发货订单取消请求写状态码和错误 code 断言。'),
      ],
      tip: '第三周验收：从空目录在 90 分钟内交付一个有 6 个测试的 Todo API。',
    }),
    makeDay({
      day: 22,
      week: 4,
      title: '配置、环境变量与可观察性',
      intro: '生产问题首先需要被看见。今天让配置可验证，让一次请求可以通过请求 ID 和结构化日志被追踪。',
      goals: ['从环境变量加载和验证配置。', '记录结构化且脱敏的业务日志。', '设计存活与就绪健康检查。'],
      concepts: [
        { title: '配置边界', body: '配置在应用启动时集中读取并验证。关键值缺失应快速失败，不要在业务代码深处才发现。', code: 'database_path = os.environ.get("DATABASE_PATH", "data/app.db")\napi_key = os.environ["ADMIN_API_KEY"]' },
        { title: '请求关联', body: '每个请求获得 request_id，所有相关日志带同一标识。这样可以跨路由、服务和外部调用还原链路。', code: 'logger.info("order_created request_id=%s order_id=%s", request_id, order_id)' },
        { title: '健康检查', body: '存活检查只说明进程在运行，就绪检查确认数据库等关键依赖可用。不要在健康检查里执行昂贵业务。', code: '@app.get("/ready")\ndef ready(db = Depends(get_db)):\n    db.execute("SELECT 1")\n    return {"ready": True}' },
      ],
      lab: {
        title: '可诊断订单服务',
        brief: '增加开发和测试配置、请求 ID、中间件日志以及 /health 和 /ready。',
        starter: 'class Settings:\n    def __init__(self):\n        self.database_path = os.environ.get("DATABASE_PATH", "data/app.db")\n\n@app.middleware("http")\nasync def request_context(request, call_next):\n    ...',
        checks: ['启动时验证关键配置', '日志包含请求 ID', '不记录 API Key', 'ready 能检测数据库连接'],
      },
      quiz: [
        makeQuiz('关键环境变量缺失时，最合适的行为是？', ['默默使用空字符串', '应用启动时快速失败并说明缺失项', '第一次订单请求时随机处理', '把值写入日志后继续'], 1, '配置校验', '启动时失败能让部署问题立刻暴露，避免应用带错误配置接收流量。', '移除 ADMIN_API_KEY 并确认启动错误包含变量名但不包含任何密钥值。'),
        makeQuiz('哪项信息不应写入普通应用日志？', ['request_id', 'order_id', '完整 API Key', 'HTTP 状态码'], 2, '日志安全', '密钥一旦进入集中日志会扩大泄露范围，应只记录是否存在或脱敏指纹。', '检查现有日志格式，列出必须脱敏的字段。'),
      ],
      tip: '日志回答发生了什么，不要让日志本身成为新的数据泄露源。',
    }),
    makeDay({
      day: 23,
      week: 4,
      title: '项目结构与可维护边界',
      intro: '成熟项目不靠目录数量，而靠每条规则有明确归属。今天围绕变化原因重构，并删除重复。',
      goals: ['识别高内聚模块和单向依赖。', '消除重复业务规则。', '处理循环导入和模糊公共 API。'],
      concepts: [
        { title: '按变化组织', body: '总在同一需求中一起修改的代码应靠近。因不同原因变化的 HTTP、业务规则和 SQL 可以分开。', code: 'app/\n    main.py        # HTTP 装配\n    orders.py      # 订单用例与规则\n    database.py    # SQLite 操作' },
        { title: '依赖方向', body: '外层 Web 依赖核心业务，核心业务不应导入 FastAPI。数据库细节通过参数或少量函数传入。', code: 'HTTP -> service -> database\n            |\n          models' },
        { title: '删除重复', body: '同一状态规则在路由和服务各一份会漂移。把不变量放在所有调用路径都会经过的共享入口。', code: 'def cancel_order(order: Order) -> None:\n    if order.status is not Status.OPEN:\n        raise InvalidTransition()\n    order.status = Status.CANCELLED' },
      ],
      lab: {
        title: '单一业务入口重构',
        brief: '找出重复状态校验和 SQL，把取消订单收敛到一个服务入口，并保持现有测试通过。',
        starter: '# 先用 rg 搜索所有 cancel、status 和 UPDATE 调用点\n# 再决定唯一入口放在哪里',
        checks: ['所有调用者都经过同一规则', '核心业务不导入 FastAPI', '没有为单实现增加工厂', '重构前后测试结果一致'],
      },
      quiz: [
        makeQuiz('同一业务校验在三个路由重复时，最小可靠修复是？', ['继续复制并加注释', '收敛到三个路由共同调用的业务函数', '为每个路由建一个基类', '删除所有校验'], 1, '单一规则来源', '共享业务入口能一次修复所有调用路径，并避免规则漂移。', '用 rg 找到所有调用者，确认没有旁路。'),
        makeQuiz('核心订单模块为什么不应导入 FastAPI？', ['FastAPI 不能在 Python 中导入', '业务规则应可脱离传输层使用和测试', '会让数据库自动删除', '类型标注会失效'], 1, '依赖方向', '领域规则不依赖 HTTP 框架，更容易测试、复用和替换边界。', '从纯 Python 测试直接调用取消订单，确保无需创建 Request。'),
      ],
      tip: '重构先画真实调用流，再动代码。最小改动必须落在共同根因上。',
    }),
    makeDay({
      day: 24,
      week: 4,
      title: '性能、缓存与排障',
      intro: '性能优化从证据开始。今天建立基准，定位时间花在哪里，只修真实瓶颈。',
      goals: ['使用 timeit 和 cProfile 测量。', '识别低效查询、缺失索引和重复 I/O。', '理解缓存收益与失效成本。'],
      concepts: [
        { title: '先测量', body: '先固定输入和环境记录基线，再分析函数或查询耗时。没有基线就无法判断改动是否有效。', code: 'python -m timeit -s "from app import work" "work()"\npython -m cProfile -s cumulative script.py' },
        { title: '数据库性能', body: '索引加速匹配和排序，但会增加写入和存储成本。用 EXPLAIN QUERY PLAN 确认查询是否使用索引。', code: 'EXPLAIN QUERY PLAN\nSELECT * FROM orders WHERE user_id = ? ORDER BY created_at DESC' },
        { title: '缓存边界', body: '缓存适合重复、昂贵且允许短暂旧值的读取。必须先定义键、有效期、容量和失效条件。', code: 'from functools import lru_cache\n\n@lru_cache(maxsize=128)\ndef country_name(code: str) -> str:\n    ...' },
      ],
      lab: {
        title: '优化订单列表',
        brief: '构造足够数据，测量列表接口，定位最慢步骤并给出优化前后结果。',
        starter: 'def benchmark(client, repeats: int = 30) -> float:\n    ...\n\n# 使用 EXPLAIN QUERY PLAN 检查查询',
        checks: ['固定相同测试数据', '记录优化前基线', '只改真实瓶颈', '验证结果内容未改变'],
      },
      quiz: [
        makeQuiz('开始性能优化的第一步应是？', ['加入 Redis', '建立可重复基线并测量', '把所有函数改成 async', '删除测试'], 1, '性能测量', '先测量才能找到真正瓶颈并判断优化是否有效。', '为目标接口固定数据规模并记录至少 20 次耗时。'),
        makeQuiz('数据库索引的主要代价是什么？', ['查询永远变慢', '额外存储和写入维护成本', '无法使用事务', '会关闭外键'], 1, '数据库索引', '索引需要额外空间，每次相关写入都要维护。', '为筛选列加索引后比较读取和批量插入耗时。'),
      ],
      tip: '只有性能数据证明不够时才引入缓存，因为失效比读取更难。',
    }),
    makeDay({
      day: 25,
      week: 4,
      title: '依赖、打包与自动化检查',
      intro: '可交付项目必须能在新环境重复安装、运行和检查。今天建立一条最短质量流水线。',
      goals: ['管理直接依赖和版本范围。', '使用 Ruff 统一格式与静态检查。', '提供一次运行的本地质量命令。'],
      concepts: [
        { title: '依赖声明', body: '只声明项目直接使用的依赖。应用交付通常需要可重复版本，库则使用合理兼容范围。', code: 'fastapi>=0.115,<1\nuvicorn>=0.34,<1\npytest>=8,<9' },
        { title: 'Ruff', body: 'Ruff 可完成快速 lint 和格式化。先使用默认规则，遇到真实团队需要再增加规则，不复制庞大配置。', code: 'ruff check .\nruff format --check .' },
        { title: '检查顺序', body: '先运行快速静态检查，再运行单元和集成测试。失败时尽早停止，输出清楚命令。', code: 'ruff check . && ruff format --check . && pytest -q' },
      ],
      lab: {
        title: '一键质量检查',
        brief: '整理依赖和 README，添加一个 check 脚本，确保新虚拟环境可复现安装。',
        starter: '# scripts/check.sh\n#!/bin/sh\nset -e\nruff check .\nruff format --check .\npytest -q',
        checks: ['只保留直接依赖', '检查命令失败即退出', 'README 写明安装启动', '新环境从零验证一次'],
      },
      quiz: [
        makeQuiz('requirements 中最应该声明哪些包？', ['整个系统所有已安装包', '项目直接依赖并带合理版本约束', '只写 Python 版本', '所有标准库模块'], 1, '依赖管理', '直接依赖是项目明确需要的包，间接依赖由包管理器解析。', '在代码中 rg import，和 requirements 对照查漏补缺。'),
        makeQuiz('本地检查为何通常先运行 Ruff？', ['它能替代所有测试', '它快，能尽早发现明显问题', '它会自动部署', '它必须访问生产数据库'], 1, '质量流水线', '快速检查先失败能缩短反馈时间，但不能替代行为测试。', '故意加入未使用导入，观察 Ruff 在 pytest 前发现问题。'),
      ],
      tip: '自动化命令越短越容易每天执行，先保证它可靠。',
    }),
    makeDay({
      day: 26,
      week: 4,
      title: '综合项目一：需求与核心实现',
      intro: '开始轻量工单系统。今天只完成一条端到端主链路，再围绕真实需求扩展。',
      goals: ['把需求转换为数据模型和用例。', '完成 SQLite schema 与核心服务。', '跑通创建和查询端到端链路。'],
      concepts: [
        { title: '领域模型', body: '工单至少包含标题、状态、创建人、处理人和时间。标签和操作记录是独立关系，先按查询需求建模。', code: 'Ticket(id, title, status, creator_id, assignee_id, created_at)\nTicketEvent(id, ticket_id, action, actor_id, created_at)' },
        { title: '状态不变量', body: '创建后默认 open，只有已指派工单可进入 processing，只有 processing 可 resolved。所有入口共用同一迁移函数。', code: 'ALLOWED = {\n    Status.OPEN: {Status.ASSIGNED},\n    Status.ASSIGNED: {Status.PROCESSING},\n    Status.PROCESSING: {Status.RESOLVED},\n}' },
        { title: '纵向切片', body: '先完成模型、写入、服务、路由和测试的一条细切片，比先写完所有数据库代码更早暴露集成问题。', code: 'POST /tickets -> service.create -> INSERT -> response -> API test' },
      ],
      lab: {
        title: '工单创建与查询',
        brief: '设计表结构，完成创建工单、工单详情和基础列表，记录 created 事件。',
        starter: 'class TicketCreate(BaseModel):\n    title: str = Field(min_length=1, max_length=120)\n    creator_id: int = Field(gt=0)\n\n@app.post("/tickets", status_code=201)\ndef create_ticket(...):\n    ...',
        checks: ['标题去除首尾空格后非空', '创建与事件同一事务', '查询不存在返回 404', '至少一条端到端测试'],
      },
      quiz: [
        makeQuiz('综合项目最合适的第一步是？', ['先创建所有未来可能的抽象', '跑通一个最小端到端主链路', '先写缓存层', '先设计微服务拆分'], 1, '纵向切片', '最小端到端链路能尽早验证模型、数据库、业务和 HTTP 是否正确连接。', '用一句话写出创建工单从请求到响应的完整路径。'),
        makeQuiz('创建工单和记录 created 事件应如何提交？', ['两个独立事务', '同一个事务', '只保存事件', '依赖定时任务最终补齐'], 1, '事务一致性', '事件描述工单创建事实，两者应共同成功或共同失败。', '让事件写入违反约束，确认工单也未保存。'),
      ],
      tip: '今天不做通知、搜索和缓存。主链路稳定后，未来需求自己会说明是否需要它们。',
    }),
    makeDay({
      day: 27,
      week: 4,
      title: '综合项目二：测试、错误与文档',
      intro: '把能运行的项目变成别人能信任的项目。今天补齐失败路径、测试、日志和使用说明。',
      goals: ['覆盖核心业务和 API 契约。', '演练数据库、权限和上游失败。', '让新开发者按 README 独立启动。'],
      concepts: [
        { title: '风险驱动测试', body: '优先测试非法状态、重复操作、权限和事务回滚。简单字段 getter 不需要单独测试。', code: 'def test_cannot_resolve_open_ticket(service):\n    with pytest.raises(InvalidTransition):\n        service.resolve(ticket_id)' },
        { title: '故障演练', body: '主动制造数据库约束失败、错误密钥和外部超时，确认响应确定且数据没有半完成。', code: 'with pytest.raises(sqlite3.IntegrityError):\n    create_invalid_event(conn)' },
        { title: '交付文档', body: 'README 最少包含环境要求、安装、配置、启动、测试、示例请求和已知限制。命令必须从新环境验证。', code: 'python -m venv .venv\nsource .venv/bin/activate\npip install -r requirements.txt\nuvicorn app.main:app --reload' },
      ],
      lab: {
        title: '达到交付门槛',
        brief: '补齐工单状态、权限、404、回滚和列表测试，写可复现 README 与示例请求。',
        starter: '# README 必须包含\n1. 安装\n2. 环境变量\n3. 启动\n4. 测试\n5. 示例请求\n6. 已知限制',
        checks: ['pytest 全部通过', 'ruff check 全部通过', '敏感日志已检查', '新环境按 README 验证'],
      },
      quiz: [
        makeQuiz('综合项目最优先补哪类测试？', ['属性 getter', '高风险业务失败和事务边界', '每行代码一个测试', '框架内部实现'], 1, '测试优先级', '非法状态、权限和回滚直接影响业务正确性与数据安全。', '按影响和发生概率给当前缺失测试排序。'),
        makeQuiz('README 中最重要的质量标准是？', ['字数超过五千', '新开发者能按命令复现运行和测试', '使用复杂架构图', '包含所有源代码'], 1, '交付文档', '文档首先是可执行的交付接口，命令必须在干净环境验证。', '从新虚拟环境逐条复制 README 命令，记录任何隐含步骤。'),
      ],
      tip: '已知限制写清楚比假装完整更专业。不要用未实现的路线图填充交付。',
    }),
    makeDay({
      day: 28,
      week: 4,
      title: '交付、总测与后续路线',
      intro: '最后一天把知识变成可证明的能力。你会演示、答题、限时实现并制定下一阶段路线。',
      goals: ['完成项目演示和代码评审。', '通过概念与业务实现总测。', '根据真实目标选择后续专项。'],
      concepts: [
        { title: '演示结构', body: '十分钟演示依次说明问题、核心数据、主流程、失败路径、测试证据和已知限制。不要逐文件朗读。', code: '1. 用户问题\n2. 核心模型\n3. 创建与流转\n4. 一次失败演练\n5. 测试结果\n6. 已知限制' },
        { title: '代码评审', body: '检查命名、边界、异常、事务、测试、日志、安全和依赖。优先修影响正确性和数据安全的问题。', code: 'ruff check .\npytest -q\nrg "except:|API_KEY|password|SELECT|UPDATE" app tests' },
        { title: '后续路线', body: '常规后台优先继续 FastAPI 或学习 Django；数据方向学习 pandas 和 SQL；任务系统学习队列；高并发方向深入 asyncio 与部署。', code: '选择标准：当前工作需求 > 项目缺口 > 兴趣探索' },
      ],
      lab: {
        title: '60 分钟业务实现题',
        brief: '为工单系统增加评论：创建评论、列表、权限校验、长度限制、事务记录和 API 测试。',
        starter: 'class CommentCreate(BaseModel):\n    body: str = Field(min_length=1, max_length=1000)\n\n@app.post("/tickets/{ticket_id}/comments", status_code=201)\ndef create_comment(...):\n    ...',
        checks: ['先写接口和失败情况', '评论与事件同事务', '不存在工单返回 404', '至少成功、空评论和无权限测试'],
      },
      quiz: [
        makeQuiz('项目演示最有说服力的内容是？', ['逐行朗读源代码', '展示主流程、失败处理和测试证据', '只展示目录树', '只讨论未来计划'], 1, '技术沟通', '业务主链路和失败证据能直接说明系统是否可靠。', '把演示压缩成六张提示卡，每张只写一句话。'),
        makeQuiz('完成本课程后选择下一技能的首要依据是？', ['哪个名词最热门', '当前工作需求和项目真实缺口', '哪个框架文件最多', '随机选择'], 1, '学习路线', '以实际交付需要选择专项，能让知识进入反馈循环并减少无效学习。', '写下未来三个月最可能承担的 Python 任务，再选择一个专项。'),
      ],
      tip: '总测不是结束。把综合项目用于一次真实小需求，才会把知识转成长期能力。',
    }),
  ],
};
