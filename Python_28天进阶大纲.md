# Python 28 天业务开发进阶大纲

> 适合对象：具备 JavaScript、HTML、CSS 和前端工程经验，希望系统转入 Python 业务开发的工程师  
> 学习周期：28 天，每天约 4 小时，总计约 112 小时  
> 最终产出：一个带测试、SQLite 持久化、日志、配置和 API 文档的 FastAPI 业务服务

## 一、学习目标

完成课程后，你应当能够：

1. 熟练使用 Python 语法、内置数据结构、函数、类、异常和文件系统 API。
2. 理解 Python 与 JavaScript 在类型、作用域、异步、模块和对象模型上的关键差异。
3. 使用类型标注、虚拟环境、Ruff、pytest 和日志构建可维护项目。
4. 使用 FastAPI、Pydantic、SQLite 完成常见 CRUD 和业务规则。
5. 编写单元测试与 API 集成测试，定位常见错误并给出修复方案。
6. 完成环境配置、依赖锁定、启动脚本、接口文档和交付说明。

## 二、每天的 4 小时节奏

| 时间 | 内容 | 产出 |
|---|---|---|
| 00:00-00:45 | 阅读当天知识点和示例 | 概念笔记 |
| 00:45-01:45 | 跟写与改写示例 | 可运行的小程序 |
| 01:45-03:15 | 完成当天代码任务 | 业务练习代码 |
| 03:15-03:45 | 随堂测验与纠错 | 测验结果、错题原因 |
| 03:45-04:00 | 复盘与提交 | 3 条学习总结 |

不要只看代码。每个示例至少亲自输入一次，再主动修改输入、边界条件和异常路径。

### 每节课的零基础教学结构

1. **为什么要学**：先说明这个知识解决什么问题，以及它与前一天的关系。
2. **前置术语**：在正文出现前，用日常语言解释新名词。
3. **连续讲解**：每个主题至少包含概念背景、运行机制和使用边界。
4. **可运行示例**：示例可以直接复制到编辑器运行。
5. **逐步执行**：按解释器真实执行顺序拆解每一步发生了什么。
6. **前端类比**：连接已有的 JavaScript、浏览器和前端工程经验。
7. **理解检查**：先自己回答，再展开查看解释。
8. **错误诊断**：说明常见错误的原因和修复方法。
9. **递进练习**：从跟写、改写逐步进入独立实现。
10. **完整任务和测验**：使用代码任务与即时反馈验证是否真正掌握。

## 三、前端知识迁移表

| 前端常见概念 | Python 对应概念 | 需要特别注意 |
|---|---|---|
| `const` / `let` | 普通变量、命名约定 | Python 没有语言级常量 |
| `null` / `undefined` | `None` | 使用 `is None` 判断 |
| 对象与数组 | `dict`、`list` | 可变性和哈希性不同 |
| `map/filter/reduce` | 推导式、生成器、内置函数 | 优先清晰的推导式 |
| Promise | coroutine、`asyncio.Task` | coroutine 不会自动执行 |
| ES Module | module、package | 导入路径和执行入口不同 |
| TypeScript interface | `Protocol`、`TypedDict`、数据类 | 类型提示默认不在运行时强制 |
| Jest/Vitest | pytest | fixture 和参数化是核心能力 |
| Express/Nest 路由 | FastAPI 路由与依赖 | 请求模型自动校验 |

## 第一周：语言基础与 Python 思维

### Day 1：环境、运行方式与第一段程序

- 目标：能在虚拟环境中运行脚本、模块和交互式命令。
- 知识点：解释器、REPL、缩进、注释、`print`、`input`、`__name__ == "__main__"`、虚拟环境。
- 与前端对照：Node 运行脚本和 Python 运行模块的差异。
- 代码任务：编写命令行个人名片，接收姓名、职位和学习目标并格式化输出。
- 验收：能说明全局 Python 和项目 `.venv` 的区别，脚本可从终端独立运行。
- 小测方向：入口守卫的作用、虚拟环境解决的问题。

### Day 2：变量、基础类型与字符串

- 目标：正确处理数字、布尔值、`None` 和文本。
- 知识点：动态类型、`int`、`float`、`bool`、`str`、类型转换、f-string、切片、常用字符串方法。
- 易错点：`bool` 是 `int` 的子类；浮点数不适合直接表示金额。
- 代码任务：把用户输入的商品单价和数量转换成整数分，输出格式化订单金额。
- 验收：能拒绝无效输入，金额计算不依赖二进制浮点误差。
- 小测方向：`is` 与 `==`、字符串不可变性。

### Day 3：分支、循环与模式匹配

- 目标：用清晰的控制流表达业务规则。
- 知识点：`if/elif/else`、真值、`for`、`while`、`range`、`enumerate`、`zip`、`break/continue`、`match`。
- 易错点：空容器为假；不要照搬 JavaScript 的 `switch` 思维。
- 代码任务：实现会员等级与优惠券叠加规则，输出折扣原因。
- 验收：覆盖金额边界、无会员、过期券和不可叠加券。
- 小测方向：短路求值、循环 `else` 的触发条件。

### Day 4：函数、参数与作用域

- 目标：把业务逻辑拆成可复用、可测试的函数。
- 知识点：位置参数、关键字参数、默认值、仅位置参数、仅关键字参数、`*args`、`**kwargs`、返回值、LEGB。
- 易错点：可变默认参数会被多次调用共享。
- 代码任务：实现订单金额计算器，将校验、折扣、税费和格式化拆成纯函数。
- 验收：函数职责单一；调用者能从签名理解必填参数。
- 小测方向：默认参数求值时机、局部与闭包作用域。

### Day 5：列表、元组、字典与集合

- 目标：为不同业务数据选择正确容器。
- 知识点：增删改查、切片、解包、哈希、字典遍历、集合运算、浅拷贝。
- 易错点：列表不能作为字典键；赋值不是复制。
- 代码任务：从订单行中合并相同 SKU，统计数量、去重标签并保持展示顺序。
- 验收：输入不被意外修改；结果顺序稳定。
- 小测方向：可变对象、集合去重的适用范围。

### Day 6：推导式、迭代器与生成器

- 目标：处理数据流水线，同时控制内存占用。
- 知识点：列表/字典/集合推导式、迭代协议、`iter`、`next`、`yield`、生成器表达式、惰性求值。
- 易错点：生成器只能顺序消费；复杂推导式应改成普通循环。
- 代码任务：逐行读取访问日志，惰性筛选 5xx 请求并汇总路径。
- 验收：不把整个大文件一次读入内存。
- 小测方向：可迭代对象和迭代器的区别、生成器的消费行为。

### Day 7：模块、包与第一周复盘

- 目标：把多文件代码组织成可导入的包。
- 知识点：模块搜索路径、绝对导入、相对导入、`__init__.py`、`python -m`、依赖与 `requirements.txt`。
- 代码任务：把前六天的订单计算器整理为 `order_tools` 包，并添加命令行入口。
- 阶段测试：15 道概念题 + 1 道 45 分钟代码题。
- 第一周里程碑：能独立写一个结构清楚的命令行小程序。

## 第二周：工程能力、数据与质量

### Day 8：类、实例与封装

- 目标：在状态和行为确实属于同一实体时使用类。
- 知识点：类、实例、`self`、类属性、实例属性、方法、属性、组合。
- 易错点：不要把每个函数都塞进类；优先组合而不是深继承。
- 代码任务：实现购物车实体，支持添加商品、移除商品和计算总额。
- 验收：购物车自身维护不变量，外部不能轻易制造负数量。
- 小测方向：实例属性查找、类方法和静态方法的使用场景。

### Day 9：数据类、枚举与 Python 数据模型

- 目标：用简洁模型表达稳定的领域数据。
- 知识点：`dataclass`、`field`、`Enum`、`__repr__`、`__eq__`、`__iter__`、协议式设计。
- 代码任务：定义订单、订单项和订单状态模型，实现总额属性和状态迁移。
- 验收：禁止非法状态迁移；对象打印结果便于调试。
- 小测方向：数据类自动生成的方法、枚举与魔法字符串。

### Day 10：异常、上下文管理器与日志

- 目标：区分可恢复错误、编程错误和系统错误。
- 知识点：`try/except/else/finally`、自定义异常、异常链、`with`、`logging`。
- 易错点：禁止裸 `except`；不要用返回 `None` 隐藏失败原因。
- 代码任务：为导入订单流程增加校验异常、失败日志和资源清理。
- 验收：错误信息包含上下文且不泄露敏感数据。
- 小测方向：`finally`、异常捕获范围。

### Day 11：文件、路径与序列化

- 目标：安全读写文本、JSON 和 CSV。
- 知识点：`pathlib`、编码、原子写入概念、`json`、`csv`、临时文件。
- 代码任务：实现订单 JSON 导入器，输出有效记录和错误报告。
- 验收：显式使用 UTF-8；损坏数据不会覆盖原文件。
- 小测方向：`Path` 相对路径、JSON 可表示的类型。

### Day 12：类型标注与结构化接口

- 目标：让编辑器和团队更早发现接口错误。
- 知识点：联合类型、容器泛型、`TypeAlias`、`TypedDict`、`Protocol`、`Callable`、`TypeVar` 基础。
- 易错点：类型标注不是运行时校验；不要为了类型而制造多余抽象。
- 代码任务：为订单包补齐公共函数类型，并定义一个最小仓储协议。
- 验收：公共 API 无模糊的 `Any`；调用方能获得补全。
- 小测方向：`Optional` 的真实含义、结构化子类型。

### Day 13：pytest 与测试设计

- 目标：为纯函数、异常路径和边界条件写可读测试。
- 知识点：Arrange-Act-Assert、fixture、参数化、异常断言、测试替身、覆盖率的局限。
- 代码任务：为折扣规则和状态迁移编写测试，至少覆盖 8 个有意义场景。
- 验收：测试失败信息能指出业务问题；测试之间互不依赖。
- 小测方向：fixture 生命周期、测试实现细节的危害。

### Day 14：SQLite、SQL 与事务

- 目标：使用标准库完成可靠的数据持久化。
- 知识点：表、主键、唯一约束、外键、索引、参数化 SQL、事务、提交与回滚。
- 易错点：禁止字符串拼接 SQL；不要为每一行单独提交事务。
- 代码任务：创建订单数据库，实现新增、查询、更新状态和分页列表。
- 阶段测试：数据库建模题 + 事务故障恢复题。
- 第二周里程碑：能开发带存储、日志和自动测试的 Python 包。

## 第三周：Web API 业务开发

### Day 15：HTTP 与 REST 基础

- 目标：从服务端视角理解请求和响应。
- 知识点：方法、状态码、请求头、JSON、幂等性、缓存、分页、错误结构、CORS。
- 代码任务：为订单服务设计 API 契约，写出路径、方法、请求体、响应体和错误码。
- 验收：资源命名一致；状态码能表达结果。
- 小测方向：PUT 与 PATCH、401 与 403。

### Day 16：FastAPI 路由与 Pydantic 模型

- 目标：运行第一个具有自动文档的 API。
- 知识点：应用实例、路由、路径参数、查询参数、请求模型、响应模型、OpenAPI。
- 代码任务：实现健康检查、创建订单、查询订单和订单列表接口。
- 验收：无效请求由模型拒绝；响应不泄露内部字段。
- 小测方向：请求模型与响应模型的边界。

### Day 17：依赖注入、校验与统一错误

- 目标：复用连接、配置和鉴权等横切逻辑。
- 知识点：`Depends`、校验器、异常处理器、错误码、请求上下文。
- 代码任务：封装数据库连接依赖，统一输出 `code/message/details` 错误结构。
- 验收：路由不重复打开连接；业务异常能映射为稳定响应。
- 小测方向：依赖缓存、业务校验放置位置。

### Day 18：CRUD 分层与业务规则

- 目标：让路由、业务逻辑和持久化各自清晰。
- 知识点：router、service、repository 的最小边界，DTO 与领域模型，事务边界。
- 易错点：分层不是文件越多越好；只有变化原因不同才拆分。
- 代码任务：完成订单创建、列表、详情、取消和发货流程。
- 验收：业务规则可脱离 HTTP 单独测试。
- 小测方向：事务应由谁控制、贫血路由与臃肿路由。

### Day 19：认证、授权与安全基础

- 目标：理解业务 API 的主要安全边界。
- 知识点：密码哈希、会话/令牌概念、认证与授权、最小权限、输入校验、机密配置、常见注入风险。
- 代码任务：为管理接口增加 API Key 依赖，并限制敏感操作。
- 验收：密钥不写入源码和日志；普通用户不能调用管理接口。
- 小测方向：哈希与加密、认证与授权。

### Day 20：异步、并发与超时

- 目标：正确处理 I/O 密集任务，不阻塞事件循环。
- 知识点：coroutine、`await`、Task、TaskGroup、超时、取消、同步与异步边界。
- 易错点：`async def` 中直接运行阻塞 I/O 仍会阻塞。
- 代码任务：并发查询两个模拟外部服务，设置超时并合并结果。
- 验收：任一上游超时时能降级；任务被取消时正确清理。
- 小测方向：coroutine 的启动时机、CPU 密集任务处理方式。

### Day 21：API 测试与第三周复盘

- 目标：用 HTTPX 和 pytest 验证完整请求链路。
- 知识点：测试客户端、依赖覆盖、临时数据库、成功/失败路径、契约测试思维。
- 代码任务：为订单 API 编写创建、重复提交、非法状态和分页测试。
- 阶段测试：从空目录搭建一个小型 Todo API，限时 90 分钟。
- 第三周里程碑：能独立开发并测试常见 CRUD API。

## 第四周：生产化与综合项目

### Day 22：配置、环境变量与可观察性

- 目标：让同一份代码安全运行在不同环境。
- 知识点：环境变量、配置默认值、启动校验、结构化日志、请求 ID、健康检查。
- 代码任务：为订单服务增加开发/测试配置和请求日志。
- 验收：缺少关键配置时快速失败；日志能串联一次请求。
- 小测方向：配置优先级、哪些信息不能记录。

### Day 23：项目结构与可维护边界

- 目标：根据变化方向组织代码，而不是套模板。
- 知识点：高内聚、低耦合、依赖方向、领域规则、公共 API、循环导入处理。
- 代码任务：重构综合项目，删除重复校验并让服务层成为唯一业务入口。
- 验收：同一规则只有一个实现位置；没有为未来需求准备的空抽象。
- 小测方向：何时拆模块、如何识别错误分层。

### Day 24：性能、缓存与排障

- 目标：先测量，再处理真实瓶颈。
- 知识点：时间复杂度、`timeit`、`cProfile`、数据库查询、索引、缓存失效、N+1 概念。
- 代码任务：对慢列表接口做基准，定位瓶颈并给出前后数据。
- 验收：优化有测量证据；结果正确性未改变。
- 小测方向：索引的代价、缓存不适用场景。

### Day 25：依赖、打包与自动化检查

- 目标：让其他开发者可重复安装和检查项目。
- 知识点：依赖版本范围、`pyproject.toml`、Ruff、测试命令、构建基础、CI 检查顺序。
- 代码任务：统一格式与静态检查，编写一个本地 `check` 命令。
- 验收：新环境按 README 可安装；检查命令一次通过。
- 小测方向：锁文件与版本范围、格式化和 lint 的职责。

### Day 26：综合项目一，需求与核心实现

- 项目：轻量工单系统 API。
- 需求：创建工单、分配处理人、状态流转、标签、列表筛选、操作记录。
- 当日产出：数据模型、SQLite schema、核心服务、创建和查询接口。
- 必须覆盖：标题为空、重复操作、非法状态迁移、不存在的处理人。
- 验收：先完成一条端到端主链路，再扩展次要能力。

### Day 27：综合项目二，测试、错误与文档

- 当日产出：API 集成测试、关键单元测试、统一错误、示例请求、接口说明。
- 质量门槛：核心流程测试通过；Ruff 无错误；新用户按 README 能启动。
- 故障演练：数据库写入失败、外部查询超时、非法 API Key。
- 验收：错误路径有确定响应，不用阅读源码也能调用 API。

### Day 28：交付、总测与后续路线

- 最终演示：10 分钟说明需求、架构、核心流程、测试策略和已知限制。
- 总测：25 道概念题 + 2 道代码阅读题 + 1 道 60 分钟业务实现题。
- 代码评审：命名、边界、异常、测试、日志、安全和可维护性。
- 最终里程碑：在空目录中独立创建、测试并交付一个 Python API 项目。
- 后续路线：根据工作需要选择 Django、数据处理、任务队列、云部署或更深入的异步编程。

## 四、阶段验收标准

| 阶段 | 能力要求 | 通过标准 |
|---|---|---|
| 第一周 | 写清晰的命令行程序 | 概念测验 ≥ 70%，代码任务可运行 |
| 第二周 | 写可维护、可测试、可持久化的包 | 测试覆盖关键规则，无拼接 SQL |
| 第三周 | 独立完成 CRUD API | 主流程和 4 类失败路径通过 |
| 第四周 | 完成生产化交付 | README 可复现，测试和 Ruff 全通过 |

## 五、学习成果评分

- 语法与数据处理：20 分
- 函数、类与模块设计：15 分
- 数据库与事务：15 分
- HTTP 与 FastAPI：20 分
- 测试和错误处理：15 分
- 工程化、安全与交付：15 分

总分 80 分以上表示已经具备常规 Python 业务开发能力。60-79 分应针对薄弱模块完成一次返工。低于 60 分建议从第二周开始重做关键练习。

## 六、每日复盘模板

```markdown
# Day N 复盘

## 今天能独立完成什么
- 

## 今天最容易犯的错误
- 

## 一个与 JavaScript 不同的点
- 

## 明天开始前要重做的题
- 
```

## 七、学习纪律

1. 每天至少提交一个可以运行的文件。
2. 任何异常都先阅读 traceback，再修改代码。
3. 先写输入、输出和边界，再开始写实现。
4. 每个非简单业务分支至少留下一个自动化测试。
5. 不背框架 API，优先理解数据流和失败路径。
