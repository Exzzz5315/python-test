# 练习题与项目说明

## 一、练习使用方法

网页课堂每天提供两道随堂题和一项代码任务。选择题用于即时纠错，代码题在 `练习代码/` 中完成。

建议每道代码题按以下顺序进行：

1. 用三句话写清输入、输出和失败情况。
2. 先实现最小成功路径。
3. 增加一个边界条件和一个异常路径。
4. 运行代码、测试和 Ruff。
5. 删除重复或暂时用不到的代码。

## 二、基础小题

### 1. 安全计数器

实现 `count_words(text: str) -> dict[str, int]`：

- 忽略大小写。
- 忽略首尾常见标点。
- 空文本返回空字典。
- 不修改传入参数。

### 2. 批量价格转换

实现 `to_cents(values: list[str]) -> list[int]`：

- 输入示例：`["12.30", "0.99"]`。
- 输出示例：`[1230, 99]`。
- 非法值抛出带索引的 `ValueError`。

### 3. 订单合并

给定若干 `{sku, quantity}` 字典，合并相同 SKU，并保持 SKU 第一次出现的顺序。数量小于等于 0 时拒绝处理。

### 4. 惰性日志过滤

实现生成器 `errors(lines)`，只返回状态码大于等于 500 的日志行。验证它不会一次性消费全部输入。

### 5. 状态机

实现工单状态迁移：

```text
open -> assigned -> processing -> resolved -> closed
```

允许 `resolved -> processing` 退回。其他迁移抛出 `InvalidTransition`。

## 三、工程小题

### 6. 原子配置保存

把配置先写入同目录临时文件，成功后替换正式文件。写入失败时原文件必须保持不变。

### 7. 参数化测试

为会员折扣函数设计至少 8 组参数，必须包含边界值、无优惠、叠加冲突和非法金额。

### 8. 事务回滚

在同一个事务中创建订单和两个订单项。故意让第二个订单项违反约束，确认三条数据都没有提交。

### 9. 统一错误模型

设计 API 错误：

```json
{
  "code": "ORDER_NOT_FOUND",
  "message": "订单不存在",
  "details": {"order_id": 42}
}
```

实现至少三类业务异常到 HTTP 状态码的映射。

### 10. 并发上游调用

同时请求库存服务和价格服务：

- 总超时 800ms。
- 库存超时直接失败。
- 推荐价格超时可降级为空。
- 取消请求时不得遗留后台任务。

## 四、阶段限时题

### 第一周：命令行记账工具，45 分钟

输入多行 `类别 金额 备注`，输出总额、分类汇总和最大一笔支出。非法行写入错误列表，不中断其余数据处理。

### 第二周：JSON 订单导入器，60 分钟

读取 JSON 文件，校验字段，写入 SQLite。重复订单跳过，错误记录输出到报告。整个批次必须使用事务。

### 第三周：Todo API，90 分钟

实现新增、列表、完成和删除接口，包含请求校验、404、分页和至少 6 个 API 测试。

## 五、综合项目：轻量工单系统 API

### 必做能力

- 工单创建与查询。
- 指派处理人。
- 合法状态迁移。
- 标签和列表筛选。
- 操作记录。
- API Key 保护管理操作。
- SQLite 持久化。
- 单元测试和 API 集成测试。
- 统一错误响应、日志和 README。

### 建议目录

```text
ticket_service/
├── app/
│   ├── main.py
│   ├── models.py
│   ├── service.py
│   └── repository.py
├── tests/
├── requirements.txt
└── README.md
```

只有当单个文件已经出现两个以上不同变化原因时再拆分。项目规模小时，不需要接口工厂、事件总线或通用仓储基类。

### 验收清单

- [ ] `uvicorn app.main:app --reload` 可启动。
- [ ] `/docs` 可查看并调用接口。
- [ ] 无效输入返回 422 或明确业务错误。
- [ ] 非法状态迁移不会写入数据库。
- [ ] SQL 使用参数化查询。
- [ ] 日志不包含密钥和完整敏感数据。
- [ ] `pytest` 全部通过。
- [ ] `ruff check .` 全部通过。
- [ ] 新开发者按 README 可在 10 分钟内运行。

## 六、自我诊断

遇到错题时不要只记答案，先给错误分类：

- 概念错误：不知道语言规则，回看知识点并写最小示例。
- 边界遗漏：补一条测试，再修改实现。
- API 记忆错误：查文档并记录入口，不背完整签名。
- 设计错误：重新写输入、输出、职责和失败路径。
- 粗心错误：使用 traceback、类型提示和 Ruff 定位。
